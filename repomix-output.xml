This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.devcontainer/
  devcontainer.json
.github/
  workflows/
    ci.yml
.hypothesis/
  constants/
    05003b5d7106c082
    0d3b3fdcad9394ec
    106995397ae2bf3d
    11a42b06188f0505
    1a3c2f57c0751bf4
    1d3092d8c424df83
    1fe777c7bd202e05
    2d2210d13ed3429c
    32200bad776ad762
    4416abb4093e924d
    44dff5104edbd685
    46926e31f2514b95
    4d39d059002a363c
    5450694c90f33787
    55599460d862cb2d
    55ec52dcd3a47e5f
    622e3ca106d878d8
    75e503854c8b7fc5
    7fd29d8ab83924eb
    8fc00151b57c2cff
    aadb416cbccb1f4e
    abd2f506ec370539
    ac417f1031ce8681
    ade185e83c517a1e
    b0f23aacd139da46
    b1060405a8982841
    b2e4d897dfc0ba57
    b712ebe763127196
    b9ebac38c647a1bc
    c04f228ef21af2d4
    c326761965c25402
    ce88c44d7647a3bf
    d0798f0c54aaac4c
    da39a3ee5e6b4b0d
    dc15ef223874a3ab
    dc690edcfb974253
    df4a86fb1f327905
    e17294144a5522a0
    e1ac37e50638a5b7
    e379c7b39a11f107
    e387c69cbe66d125
    e64ba8c596bee195
    e7068b01ae137f00
    eb6d189fc4afb526
    ed780645df24b02a
    f1296bf796213b43
    f4f937713280162b
    f7c3bc892fba18c9
docs/
  api.md
  cli.md
  SECRETS.md
examples/
  configs/
    debt_payoff.json
    sample.json
    simple_baseline.json
    stress_tight.fixed.json
    stress_tight.json
scripts/
  regen_golden.py
src/
  cashsim/
    analytics/
      __init__.py
      break_even.py
      snapshot.py
    api/
      v1/
        routes.py
        schemas.py
      __init__.py
      app.py
      middleware.py
      settings.py
    io/
      __init__.py
      config_io.py
      exporters.py
      gsheets.py
    planning/
      __init__.py
      planner.py
    sim/
      __init__.py
      core.py
      events.py
      gas.py
      interest.py
      reserve.py
      roll.py
      types_runtime.py
      types.py
    ui/
      __init__.py
      components.py
      formatters.py
      state.py
      streamlit_app.py
    utils/
      __init__.py
      date_utils.py
      money.py
    __init__.py
    __main__.py
    _typing_shims.py
    batch.py
    cli.py
    compare.py
    constants.py
    models.py
tests/
  contract/
    test_api_compare.py
    test_api_export.py
    test_api_simulate.py
  fixtures/
    golden/
      debt_payoff/
        metrics.json
        series.csv
      sample/
        metrics.json
        series.csv
      simple_baseline/
        metrics.json
        series.csv
      stress_tight/
        metrics.json
        series.csv
      stress_tight_fixed/
        metrics.json
        series.csv
      _meta.json
  golden/
    test_golden_scenarios.py
  integration/
    test_api_matches_cli.py
    test_cli_compare_report.py
    test_cli_compare.py
    test_cli_determinism.py
    test_cli_export.py
  test_interest_decimal.py
  test_property_adb.py
.gitignore
.monkey-mods.txt
.pre-commit-config.yaml
.repomixignore
example.json
mypy.ini
patch.diff
pyproject.toml
pyrightconfig.json
README.md
repomix.config.json
requirements.txt
run.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path=".hypothesis/constants/0d3b3fdcad9394ec">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/v1/schemas.py
# hypothesis_version: 6.148.7

[0.01, 366, 'csv', 'forbid', 'json']
</file>

<file path=".hypothesis/constants/106995397ae2bf3d">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/v1/schemas.py
# hypothesis_version: 6.148.7

[366, 'csv', 'forbid', 'json']
</file>

<file path=".hypothesis/constants/1fe777c7bd202e05">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/__init__.py
# hypothesis_version: 6.148.7

['app']
</file>

<file path=".hypothesis/constants/4416abb4093e924d">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/v1/routes.py
# hypothesis_version: 6.148.7

[400, '%.2f', '/compare', '/export', '/simulate', 'balance', 'csv', 'date', 'days', 'events_csv', 'include_events', 'kind', 'meta', 'metrics', 'raw', 'records', 'series_csv', 'series_diff', 'series_format', 'series_json', 'start', 'total_cc_balance', 'total_iou_balance']
</file>

<file path=".hypothesis/constants/55599460d862cb2d">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/v1/routes.py
# hypothesis_version: 6.148.7

[400, '%.2f', '/compare', '/export', '/simulate', 'csv', 'date', 'days', 'events_csv', 'include_events', 'kind', 'meta', 'metrics', 'raw', 'records', 'series_columns', 'series_csv', 'series_diff', 'series_format', 'series_json', 'start']
</file>

<file path=".hypothesis/constants/7fd29d8ab83924eb">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/compare.py
# hypothesis_version: 6.148.7

[0.0, 0.01, 0.5, '## Category totals', '## Metrics', '## Timeline deltas', '_a', '_b', 'a', 'amount', 'amt', 'b', 'balance', 'bill_due', 'category_totals', 'cc_extra_on_due', 'cc_extra_paid', 'cc_interest_posted', 'cc_min_paid', 'cc_mins_paid', 'coerce', 'cushion_breach_date', 'date', 'days', 'debt_payments_total', 'delta', 'delta_days', 'earn', 'first_negative_date', 'gas_bucket', 'income', 'interest_posted', 'max_abs_delta', 'max_abs_delta_date', 'mean_abs_delta', 'meta', 'metrics', 'min_balance_date', 'non_debt_bills', 'oneoff_paid', 'oneoffs_paid', 'outer', 'rmse', 'series_columns', 'series_summary', 'spend_total', 'start', 'threshold', 'timeline', 'total_cc_balance', 'total_iou_balance', '|---|---:|---:|---:|', '|---|---|---|---:|']
</file>

<file path=".hypothesis/constants/b1060405a8982841">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/middleware.py
# hypothesis_version: 6.148.7

[1000, 'server-timing', 'x-request-id']
</file>

<file path=".hypothesis/constants/c04f228ef21af2d4">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/settings.py
# hypothesis_version: 6.148.7

[8000, '*', '127.0.0.1', 'CASHSIM_', 'info']
</file>

<file path=".hypothesis/constants/c326761965c25402">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/cli.py
# hypothesis_version: 6.148.7

[366, 8000, '--a', '--b', '--config', '--days', '--events/--no-events', '--host', '--out', '--overwrite', '--port', '--reload', '--series-format', '--series/--no-series', '--start', '127.0.0.1', 'Config A (baseline).', 'Output directory.', '__file__', '__main__', 'allow_extra_args', 'balance', 'cashsim.api.app:app', 'compare.json', 'csv', 'csv|json', 'metrics', 'out', 'records', 'run', 'series', 'series_diff.csv', 'streamlit', 'total_cc_balance', 'total_iou_balance', 'ui', 'utf-8', 'uvicorn']
</file>

<file path=".hypothesis/constants/e17294144a5522a0">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/io/exporters.py
# hypothesis_version: 6.148.7

['%.2f', ',', ':', '_bal', 'balance', 'bill_due', 'cc_', 'cc_events', 'cc_extra_on_due', 'cc_interest_posted', 'cc_min_paid', 'compare.json', 'config_path', 'csv', 'date', 'days', 'earn', 'events.csv', 'fillups', 'gas_bucket', 'include_events', 'json', 'kind', 'metrics.json', 'object', 'oneoff_contribs', 'oneoff_paid', 'raw', 'records', 'run_meta.json', 'series.csv', 'series.json', 'series_diff.csv', 'series_format', 'start', 'total_cc_balance', 'total_iou_balance', 'utf-8', 'weekday']
</file>

<file path=".hypothesis/constants/ed780645df24b02a">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/api/app.py
# hypothesis_version: 6.148.7

['*', ',', '/healthz', '/v1', '1.0.0', 'CashSim API', 'ok', 'status', 'v1']
</file>

<file path=".hypothesis/constants/f4f937713280162b">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/cli.py
# hypothesis_version: 6.148.7

[0.01, 366, 8000, '--a', '--b', '--config', '--days', '--events/--no-events', '--host', '--out', '--overwrite', '--port', '--reload', '--report', '--series-format', '--series/--no-series', '--start', '--threshold', '127.0.0.1', 'Config A (baseline).', 'Output directory.', '__file__', '__main__', 'allow_extra_args', 'cashsim.api.app:app', 'compare.json', 'csv', 'csv|json', 'meta', 'metrics', 'out', 'records', 'run', 'series', 'series_columns', 'series_diff.csv', 'streamlit', 'ui', 'utf-8', 'uvicorn']
</file>

<file path="docs/api.md">
# CashSim API (v1)

## Run locally
~~~bash
cashsim api --reload
# or:
uvicorn cashsim.api.app:app --reload
~~~

## OpenAPI
- Swagger UI: http://127.0.0.1:8000/docs
- Schema: http://127.0.0.1:8000/openapi.json

## Example requests

### Simulate
~~~bash
curl -sS -X POST http://127.0.0.1:8000/v1/simulate \
  -H 'content-type: application/json' \
  -d @- <<'JSON'
{
  "start": "2025-01-01",
  "days": 10,
  "dials": {
    "current_cash": 300,
    "safety_cushion": 150,
    "weekday_earnings": 120,
    "gas_pct": 0.10,
    "gas_fill_size": 25,
    "bills": [{"name":"rent","amount":200,"due_day":5}],
    "oneoff_bills": [],
    "credit_cards": [],
    "ious": [],
    "interest_mode": "simple_daily_unposted"
  }
}
JSON
~~~

### Export (CSV)
~~~bash
curl -sS -X POST http://127.0.0.1:8000/v1/export \
  -H 'content-type: application/json' \
  -d @- <<'JSON'
{
  "start": "2025-01-01",
  "days": 10,
  "series_format": "csv",
  "include_events": true,
  "dials": {
    "current_cash": 300,
    "safety_cushion": 150,
    "weekday_earnings": 120,
    "gas_pct": 0.10,
    "gas_fill_size": 25,
    "bills": [{"name":"rent","amount":200,"due_day":5}],
    "oneoff_bills": [],
    "credit_cards": [],
    "ious": [],
    "interest_mode": "simple_daily_unposted"
  }
}
JSON
~~~
</file>

<file path="scripts/regen_golden.py">
from __future__ import annotations

import argparse
import json
import shutil
from datetime import date
from pathlib import Path

from cashsim.batch import run_from_config
from cashsim.io.exporters import write_metrics_json, write_series_csv


def _scenario_name(config_path: Path) -> str:
    # e.g. stress_tight.fixed.json -> stress_tight_fixed
    stem = config_path.stem
    return stem.replace(".", "_")


def main() -> None:
    ap = argparse.ArgumentParser(description="Regenerate CashSim golden fixtures.")
    ap.add_argument("--start", default="2025-01-01", help="YYYY-MM-DD")
    ap.add_argument("--days", type=int, default=31)
    ap.add_argument("--src", type=Path, default=Path("examples/configs"))
    ap.add_argument("--out", type=Path, default=Path("tests/fixtures/golden"))
    ap.add_argument(
        "--scenarios",
        nargs="*",
        default=None,
        help="Optional list of config basenames (without .json) to include. Example: debt_payoff stress_tight.fixed",
    )
    ap.add_argument("--overwrite", action="store_true")
    args = ap.parse_args()

    start = date.fromisoformat(args.start)

    if not args.src.exists():
        raise SystemExit(f"Source dir not found: {args.src}")

    configs = sorted(args.src.glob("*.json"))
    if args.scenarios:
        wanted = set(args.scenarios)
        configs = [p for p in configs if p.stem in wanted]

    if not configs:
        raise SystemExit("No configs found. Check --src or --scenarios.")

    args.out.mkdir(parents=True, exist_ok=True)

    meta = {
        "start": args.start,
        "days": args.days,
        "source_dir": str(args.src),
        "scenarios": [_scenario_name(p) for p in configs],
    }
    (args.out / "_meta.json").write_text(json.dumps(meta, indent=2, sort_keys=True) + "\n", encoding="utf-8")

    for cfg in configs:
        name = _scenario_name(cfg)
        dest = args.out / name

        if dest.exists() and not args.overwrite:
            raise SystemExit(f"Refusing to overwrite {dest} (use --overwrite)")

        dest.mkdir(parents=True, exist_ok=True)

        # Keep a single canonical config filename per scenario
        config_copy = dest / "config.json"
        shutil.copyfile(cfg, config_copy)

        run = run_from_config(config=config_copy, start=start, days=args.days)
        write_metrics_json(run.metrics, dest / "metrics.json")
        write_series_csv(run.df, dest / "series.csv")

        print(f"Wrote golden scenario: {name} -> {dest}")


if __name__ == "__main__":
    main()
</file>

<file path="src/cashsim/api/v1/routes.py">
from __future__ import annotations

from io import StringIO
from typing import Any

import pandas as pd
from fastapi import APIRouter, HTTPException

from cashsim.compare import metrics_delta, series_diff
from cashsim.io.exporters import EVENT_COLUMNS, metrics_to_dict, normalize_series_for_export
from cashsim.sim.core import simulate_month
from cashsim.compare import build_compare_payload, series_diff


from .schemas import (
    CompareRequest,
    CompareResponse,
    ExportRequest,
    ExportResponse,
    SimMetricsWire,
    SimulateRequest,
    SimulateResponse,
)

router = APIRouter()


def _series_records_raw(df: pd.DataFrame) -> list[dict[str, Any]]:
    """Match CLI `cashsim simulate`: engine raw series records."""
    return df.to_dict(orient="records")


def _series_records_export(df: pd.DataFrame) -> list[dict[str, Any]]:
    """Match CLI `cashsim export --series-format json`: normalized, stable records."""
    out = normalize_series_for_export(df)
    return out.to_dict(orient="records")


def _series_csv_export(df: pd.DataFrame) -> str:
    """Match CLI `cashsim export --series-format csv`: normalized, stable CSV."""
    out = normalize_series_for_export(df)
    buf = StringIO()

    # lineterminator avoids Windows-style CRLF surprises in tests
    out.to_csv(buf, index=False, lineterminator="\n", float_format="%.2f")
    return buf.getvalue()


def _events_csv_export(df: pd.DataFrame) -> str:
    """Flatten per-day nested event lists into a single CSV string."""

    rows: list[dict[str, object]] = []

    if "date" not in df.columns:
        raise ValueError("series df missing 'date' column")

    for _, r in df.iterrows():
        day = r["date"]
        for col in EVENT_COLUMNS:
            if col not in df.columns:
                continue
            events = r[col] or []
            for ev in events:
                if isinstance(ev, dict):
                    row = {"date": day, "kind": col}
                    row.update(ev)
                    rows.append(row)
                else:
                    rows.append({"date": day, "kind": col, "raw": ev})

    out = pd.DataFrame.from_records(rows)
    if len(out) == 0:
        out = pd.DataFrame(columns=["date", "kind"])

    # Stable date serialization
    if "date" in out.columns:
        out["date"] = pd.to_datetime(out["date"]).dt.date.astype(str)

    buf = StringIO()
    out.to_csv(buf, index=False, lineterminator="\n")
    return buf.getvalue()


@router.post("/simulate", response_model=SimulateResponse)
def simulate(req: SimulateRequest) -> SimulateResponse:
    try:
        df, metrics = simulate_month(req.dials, start=req.start, days=req.days)
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e)) from e

    return SimulateResponse(
        metrics=SimMetricsWire.model_validate(metrics_to_dict(metrics)),
        series=_series_records_raw(df),
    )


@router.post("/compare", response_model=CompareResponse)
def compare(req: CompareRequest) -> CompareResponse:
    try:
        df_a, m_a = simulate_month(req.a, start=req.start, days=req.days)
        df_b, m_b = simulate_month(req.b, start=req.start, days=req.days)
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e)) from e

    payload = build_compare_payload(
        metrics_a=m_a,
        metrics_b=m_b,
        df_a=df_a,
        df_b=df_b,
        start=req.start.isoformat(),
        days=req.days,
        threshold=req.threshold,
    )

    # API keeps series_diff optional
    if req.series:
        cols = payload.get("meta", {}).get("series_columns", [])
        diff = series_diff(df_a, df_b, columns=list(cols))
        payload["series_diff"] = diff.to_dict(orient="records")
    else:
        payload["series_diff"] = None

    return CompareResponse(**payload)


@router.post("/export", response_model=ExportResponse)
def export(req: ExportRequest) -> ExportResponse:
    try:
        df, metrics = simulate_month(req.dials, start=req.start, days=req.days)
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e)) from e

    metrics_wire = SimMetricsWire.model_validate(metrics_to_dict(metrics))
    meta = {
        "start": req.start.isoformat(),
        "days": req.days,
        "series_format": req.series_format,
        "include_events": req.include_events,
    }

    payload: dict[str, Any] = {"metrics": metrics_wire, "meta": meta}

    if req.series_format == "csv":
        payload["series_csv"] = _series_csv_export(df)
    else:
        payload["series_json"] = _series_records_export(df)

    if req.include_events:
        payload["events_csv"] = _events_csv_export(df)

    return ExportResponse(**payload)
</file>

<file path="src/cashsim/api/v1/schemas.py">
from __future__ import annotations

from datetime import date
from typing import Any, Literal

from pydantic import BaseModel, ConfigDict, Field

from cashsim.models import Dials


class SimulateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    dials: Dials
    start: date = Field(..., description="Start date (YYYY-MM-DD)")
    days: int = Field(31, ge=1, le=366)


class SimMetricsWire(BaseModel):
    """Wire-safe metrics (date fields are ISO strings or null)."""

    model_config = ConfigDict(extra="forbid")

    min_balance: float
    min_balance_date: str | None
    first_negative_date: str | None
    total_upcoming_bills: float
    friend_ask_needed: float
    friend_ask_latest_date: str | None
    cushion_breach_date: str | None
    accrued_interest_estimate: float


class SimulateResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")

    metrics: SimMetricsWire
    series: list[dict[str, Any]]


class CompareRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    a: Dials
    b: Dials
    start: date
    days: int = Field(31, ge=1, le=366)
    series: bool = False
    threshold: float = Field(0.01, ge=0)


class CompareResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")

    metrics: dict[str, Any]
    timeline: dict[str, Any]
    category_totals: dict[str, Any]
    series_summary: dict[str, Any]
    meta: dict[str, Any]
    series_diff: list[dict[str, Any]] | None = None


class ExportRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    dials: Dials
    start: date
    days: int = Field(31, ge=1, le=366)
    series_format: Literal["csv", "json"] = "csv"
    include_events: bool = False


class ExportResponse(BaseModel):
    model_config = ConfigDict(extra="forbid")

    metrics: SimMetricsWire
    meta: dict[str, Any]

    # Exactly one of these should be present based on series_format:
    series_csv: str | None = None
    series_json: list[dict[str, Any]] | None = None

    # Present only if include_events=true
    events_csv: str | None = None
</file>

<file path="src/cashsim/api/__init__.py">
from .app import app

__all__ = ["app"]
</file>

<file path="src/cashsim/api/app.py">
from __future__ import annotations

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse

from cashsim.api.middleware import RequestIdMiddleware, TimingMiddleware
from cashsim.api.settings import ApiSettings
from cashsim.api.v1.routes import router as v1_router


def create_app() -> FastAPI:
    settings = ApiSettings()

    app = FastAPI(
        title="CashSim API",
        version="1.0.0",
        default_response_class=JSONResponse,
    )

    # Versioned API surface
    app.include_router(v1_router, prefix="/v1", tags=["v1"])

    # Middleware
    app.add_middleware(RequestIdMiddleware)
    app.add_middleware(TimingMiddleware)

    if settings.allow_cors:
        app.add_middleware(
            CORSMiddleware,
            allow_origins=[o.strip() for o in settings.cors_allow_origins.split(",") if o.strip()],
            allow_credentials=False,
            allow_methods=["*"] ,
            allow_headers=["*"] ,
        )

    @app.get("/healthz")
    def healthz() -> dict[str, str]:
        return {"status": "ok"}

    return app


app = create_app()
</file>

<file path="src/cashsim/api/middleware.py">
from __future__ import annotations

import time
import uuid

from fastapi import Request
from starlette.middleware.base import BaseHTTPMiddleware


class RequestIdMiddleware(BaseHTTPMiddleware):
    """Adds an X-Request-Id header to every response.

    Helpful for debugging and log correlation.
    """

    async def dispatch(self, request: Request, call_next):
        request_id = request.headers.get("x-request-id") or str(uuid.uuid4())
        request.state.request_id = request_id
        response = await call_next(request)
        response.headers["x-request-id"] = request_id
        return response


class TimingMiddleware(BaseHTTPMiddleware):
    """Adds simple server timing header for quick profiling."""

    async def dispatch(self, request: Request, call_next):
        start = time.perf_counter()
        response = await call_next(request)
        ms = (time.perf_counter() - start) * 1000
        response.headers["server-timing"] = f"app;dur={ms:.2f}"
        return response
</file>

<file path="src/cashsim/api/settings.py">
from __future__ import annotations

from pydantic_settings import BaseSettings, SettingsConfigDict


class ApiSettings(BaseSettings):
    """Runtime settings for the API process (env-overridable)."""

    model_config = SettingsConfigDict(env_prefix="CASHSIM_", case_sensitive=False)

    host: str = "127.0.0.1"
    port: int = 8000
    log_level: str = "info"

    # Basic hardening toggles
    allow_cors: bool = False
    cors_allow_origins: str = "*"
</file>

<file path="tests/contract/test_api_compare.py">
from __future__ import annotations

from fastapi.testclient import TestClient

from cashsim.api.app import app

client = TestClient(app)


def _base() -> dict:
    return {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.10,
        "gas_fill_size": 25.0,
        "bills": [{"name": "rent", "amount": 200.0, "usual_day": 5}],
        "oneoffs": [],
        "credit_cards": [],
        "ious": [],
        "interest_mode": "statement_adb",
    }


def test_compare_metrics_delta_deterministic() -> None:
    a = _base()
    b = {**_base(), "weekday_earnings": 130.0}

    payload = {"a": a, "b": b, "start": "2025-01-01", "days": 10, "series": False}
    r1 = client.post("/v1/compare", json=payload)
    r2 = client.post("/v1/compare", json=payload)

    assert r1.status_code == 200, r1.text
    assert r2.status_code == 200, r2.text
    assert r1.json() == r2.json()
</file>

<file path="tests/contract/test_api_export.py">
from __future__ import annotations

from fastapi.testclient import TestClient

from cashsim.api.app import app

client = TestClient(app)


def _dials_dict() -> dict:
    return {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.10,
        "gas_fill_size": 25.0,
        "bills": [{"name": "rent", "amount": 200.0, "usual_day": 5}],
        "oneoffs": [],
        "credit_cards": [],
        "ious": [],
        "interest_mode": "statement_adb",
    }


def test_export_csv_has_series_csv_field() -> None:
    payload = {
        "dials": _dials_dict(),
        "start": "2025-01-01",
        "days": 10,
        "series_format": "csv",
        "include_events": False,
    }
    r = client.post("/v1/export", json=payload)
    assert r.status_code == 200, r.text

    body = r.json()
    assert "metrics" in body
    assert "meta" in body
    assert body.get("series_csv")
    assert body.get("series_json") is None


def test_export_json_has_series_json_field() -> None:
    payload = {
        "dials": _dials_dict(),
        "start": "2025-01-01",
        "days": 10,
        "series_format": "json",
        "include_events": False,
    }
    r = client.post("/v1/export", json=payload)
    assert r.status_code == 200, r.text

    body = r.json()
    assert isinstance(body.get("series_json"), list)
    assert body.get("series_csv") is None
</file>

<file path="tests/contract/test_api_simulate.py">
from __future__ import annotations

from fastapi.testclient import TestClient

from cashsim.api.app import app

client = TestClient(app)


def _dials_dict() -> dict:
    return {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.10,
        "gas_fill_size": 25.0,
        "bills": [{"name": "rent", "amount": 200.0, "usual_day": 5}],
        "oneoffs": [],
        "credit_cards": [],
        "ious": [],
        "interest_mode": "statement_adb",
    }


def test_simulate_valid_payload_200_and_schema_stable() -> None:
    payload = {"dials": _dials_dict(), "start": "2025-01-01", "days": 10}
    r = client.post("/v1/simulate", json=payload)
    assert r.status_code == 200, r.text

    body = r.json()
    assert set(body.keys()) == {"metrics", "series"}
    assert "min_balance" in body["metrics"]
    assert isinstance(body["series"], list)
    assert len(body["series"]) == 10


def test_simulate_invalid_payload_422() -> None:
    # Missing required field: dials
    r = client.post("/v1/simulate", json={"start": "2025-01-01", "days": 10})
    assert r.status_code == 422
</file>

<file path="tests/fixtures/golden/debt_payoff/metrics.json">
{
  "accrued_interest_estimate": 20.97,
  "cushion_breach_date": "2025-01-01",
  "first_negative_date": "2025-01-01",
  "friend_ask_latest_date": "2025-01-01",
  "friend_ask_needed": 410.2,
  "min_balance": -410.2,
  "min_balance_date": "2025-01-01",
  "total_upcoming_bills": 1041.0
}
</file>

<file path="tests/fixtures/golden/debt_payoff/series.csv">
date,weekday,earn,bill_due,balance,gas_bucket,fillups,cc_events,cc_interest_posted,cc_min_paid,cc_extra_on_due,oneoff_contribs,oneoff_paid,total_cc_balance,total_iou_balance,accrued_interest_unposted,cc_card_a_bal,cc_card_b_bal
2025-01-01,Wed,140.00,850.00,-410.20,0.20,1,[],[],[],[],[],[],2700.00,0,1.68,1800.00,900.00
2025-01-02,Thu,140.00,0.00,-320.40,0.40,1,[],[],[],[],[],[],2700.00,0,3.35,1800.00,900.00
2025-01-03,Fri,140.00,0.00,-230.60,0.60,1,[],[],[],[],[],[],2700.00,0,5.03,1800.00,900.00
2025-01-04,Sat,140.00,0.00,-167.80,0.80,1,"[{""date"":""2025-01-04"",""account"":""Card B"",""finance_charge"":0.0,""minimum_paid"":27.0,""extra_paid"":0.0,""new_balance"":873.0}]",[],"[[""Card B"",27.0]]",[],[],[],2673.00,0,6.70,1800.00,873.00
2025-01-05,Sun,140.00,0.00,-78.00,1.00,1,[],[],[],[],[],[],2673.00,0,8.37,1800.00,873.00
2025-01-06,Mon,140.00,0.00,11.80,1.20,1,[],[],[],[],[],[],2673.00,0,10.03,1800.00,873.00
2025-01-07,Tue,140.00,0.00,101.60,1.40,1,[],[],[],[],[],[],2673.00,0,11.69,1800.00,873.00
2025-01-08,Wed,140.00,0.00,191.40,1.60,1,[],[],[],[],[],[],2673.00,0,13.35,1800.00,873.00
2025-01-09,Thu,140.00,0.00,281.20,1.80,1,[],[],[],[],[],[],2673.00,0,15.02,1800.00,873.00
2025-01-10,Fri,140.00,0.00,371.00,2.00,1,"[{""date"":""2025-01-10"",""account"":""Card B"",""finance_charge"":4.36,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":877.36}]","[[""Card B"",4.36]]",[],[],[],[],2677.36,0,12.32,1800.00,877.36
2025-01-11,Sat,140.00,0.00,460.80,2.20,1,[],[],[],[],[],[],2677.36,0,13.99,1800.00,877.36
2025-01-12,Sun,140.00,0.00,550.60,2.40,1,[],[],[],[],[],[],2677.36,0,15.65,1800.00,877.36
2025-01-13,Mon,140.00,0.00,640.40,2.60,1,[],[],[],[],[],[],2677.36,0,17.32,1800.00,877.36
2025-01-14,Tue,140.00,0.00,485.00,2.80,1,"[{""date"":""2025-01-14"",""account"":""Card A"",""finance_charge"":0.0,""minimum_paid"":54.0,""extra_paid"":0.0,""new_balance"":1746.0},{""date"":""2025-01-14"",""account"":""Card A"",""finance_charge"":0.0,""minimum_paid"":0.0,""extra_paid"":191.2,""new_balance"":1554.8,""surplus_used"":191.2,""reserve_needed"":485.0,""reserve_7d_bills"":110.0,""reserve_7d_mins"":0.0,""reserve_7d_gas"":175.0}]",[],"[[""Card A"",54.0]]","[[""Card A"",191.2]]",[],[],2432.16,0,18.98,1554.80,877.36
2025-01-15,Wed,140.00,110.00,464.80,3.00,1,[],[],[],[],[],[],2432.16,0,20.48,1554.80,877.36
2025-01-16,Thu,140.00,0.00,554.60,3.20,1,[],[],[],[],[],[],2432.16,0,21.98,1554.80,877.36
2025-01-17,Fri,140.00,0.00,644.40,3.40,1,[],[],[],[],[],[],2432.16,0,23.47,1554.80,877.36
2025-01-18,Sat,140.00,0.00,734.20,3.60,1,[],[],[],[],[],[],2432.16,0,24.97,1554.80,877.36
2025-01-19,Sun,140.00,0.00,824.00,3.80,1,[],[],[],[],[],[],2432.16,0,26.47,1554.80,877.36
2025-01-20,Mon,140.00,0.00,913.80,4.00,1,"[{""date"":""2025-01-20"",""account"":""Card A"",""finance_charge"":23.64,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":1578.44}]","[[""Card A"",23.64]]",[],[],[],[],2455.80,0,4.32,1578.44,877.36
2025-01-21,Tue,140.00,0.00,1003.60,4.20,1,[],[],[],[],[],[],2455.80,0,5.84,1578.44,877.36
2025-01-22,Wed,140.00,0.00,1093.40,4.40,1,[],[],[],[],[],[],2455.80,0,7.35,1578.44,877.36
2025-01-23,Thu,140.00,0.00,1183.20,4.60,1,[],[],[],[],[],[],2455.80,0,8.86,1578.44,877.36
2025-01-24,Fri,140.00,0.00,1273.00,4.80,1,[],[],[],[],[],[],2455.80,0,10.38,1578.44,877.36
2025-01-25,Sat,140.00,0.00,1362.80,5.00,1,[],[],[],[],[],[],2455.80,0,11.89,1578.44,877.36
2025-01-26,Sun,140.00,0.00,1452.60,5.20,1,[],[],[],[],[],[],2455.80,0,13.40,1578.44,877.36
2025-01-27,Mon,140.00,0.00,1542.40,5.40,1,[],[],[],[],[],[],2455.80,0,14.92,1578.44,877.36
2025-01-28,Tue,140.00,0.00,1632.20,5.60,1,[],[],[],[],[],[],2455.80,0,16.43,1578.44,877.36
2025-01-29,Wed,140.00,0.00,1722.00,5.80,1,[],[],[],[],[],[],2455.80,0,17.94,1578.44,877.36
2025-01-30,Thu,140.00,0.00,1811.80,6.00,1,[],[],[],[],[],[],2455.80,0,19.46,1578.44,877.36
2025-01-31,Fri,140.00,0.00,1901.60,6.20,1,[],[],[],[],[],[],2455.80,0,20.97,1578.44,877.36
</file>

<file path="tests/fixtures/golden/sample/metrics.json">
{
  "accrued_interest_estimate": 307.28,
  "cushion_breach_date": null,
  "first_negative_date": null,
  "friend_ask_latest_date": null,
  "friend_ask_needed": 0.0,
  "min_balance": 31.0,
  "min_balance_date": "2025-01-01",
  "total_upcoming_bills": 988.08
}
</file>

<file path="tests/fixtures/golden/sample/series.csv">
date,weekday,earn,bill_due,balance,gas_bucket,fillups,cc_events,cc_interest_posted,cc_min_paid,cc_extra_on_due,oneoff_contribs,oneoff_paid,total_cc_balance,total_iou_balance,accrued_interest_unposted,cc_aidvantage_bal,cc_bank_of_america_bal,cc_chase_visa_bal,cc_creditone_bal,cc_dentalfirst_bal,cc_samsung_bal
2025-01-01,Wed,100.00,0.00,112.50,18.50,0,"[{""date"":""2025-01-01"",""account"":""Bank of America"",""finance_charge"":1.79,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":3072.42},{""date"":""2025-01-01"",""account"":""DentalFirst"",""finance_charge"":2.02,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":2238.02},{""date"":""2025-01-01"",""account"":""CreditOne"",""finance_charge"":0.79,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":976.92}]","[[""Bank of America"",1.79],[""DentalFirst"",2.02],[""CreditOne"",0.79]]",[],[],[],[],20450.58,1650.00,6.26,3821.26,3072.42,9141.96,976.92,2238.02,1200.00
2025-01-02,Thu,100.00,0.00,169.00,12.00,1,[],[],[],[],[],[],20450.58,1650.00,17.12,3821.26,3072.42,9141.96,976.92,2238.02,1200.00
2025-01-03,Fri,100.00,0.00,185.38,5.50,1,"[{""date"":""2025-01-03"",""account"":""Chase VISA"",""finance_charge"":18.78,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":9160.74},{""date"":""2025-01-03"",""account"":""Samsung"",""finance_charge"":0.0,""minimum_paid"":40.12,""extra_paid"":0.0,""new_balance"":1159.88}]","[[""Chase VISA"",18.78]]","[[""Samsung"",40.12]]",[],[],[],20429.24,1650.00,9.20,3821.26,3072.42,9160.74,976.92,2238.02,1159.88
2025-01-04,Sat,100.00,0.00,266.88,24.00,0,[],[],[],[],[],[],20429.24,1650.00,20.07,3821.26,3072.42,9160.74,976.92,2238.02,1159.88
2025-01-05,Sun,100.00,12.00,227.20,17.50,1,"[{""date"":""2025-01-05"",""account"":""Bank of America"",""finance_charge"":0.0,""minimum_paid"":84.18,""extra_paid"":0.0,""new_balance"":2988.24}]",[],"[[""Bank of America"",84.18]]",[],[],[],20345.06,1650.00,30.94,3821.26,2988.24,9160.74,976.92,2238.02,1159.88
2025-01-06,Mon,100.00,0.00,283.70,11.00,1,[],[],[],[],[],[],20345.06,1650.00,41.77,3821.26,2988.24,9160.74,976.92,2238.02,1159.88
2025-01-07,Tue,100.00,0.00,340.20,4.50,1,[],[],[],[],[],[],20345.06,1650.00,52.59,3821.26,2988.24,9160.74,976.92,2238.02,1159.88
2025-01-08,Wed,100.00,0.00,421.70,23.00,0,[],[],[],[],[],[],20345.06,1650.00,63.41,3821.26,2988.24,9160.74,976.92,2238.02,1159.88
2025-01-09,Thu,100.00,0.00,478.20,16.50,1,[],[],[],[],[],[],20345.06,1650.00,74.24,3821.26,2988.24,9160.74,976.92,2238.02,1159.88
2025-01-10,Fri,100.00,0.00,453.68,10.00,1,"[{""date"":""2025-01-10"",""account"":""DentalFirst"",""finance_charge"":0.0,""minimum_paid"":81.02,""extra_paid"":0.0,""new_balance"":2157.0}]",[],"[[""DentalFirst"",81.02]]",[],[],[],20264.04,1650.00,85.06,3821.26,2988.24,9160.74,976.92,2157.00,1159.88
2025-01-11,Sat,100.00,11.00,461.84,3.50,1,"[{""date"":""2025-01-11"",""account"":""Aidvantage"",""finance_charge"":0.0,""minimum_paid"":0.0,""extra_paid"":11.0,""new_balance"":3810.26,""surplus_used"":11.0,""reserve_needed"":461.84,""reserve_7d_bills"":187.99,""reserve_7d_mins"":48.85,""reserve_7d_gas"":125.0}]",[],[],"[[""Aidvantage"",11.0]]","[[""Auto Repair Service"",26.34]]",[],20253.04,1650.00,95.81,3810.26,2988.24,9160.74,976.92,2157.00,1159.88
2025-01-12,Sun,100.00,11.00,486.84,22.00,0,[],[],[],[],"[[""Auto Repair Service"",45.5]]",[],20253.04,1650.00,106.56,3810.26,2988.24,9160.74,976.92,2157.00,1159.88
2025-01-13,Mon,100.00,146.99,255.00,15.50,1,"[{""date"":""2025-01-13"",""account"":""CreditOne"",""finance_charge"":0.0,""minimum_paid"":48.85,""extra_paid"":0.0,""new_balance"":928.07},{""date"":""2025-01-13"",""account"":""CreditOne"",""finance_charge"":0.0,""minimum_paid"":0.0,""extra_paid"":92.5,""new_balance"":835.57,""surplus_used"":92.5,""reserve_needed"":255.0,""reserve_7d_bills"":30.0,""reserve_7d_mins"":0.0,""reserve_7d_gas"":125.0}]",[],"[[""CreditOne"",48.85]]","[[""CreditOne"",92.5]]",[],[],20111.69,1650.00,117.31,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-14,Tue,100.00,0.00,264.06,9.00,1,[],[],[],[],"[[""Auto Repair Service"",47.44]]",[],20111.69,1650.00,127.94,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-15,Wed,100.00,20.00,264.06,2.50,1,[],[],[],[],"[[""Auto Repair Service"",36.5]]",[],20111.69,1650.00,138.58,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-16,Thu,100.00,0.00,269.06,21.00,0,[],[],[],[],"[[""Auto Repair Service"",76.5]]",[],20111.69,1650.00,149.21,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-17,Fri,100.00,10.00,244.06,14.50,1,[],[],[],[],"[[""Auto Repair Service"",71.5]]",[],20111.69,1650.00,159.85,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-18,Sat,100.00,0.00,300.56,8.00,1,[],[],[],[],[],[],20111.69,1650.00,170.49,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-19,Sun,100.00,0.00,357.06,1.50,1,[],[],[],[],[],[],20111.69,1650.00,181.12,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-20,Mon,100.00,9.06,429.50,20.00,0,[],[],[],[],[],[],20111.69,1650.00,191.76,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-21,Tue,100.00,0.00,486.00,13.50,1,[],[],[],[],[],[],20111.69,1650.00,202.39,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-22,Wed,100.00,0.00,542.50,7.00,1,[],[],[],[],[],[],20111.69,1650.00,213.03,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-23,Thu,100.00,0.00,599.00,0.50,1,[],[],[],[],[],[],20111.69,1650.00,223.66,3810.26,2988.24,9160.74,835.57,2157.00,1159.88
2025-01-24,Fri,100.00,105.00,268.62,19.00,0,"[{""date"":""2025-01-24"",""account"":""Chase VISA"",""finance_charge"":0.0,""minimum_paid"":306.88,""extra_paid"":0.0,""new_balance"":8853.86}]",[],"[[""Chase VISA"",306.88]]",[],[],[],19804.81,1650.00,234.30,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-25,Sat,100.00,88.00,237.12,12.50,1,[],[],[],[],[],[],19804.81,1650.00,244.72,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-26,Sun,100.00,0.00,238.98,6.00,1,[],[],[],[],"[[""Auto Repair Service"",54.64]]",[],19804.81,1650.00,255.15,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-27,Mon,100.00,0.00,263.98,24.50,0,[],[],[],[],"[[""Auto Repair Service"",56.5]]",[],19804.81,1650.00,265.58,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-28,Tue,100.00,10.99,279.10,18.00,1,[],[],[],[],"[[""Auto Repair Service"",30.39]]",[],19804.81,1650.00,276.00,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-29,Wed,100.00,2.99,268.11,11.50,1,[],[],[],[],"[[""Auto Repair Service"",64.5]]",[],19804.81,1650.00,286.43,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-30,Thu,100.00,0.00,324.61,5.00,1,[],[],[],[],[],[],19804.81,1650.00,296.85,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
2025-01-31,Fri,100.00,0.00,384.00,23.50,0,[],[],[],[],"[[""Auto Repair Service"",11.19],[""Car Key Copy for Bobbie"",10.92]]",[],19804.81,1650.00,307.28,3810.26,2988.24,8853.86,835.57,2157.00,1159.88
</file>

<file path="tests/fixtures/golden/simple_baseline/metrics.json">
{
  "accrued_interest_estimate": 0,
  "cushion_breach_date": "2025-01-01",
  "first_negative_date": "2025-01-01",
  "friend_ask_latest_date": "2025-01-01",
  "friend_ask_needed": 298.0,
  "min_balance": -298.0,
  "min_balance_date": "2025-01-01",
  "total_upcoming_bills": 1015.0
}
</file>

<file path="tests/fixtures/golden/simple_baseline/series.csv">
date,weekday,earn,bill_due,balance,gas_bucket,fillups,cc_events,cc_interest_posted,cc_min_paid,cc_extra_on_due,oneoff_contribs,oneoff_paid,total_cc_balance,total_iou_balance,accrued_interest_unposted
2025-01-01,Wed,120.00,900.00,-298.00,18.00,0,[],[],[],[],[],[],0,0,0
2025-01-02,Thu,120.00,0.00,-221.00,11.00,1,[],[],[],[],[],[],0,0,0
2025-01-03,Fri,120.00,0.00,-144.00,4.00,1,[],[],[],[],[],[],0,0,0
2025-01-04,Sat,120.00,0.00,-42.00,22.00,0,[],[],[],[],[],[],0,0,0
2025-01-05,Sun,120.00,60.00,-25.00,15.00,1,[],[],[],[],[],[],0,0,0
2025-01-06,Mon,120.00,0.00,52.00,8.00,1,[],[],[],[],[],[],0,0,0
2025-01-07,Tue,120.00,0.00,129.00,1.00,1,[],[],[],[],[],[],0,0,0
2025-01-08,Wed,120.00,0.00,231.00,19.00,0,[],[],[],[],[],[],0,0,0
2025-01-09,Thu,120.00,0.00,308.00,12.00,1,[],[],[],[],[],[],0,0,0
2025-01-10,Fri,120.00,0.00,385.00,5.00,1,[],[],[],[],[],[],0,0,0
2025-01-11,Sat,120.00,0.00,487.00,23.00,0,[],[],[],[],[],[],0,0,0
2025-01-12,Sun,120.00,55.00,509.00,16.00,1,[],[],[],[],[],[],0,0,0
2025-01-13,Mon,120.00,0.00,586.00,9.00,1,[],[],[],[],[],[],0,0,0
2025-01-14,Tue,120.00,0.00,663.00,2.00,1,[],[],[],[],[],[],0,0,0
2025-01-15,Wed,120.00,0.00,765.00,20.00,0,[],[],[],[],[],[],0,0,0
2025-01-16,Thu,120.00,0.00,842.00,13.00,1,[],[],[],[],[],[],0,0,0
2025-01-17,Fri,120.00,0.00,919.00,6.00,1,[],[],[],[],[],[],0,0,0
2025-01-18,Sat,120.00,0.00,1021.00,24.00,0,[],[],[],[],[],[],0,0,0
2025-01-19,Sun,120.00,0.00,1098.00,17.00,1,[],[],[],[],[],[],0,0,0
2025-01-20,Mon,120.00,0.00,1175.00,10.00,1,[],[],[],[],[],[],0,0,0
2025-01-21,Tue,120.00,0.00,1252.00,3.00,1,[],[],[],[],[],[],0,0,0
2025-01-22,Wed,120.00,0.00,1354.00,21.00,0,[],[],[],[],[],[],0,0,0
2025-01-23,Thu,120.00,0.00,1431.00,14.00,1,[],[],[],[],[],[],0,0,0
2025-01-24,Fri,120.00,0.00,1508.00,7.00,1,[],[],[],[],[],[],0,0,0
2025-01-25,Sat,120.00,0.00,1585.00,0.00,1,[],[],[],[],[],[],0,0,0
2025-01-26,Sun,120.00,0.00,1687.00,18.00,0,[],[],[],[],[],[],0,0,0
2025-01-27,Mon,120.00,0.00,1764.00,11.00,1,[],[],[],[],[],[],0,0,0
2025-01-28,Tue,120.00,0.00,1841.00,4.00,1,[],[],[],[],[],[],0,0,0
2025-01-29,Wed,120.00,0.00,1943.00,22.00,0,[],[],[],[],[],[],0,0,0
2025-01-30,Thu,120.00,0.00,2020.00,15.00,1,[],[],[],[],[],[],0,0,0
2025-01-31,Fri,120.00,0.00,2097.00,8.00,1,[],[],[],[],[],[],0,0,0
</file>

<file path="tests/fixtures/golden/stress_tight/metrics.json">
{
  "accrued_interest_estimate": 16.07,
  "cushion_breach_date": "2025-01-01",
  "first_negative_date": "2025-01-01",
  "friend_ask_latest_date": "2025-01-01",
  "friend_ask_needed": 704.0,
  "min_balance": -704.0,
  "min_balance_date": "2025-01-01",
  "total_upcoming_bills": 1106.0
}
</file>

<file path="tests/fixtures/golden/stress_tight/series.csv">
date,weekday,earn,bill_due,balance,gas_bucket,fillups,cc_events,cc_interest_posted,cc_min_paid,cc_extra_on_due,oneoff_contribs,oneoff_paid,total_cc_balance,total_iou_balance,accrued_interest_unposted,cc_high_apr_bal
2025-01-01,Wed,95.00,900.00,-704.00,19.00,0,[],[],[],[],[],[],2200.00,0,1.81,2200.00
2025-01-02,Thu,95.00,0.00,-653.00,13.00,1,[],[],[],[],[],[],2200.00,0,3.62,2200.00
2025-01-03,Fri,95.00,0.00,-602.00,7.00,1,[],[],[],[],[],[],2200.00,0,5.42,2200.00
2025-01-04,Sat,95.00,0.00,-551.00,1.00,1,[],[],[],[],[],[],2200.00,0,7.23,2200.00
2025-01-05,Sun,95.00,0.00,-475.00,20.00,0,[],[],[],[],[],[],2200.00,0,9.04,2200.00
2025-01-06,Mon,95.00,0.00,-424.00,14.00,1,[],[],[],[],[],[],2200.00,0,10.85,2200.00
2025-01-07,Tue,95.00,0.00,-373.00,8.00,1,[],[],[],[],[],[],2200.00,0,12.65,2200.00
2025-01-08,Wed,95.00,0.00,-322.00,2.00,1,[],[],[],[],[],[],2200.00,0,14.46,2200.00
2025-01-09,Thu,95.00,0.00,-246.00,21.00,0,[],[],[],[],[],[],2200.00,0,16.27,2200.00
2025-01-10,Fri,95.00,0.00,-195.00,15.00,1,[],[],[],[],[],[],2200.00,0,18.08,2200.00
2025-01-11,Sat,95.00,0.00,-144.00,9.00,1,[],[],[],[],[],[],2200.00,0,19.88,2200.00
2025-01-12,Sun,95.00,0.00,-93.00,3.00,1,[],[],[],[],[],[],2200.00,0,21.69,2200.00
2025-01-13,Mon,95.00,0.00,-17.00,22.00,0,[],[],[],[],[],[],2200.00,0,23.50,2200.00
2025-01-14,Tue,95.00,0.00,34.00,16.00,1,[],[],[],[],[],[],2200.00,0,25.31,2200.00
2025-01-15,Wed,95.00,0.00,85.00,10.00,1,[],[],[],[],[],[],2200.00,0,27.11,2200.00
2025-01-16,Thu,95.00,0.00,70.00,4.00,1,"[{""date"":""2025-01-16"",""account"":""High APR"",""finance_charge"":0.0,""minimum_paid"":66.0,""extra_paid"":0.0,""new_balance"":2134.0}]",[],"[[""High APR"",66.0]]",[],[],[],2134.00,0,28.92,2134.00
2025-01-17,Fri,95.00,0.00,146.00,23.00,0,[],[],[],[],[],[],2134.00,0,30.68,2134.00
2025-01-18,Sat,95.00,140.00,57.00,17.00,1,[],[],[],[],[],[],2134.00,0,32.43,2134.00
2025-01-19,Sun,95.00,0.00,108.00,11.00,1,[],[],[],[],[],[],2134.00,0,34.18,2134.00
2025-01-20,Mon,95.00,0.00,159.00,5.00,1,[],[],[],[],[],[],2134.00,0,35.94,2134.00
2025-01-21,Tue,95.00,0.00,235.00,24.00,0,[],[],[],[],[],[],2134.00,0,37.69,2134.00
2025-01-22,Wed,95.00,0.00,286.00,18.00,1,"[{""date"":""2025-01-22"",""account"":""High APR"",""finance_charge"":39.44,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":2173.44}]","[[""High APR"",39.44]]",[],[],[],[],2173.44,0,0.00,2173.44
2025-01-23,Thu,95.00,0.00,275.00,12.00,1,[],[],[],[],"[[""Car repair"",62.0]]",[],2173.44,0,1.79,2173.44
2025-01-24,Fri,95.00,0.00,275.00,6.00,1,[],[],[],[],"[[""Car repair"",51.0]]",[],2173.44,0,3.57,2173.44
2025-01-25,Sat,95.00,0.00,275.00,0.00,1,[],[],[],[],"[[""Car repair"",51.0]]",[],2173.44,0,5.36,2173.44
2025-01-26,Sun,95.00,0.00,351.00,19.00,0,[],[],[],[],[],[],2173.44,0,7.14,2173.44
2025-01-27,Mon,95.00,0.00,402.00,13.00,1,[],[],[],[],[],[],2173.44,0,8.93,2173.44
2025-01-28,Tue,95.00,0.00,453.00,7.00,1,[],[],[],[],[],[],2173.44,0,10.71,2173.44
2025-01-29,Wed,95.00,0.00,504.00,1.00,1,[],[],[],[],[],[],2173.44,0,12.50,2173.44
2025-01-30,Thu,95.00,0.00,580.00,20.00,0,[],[],[],[],[],[],2173.44,0,14.29,2173.44
2025-01-31,Fri,95.00,0.00,631.00,14.00,1,[],[],[],[],[],[],2173.44,0,16.07,2173.44
</file>

<file path="tests/fixtures/golden/stress_tight_fixed/metrics.json">
{
  "accrued_interest_estimate": 16.07,
  "cushion_breach_date": "2025-01-01",
  "first_negative_date": "2025-01-01",
  "friend_ask_latest_date": "2025-01-01",
  "friend_ask_needed": 704.0,
  "min_balance": -704.0,
  "min_balance_date": "2025-01-01",
  "total_upcoming_bills": 1106.0
}
</file>

<file path="tests/fixtures/golden/stress_tight_fixed/series.csv">
date,weekday,earn,bill_due,balance,gas_bucket,fillups,cc_events,cc_interest_posted,cc_min_paid,cc_extra_on_due,oneoff_contribs,oneoff_paid,total_cc_balance,total_iou_balance,accrued_interest_unposted,cc_high_apr_bal
2025-01-01,Wed,95.00,900.00,-704.00,19.00,0,[],[],[],[],[],[],2200.00,0,1.81,2200.00
2025-01-02,Thu,95.00,0.00,-653.00,13.00,1,[],[],[],[],[],[],2200.00,0,3.62,2200.00
2025-01-03,Fri,95.00,0.00,-602.00,7.00,1,[],[],[],[],[],[],2200.00,0,5.42,2200.00
2025-01-04,Sat,95.00,0.00,-551.00,1.00,1,[],[],[],[],[],[],2200.00,0,7.23,2200.00
2025-01-05,Sun,95.00,0.00,-475.00,20.00,0,[],[],[],[],[],[],2200.00,0,9.04,2200.00
2025-01-06,Mon,95.00,0.00,-424.00,14.00,1,[],[],[],[],[],[],2200.00,0,10.85,2200.00
2025-01-07,Tue,95.00,0.00,-373.00,8.00,1,[],[],[],[],[],[],2200.00,0,12.65,2200.00
2025-01-08,Wed,95.00,0.00,-322.00,2.00,1,[],[],[],[],[],[],2200.00,0,14.46,2200.00
2025-01-09,Thu,95.00,0.00,-246.00,21.00,0,[],[],[],[],[],[],2200.00,0,16.27,2200.00
2025-01-10,Fri,95.00,0.00,-195.00,15.00,1,[],[],[],[],[],[],2200.00,0,18.08,2200.00
2025-01-11,Sat,95.00,0.00,-144.00,9.00,1,[],[],[],[],[],[],2200.00,0,19.88,2200.00
2025-01-12,Sun,95.00,0.00,-93.00,3.00,1,[],[],[],[],[],[],2200.00,0,21.69,2200.00
2025-01-13,Mon,95.00,0.00,-17.00,22.00,0,[],[],[],[],[],[],2200.00,0,23.50,2200.00
2025-01-14,Tue,95.00,0.00,34.00,16.00,1,[],[],[],[],[],[],2200.00,0,25.31,2200.00
2025-01-15,Wed,95.00,0.00,85.00,10.00,1,[],[],[],[],[],[],2200.00,0,27.11,2200.00
2025-01-16,Thu,95.00,0.00,70.00,4.00,1,"[{""date"":""2025-01-16"",""account"":""High APR"",""finance_charge"":0.0,""minimum_paid"":66.0,""extra_paid"":0.0,""new_balance"":2134.0}]",[],"[[""High APR"",66.0]]",[],[],[],2134.00,0,28.92,2134.00
2025-01-17,Fri,95.00,0.00,146.00,23.00,0,[],[],[],[],[],[],2134.00,0,30.68,2134.00
2025-01-18,Sat,95.00,140.00,57.00,17.00,1,[],[],[],[],[],[],2134.00,0,32.43,2134.00
2025-01-19,Sun,95.00,0.00,108.00,11.00,1,[],[],[],[],[],[],2134.00,0,34.18,2134.00
2025-01-20,Mon,95.00,0.00,159.00,5.00,1,[],[],[],[],[],[],2134.00,0,35.94,2134.00
2025-01-21,Tue,95.00,0.00,235.00,24.00,0,[],[],[],[],[],[],2134.00,0,37.69,2134.00
2025-01-22,Wed,95.00,0.00,286.00,18.00,1,"[{""date"":""2025-01-22"",""account"":""High APR"",""finance_charge"":39.44,""minimum_paid"":0.0,""extra_paid"":0.0,""new_balance"":2173.44}]","[[""High APR"",39.44]]",[],[],[],[],2173.44,0,0.00,2173.44
2025-01-23,Thu,95.00,0.00,275.00,12.00,1,[],[],[],[],"[[""Car repair"",62.0]]",[],2173.44,0,1.79,2173.44
2025-01-24,Fri,95.00,0.00,275.00,6.00,1,[],[],[],[],"[[""Car repair"",51.0]]",[],2173.44,0,3.57,2173.44
2025-01-25,Sat,95.00,0.00,275.00,0.00,1,[],[],[],[],"[[""Car repair"",51.0]]",[],2173.44,0,5.36,2173.44
2025-01-26,Sun,95.00,0.00,351.00,19.00,0,[],[],[],[],[],[],2173.44,0,7.14,2173.44
2025-01-27,Mon,95.00,0.00,402.00,13.00,1,[],[],[],[],[],[],2173.44,0,8.93,2173.44
2025-01-28,Tue,95.00,0.00,453.00,7.00,1,[],[],[],[],[],[],2173.44,0,10.71,2173.44
2025-01-29,Wed,95.00,0.00,504.00,1.00,1,[],[],[],[],[],[],2173.44,0,12.50,2173.44
2025-01-30,Thu,95.00,0.00,580.00,20.00,0,[],[],[],[],[],[],2173.44,0,14.29,2173.44
2025-01-31,Fri,95.00,0.00,631.00,14.00,1,[],[],[],[],[],[],2173.44,0,16.07,2173.44
</file>

<file path="tests/fixtures/golden/_meta.json">
{
  "days": 31,
  "scenarios": [
    "debt_payoff",
    "sample",
    "simple_baseline",
    "stress_tight_fixed",
    "stress_tight"
  ],
  "source_dir": "examples/configs",
  "start": "2025-01-01"
}
</file>

<file path="tests/golden/test_golden_scenarios.py">
from __future__ import annotations

import io
import json
from datetime import date
from pathlib import Path

import pandas as pd

from cashsim.batch import run_from_config
from cashsim.io.exporters import metrics_to_dict, normalize_series_for_export


GOLDEN_DIR = Path(__file__).resolve().parents[1] / "fixtures" / "golden"
START = date.fromisoformat("2025-01-01")
DAYS = 31


def _stable_series_csv(df: pd.DataFrame) -> str:
    out = normalize_series_for_export(df)
    buf = io.StringIO()
    out.to_csv(buf, index=False, lineterminator="\n", float_format="%.2f")
    return buf.getvalue()


def test_golden_scenarios_match_snapshot() -> None:
    assert GOLDEN_DIR.exists(), f"Golden dir missing: {GOLDEN_DIR} (run scripts/regen_golden.py)"

    scenario_dirs = sorted([p for p in GOLDEN_DIR.iterdir() if p.is_dir()])
    assert scenario_dirs, "No golden scenarios found (run scripts/regen_golden.py)"

    for scen in scenario_dirs:
        cfg = scen / "config.json"
        exp_metrics_path = scen / "metrics.json"
        exp_series_path = scen / "series.csv"

        assert cfg.exists(), f"Missing {cfg}"
        assert exp_metrics_path.exists(), f"Missing {exp_metrics_path}"
        assert exp_series_path.exists(), f"Missing {exp_series_path}"

        run = run_from_config(config=cfg, start=START, days=DAYS)

        expected_metrics = json.loads(exp_metrics_path.read_text(encoding="utf-8"))
        actual_metrics = metrics_to_dict(run.metrics)
        assert actual_metrics == expected_metrics, (
            f"Metrics mismatch for {scen.name}. If change is intentional: python scripts/regen_golden.py --overwrite"
        )

        expected_series = exp_series_path.read_text(encoding="utf-8")
        actual_series = _stable_series_csv(run.df)
        assert actual_series == expected_series, (
            f"Series mismatch for {scen.name}. If change is intentional: python scripts/regen_golden.py --overwrite"
        )


def test_basic_invariants_hold_for_golden_scenarios() -> None:
    scenario_dirs = sorted([p for p in GOLDEN_DIR.iterdir() if p.is_dir()])
    for scen in scenario_dirs:
        cfg = scen / "config.json"
        run = run_from_config(config=cfg, start=START, days=DAYS)

        df = run.df
        assert len(df) == DAYS
        assert "date" in df.columns

        # No NaNs in numeric columns
        num = df.select_dtypes(include=["number"])
        assert not num.isna().any().any(), f"NaNs found in numeric columns for {scen.name}"

        # Date monotonicity
        dates = pd.to_datetime(df["date"]).dt.date
        assert dates.is_monotonic_increasing

        # Required core columns should exist
        for col in ["balance", "earn", "bill_due", "gas_bucket"]:
            assert col in df.columns, f"Missing {col} for {scen.name}"
</file>

<file path="tests/integration/test_api_matches_cli.py">
from __future__ import annotations

import json
from pathlib import Path

from fastapi.testclient import TestClient
from typer.testing import CliRunner

from cashsim.api.app import app as api_app
from cashsim.cli import app as cli_app

client = TestClient(api_app)


def _write_config(tmp: Path) -> Path:
    cfg = {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.10,
        "gas_fill_size": 25.0,
        "bills": [{"name": "rent", "amount": 200.0, "usual_day": 5}],
        "oneoffs": [],
        "credit_cards": [],
        "ious": [],
        "interest_mode": "statement_adb",
    }
    path = tmp / "cfg.json"
    path.write_text(json.dumps(cfg, indent=2) + "\n", encoding="utf-8")
    return path


def test_api_simulate_matches_cli_simulate(tmp_path: Path) -> None:
    cfg_path = _write_config(tmp_path)
    start = "2025-01-01"
    days = "10"

    # CLI
    runner = CliRunner()
    r = runner.invoke(cli_app, ["simulate", "--config", str(cfg_path), "--start", start, "--days", days])
    assert r.exit_code == 0, r.stdout
    cli_body = json.loads(r.stdout)

    # API
    dials = json.loads(cfg_path.read_text(encoding="utf-8"))
    api_body = client.post("/v1/simulate", json={"dials": dials, "start": start, "days": int(days)}).json()

    assert api_body["metrics"] == cli_body["metrics"]
    assert api_body["series"] == cli_body["series"]
</file>

<file path="tests/integration/test_cli_compare_report.py">
from __future__ import annotations

import json
from pathlib import Path

from typer.testing import CliRunner

from cashsim.cli import app


def test_cli_compare_can_write_markdown_report(tmp_path: Path) -> None:
    runner = CliRunner()

    a = Path("examples/configs/simple_baseline.json")
    b = Path("examples/configs/debt_payoff.json")

    out = tmp_path / "cmp"

    res = runner.invoke(
        app,
        [
            "compare",
            "--a",
            str(a),
            "--b",
            str(b),
            "--start",
            "2025-01-01",
            "--days",
            "31",
            "--out",
            str(out),
            "--report",
            "report.md",
        ],
    )

    assert res.exit_code == 0, res.stdout

    payload = json.loads((out / "compare.json").read_text(encoding="utf-8"))
    assert "timeline" in payload
    assert "category_totals" in payload
    assert "series_summary" in payload
    assert "meta" in payload
    assert (out / "report.md").exists()
</file>

<file path=".hypothesis/constants/05003b5d7106c082">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/models.py
# hypothesis_version: 6.140.3

[0.0, 0.045, 0.07, 7000.0, 'Payment due day', 'amount', 'avalanche', 'due_simple', 'e.g. 0.02 for 2%', 'snowball', 'statement_adb']
</file>

<file path=".hypothesis/constants/11a42b06188f0505">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/.venv/bin/pytest
# hypothesis_version: 6.148.7

['.exe', '__main__']
</file>

<file path=".hypothesis/constants/1a3c2f57c0751bf4">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/__init__.py
# hypothesis_version: 6.140.3

['Bill', 'CreditCard', 'Dials', 'IOU', 'InvestmentSettings', 'OneOff', 'SimMetrics', 'break_even_grid', 'monthly_snapshot', 'simulate_month']
</file>

<file path=".hypothesis/constants/1d3092d8c424df83">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/analytics/snapshot.py
# hypothesis_version: 6.148.7

[0.0, '%Y-%m', 'M', 'annualized_leftover', 'est_gas', 'est_income', 'events', 'first_date', 'gas', 'income', 'last_date', 'leftover', 'month', 'total_bills', 'workday_count']
</file>

<file path=".hypothesis/constants/2d2210d13ed3429c">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/sim/types.py
# hypothesis_version: 6.148.7

['extra', 'finance', 'min']
</file>

<file path=".hypothesis/constants/32200bad776ad762">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/__init__.py
# hypothesis_version: 6.148.7

['.analytics.snapshot', '.models', '.sim.core', '.sim.types', 'Bill', 'CreditCard', 'Dials', 'IOU', 'InvestmentSettings', 'OneOff', 'SimMetrics', 'break_even_grid', 'monthly_snapshot', 'simulate_month']
</file>

<file path=".hypothesis/constants/44dff5104edbd685">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/constants.py
# hypothesis_version: 6.140.3

[365]
</file>

<file path=".hypothesis/constants/46926e31f2514b95">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/analytics/snapshot.py
# hypothesis_version: 6.140.3

[0.0, '%Y-%m', 'M', 'annualized_leftover', 'est_gas', 'est_income', 'events', 'first_date', 'gas', 'income', 'last_date', 'leftover', 'month', 'total_bills', 'workday_count']
</file>

<file path=".hypothesis/constants/4d39d059002a363c">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/sim/gas.py
# hypothesis_version: 6.148.7

[0.0, 7.0]
</file>

<file path=".hypothesis/constants/5450694c90f33787">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/types.py
# hypothesis_version: 6.140.3

[]
</file>

<file path=".hypothesis/constants/55ec52dcd3a47e5f">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/core.py
# hypothesis_version: 6.140.3

[0.0, 1e-09, '%a', '[^a-z0-9_]+', '_', 'adb_days', 'adb_sum', 'avalanche', 'balance', 'bill_due', 'cc_events', 'cc_extra_on_due', 'cc_interest_posted', 'cc_min_paid', 'cycle_due', 'cycle_start', 'date', 'due_simple', 'earn', 'fillups', 'gas_bucket', 'grace_active', 'min_due_locked', 'next_stmt', 'oneoff_contribs', 'oneoff_paid', 'oneoffs', 'payments_since_stmt', 'statement_adb', 'statement_balance', 'total_cc_balance', 'total_iou_balance', 'weekday']
</file>

<file path=".hypothesis/constants/622e3ca106d878d8">
# file: /Users/wholesway/Dev/01-personal/CashSim/.venv/bin/pytest
# hypothesis_version: 6.140.3

['.exe', '__main__']
</file>

<file path=".hypothesis/constants/75e503854c8b7fc5">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/interest.py
# hypothesis_version: 6.140.3

[0.0, 365, 'adb_days', 'adb_sum', 'cycle_due', 'min_due_locked', 'next_stmt', 'payments_since_stmt', 'statement_balance']
</file>

<file path=".hypothesis/constants/8fc00151b57c2cff">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/sim/core.py
# hypothesis_version: 6.148.7

[0.0, 1e-09, '%a', '[^a-z0-9_]+', '_', 'adb_days', 'adb_sum', 'avalanche', 'balance', 'bill_due', 'cc_events', 'cc_extra_on_due', 'cc_interest_posted', 'cc_min_paid', 'cycle_due', 'cycle_start', 'date', 'due_simple', 'earn', 'fillups', 'gas_bucket', 'grace_active', 'min_due_locked', 'next_stmt', 'oneoff_contribs', 'oneoff_paid', 'oneoffs', 'payments_since_stmt', 'statement_adb', 'statement_balance', 'total_cc_balance', 'total_iou_balance', 'weekday']
</file>

<file path=".hypothesis/constants/aadb416cbccb1f4e">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/io/config_io.py
# hypothesis_version: 6.148.7

['utf-8']
</file>

<file path=".hypothesis/constants/abd2f506ec370539">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/core.py
# hypothesis_version: 6.140.3

[0.0, 1e-09, '%a', '[^a-z0-9_]+', '_', 'adb_days', 'adb_sum', 'avalanche', 'balance', 'bill_due', 'cc_events', 'cc_extra_on_due', 'cc_interest_posted', 'cc_min_paid', 'cycle_due', 'cycle_start', 'date', 'due_simple', 'earn', 'fillups', 'gas_bucket', 'grace_active', 'min_due_locked', 'next_stmt', 'oneoff_contribs', 'oneoff_paid', 'oneoffs', 'payments_since_stmt', 'statement_adb', 'statement_balance', 'total_cc_balance', 'total_iou_balance', 'weekday']
</file>

<file path=".hypothesis/constants/ac417f1031ce8681">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/utils/money.py
# hypothesis_version: 6.140.3

[100, '0.01', '365']
</file>

<file path=".hypothesis/constants/ade185e83c517a1e">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/roll.py
# hypothesis_version: 6.140.3

[0.0]
</file>

<file path=".hypothesis/constants/b0f23aacd139da46">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/types.py
# hypothesis_version: 6.140.3

['extra', 'finance', 'min']
</file>

<file path=".hypothesis/constants/b2e4d897dfc0ba57">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/io/exporters.py
# hypothesis_version: 6.148.7

['%.2f', ',', ':', '_bal', 'balance', 'bill_due', 'cc_', 'cc_events', 'cc_extra_on_due', 'cc_interest_posted', 'cc_min_paid', 'compare.json', 'config_path', 'csv', 'date', 'days', 'earn', 'events.csv', 'fillups', 'gas_bucket', 'include_events', 'json', 'kind', 'metrics.json', 'object', 'oneoff_contribs', 'oneoff_paid', 'raw', 'records', 'run_meta.json', 'series.csv', 'series.json', 'series_diff.csv', 'series_format', 'start', 'total_cc_balance', 'total_iou_balance', 'utf-8', 'weekday']
</file>

<file path=".hypothesis/constants/b712ebe763127196">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/events.py
# hypothesis_version: 6.140.3

[0.0, 'account', 'date', 'extra_paid', 'finance_charge', 'minimum_paid', 'new_balance', 'reserve_7d_bills', 'reserve_7d_gas', 'reserve_7d_mins', 'reserve_needed', 'surplus_used']
</file>

<file path=".hypothesis/constants/b9ebac38c647a1bc">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/gas.py
# hypothesis_version: 6.140.3

[0.0, 7.0]
</file>

<file path=".hypothesis/constants/ce88c44d7647a3bf">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/constants.py
# hypothesis_version: 6.148.7

[365]
</file>

<file path=".hypothesis/constants/d0798f0c54aaac4c">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/io/__init__.py
# hypothesis_version: 6.148.7

[]
</file>

<file path=".hypothesis/constants/da39a3ee5e6b4b0d">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/__init__.py
# hypothesis_version: 6.140.3

[]
</file>

<file path=".hypothesis/constants/dc15ef223874a3ab">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/compare.py
# hypothesis_version: 6.148.7

['_a', '_b', 'a', 'b', 'date', 'delta', 'outer']
</file>

<file path=".hypothesis/constants/dc690edcfb974253">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/batch.py
# hypothesis_version: 6.148.7

[]
</file>

<file path=".hypothesis/constants/df4a86fb1f327905">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/sim/interest.py
# hypothesis_version: 6.148.7

[0.0, 365, 'adb_days', 'adb_sum', 'cycle_due', 'min_due_locked', 'next_stmt', 'payments_since_stmt', 'statement_balance']
</file>

<file path=".hypothesis/constants/e1ac37e50638a5b7">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/cli.py
# hypothesis_version: 6.148.7

[366, '--a', '--b', '--config', '--days', '--events/--no-events', '--out', '--overwrite', '--series-format', '--series/--no-series', '--start', 'Config A (baseline).', 'Output directory.', '__file__', '__main__', 'allow_extra_args', 'balance', 'compare.json', 'csv', 'csv|json', 'metrics', 'out', 'records', 'run', 'series', 'series_diff.csv', 'streamlit', 'total_cc_balance', 'total_iou_balance', 'ui', 'utf-8']
</file>

<file path=".hypothesis/constants/e379c7b39a11f107">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/analytics/break_even.py
# hypothesis_version: 6.140.3

['Okay', 'Risky', 'Safe', 'weekday_earnings']
</file>

<file path=".hypothesis/constants/e387c69cbe66d125">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/analytics/snapshot.py
# hypothesis_version: 6.140.3

[0.0, '%Y-%m', 'M', 'annualized_leftover', 'est_gas', 'est_income', 'events', 'first_date', 'gas', 'income', 'last_date', 'leftover', 'month', 'total_bills', 'workday_count']
</file>

<file path=".hypothesis/constants/e64ba8c596bee195">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/utils/money.py
# hypothesis_version: 6.148.7

[100, '0.01', '365']
</file>

<file path=".hypothesis/constants/e7068b01ae137f00">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/utils/money.py
# hypothesis_version: 6.140.3

[100, '0.01', '365']
</file>

<file path=".hypothesis/constants/eb6d189fc4afb526">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/sim/reserve.py
# hypothesis_version: 6.140.3

[0.0, 1.0]
</file>

<file path=".hypothesis/constants/f1296bf796213b43">
# file: /Users/wholesway/Dev/01-personal/CashSim/src/cashsim/utils/date_utils.py
# hypothesis_version: 6.140.3

[4096]
</file>

<file path=".hypothesis/constants/f7c3bc892fba18c9">
# file: /Users/wholesway/Documents/Projects-and-Data/Dev/01-personal/CashSim/src/cashsim/models.py
# hypothesis_version: 6.148.7

[0.0, 0.045, 0.07, 7000.0, 'Payment due day', 'amount', 'avalanche', 'due_simple', 'e.g. 0.02 for 2%', 'snowball', 'statement_adb']
</file>

<file path="docs/cli.md">
# CLI (Batch Mode)

## Install
`pip install -e ".[dev]"`

## Export a run
`cashsim export --config examples/configs/simple_baseline.json --start 2025-01-01 --days 31 --out out/simple --series-format csv`

Artifacts:
- out/simple/metrics.json
- out/simple/series.csv
- out/simple/run_meta.json

## Compare two scenarios
`cashsim compare --a examples/configs/simple_baseline.json --b examples/configs/stress_tight.json --start 2025-01-01 --days 31 --out out/compare`

Artifacts:
- out/compare/compare.json
</file>

<file path="docs/SECRETS.md">
# Streamlit Secrets: Exact Format & Where to Put Them

This app reads Google Sheets via a **Google Cloud service account**.
Define your secrets locally at:

- **Project-specific**: `.streamlit/secrets.toml` (recommended for local dev)
- **Global (user)**: `~/.streamlit/secrets.toml` (macOS/Linux) or `%USERPROFILE%/.streamlit/secrets.toml` (Windows)

> Streamlit merges global + project secrets; project wins on key collisions. Do **not** commit secrets to git. Add `.streamlit/` to `.gitignore`.  
> Source: Streamlit secrets docs. :contentReference[oaicite:1]{index=1}

---

## Minimal working `secrets.toml`

```toml
# .streamlit/secrets.toml

# Optional: you can keep unrelated keys here too (e.g., OpenAI_key = "…")

[gcp_service_account]
type = "service_account"
project_id = "your-project-id"
private_key_id = "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
# Keep the literal \n line breaks in the key value:
private_key = "-----BEGIN PRIVATE KEY-----\nMIIEvAIBADANBgkqhki…\n-----END PRIVATE KEY-----\n"
client_email = "svc-account@your-project-id.iam.gserviceaccount.com"
client_id = "123456789012345678901"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/svc-account%40your-project-id.iam.gserviceaccount.com"
</file>

<file path="examples/configs/debt_payoff.json">
{
  "current_cash": 350.0,
  "safety_cushion": 200.0,
  "weekday_earnings": 140.0,
  "gas_pct": 0.18,
  "gas_fill_size": 25.0,
  "bills": [
    {"name": "Rent", "usual_day": 1, "amount": 850.0},
    {"name": "Utilities", "usual_day": 15, "amount": 110.0}
  ],
  "credit_cards": [
    {"name": "Card A", "apr": 0.2499, "balance": 1800.0, "limit": 2500.0, "min_pct": 0.03, "min_floor": 25.0, "statement_day": 20, "due_day": 14},
    {"name": "Card B", "apr": 0.1799, "balance": 900.0, "limit": 2000.0, "min_pct": 0.03, "min_floor": 25.0, "statement_day": 10, "due_day": 4}
  ],
  "ious": [],
  "oneoffs": [],
  "invest": {"enable": false, "hysa_apy": 0.045, "expected_market_return": 0.07, "use_dca": false, "roth_target": 0.0, "trad_target": 0.0, "hysa_target": 0.0, "robinhood_target": 0.0, "ira_year_limit": 7000.0, "roth_ok_today": true},
  "interest_mode": "statement_adb",
  "extra_strategy": "avalanche",
  "blackouts": []
}
</file>

<file path="examples/configs/sample.json">
{
  "current_cash": 31.0,
  "safety_cushion": 100.0,
  "weekday_earnings": 100.0,
  "gas_pct": 0.185,
  "gas_fill_size": 25.0,
  "bills": [
    {
      "name": "Apple Music",
      "usual_day": 28,
      "amount": 10.99
    },
    {
      "name": "Bank of America Maintenance",
      "usual_day": 5,
      "amount": 12.0
    },
    {
      "name": "Criterion",
      "usual_day": 11,
      "amount": 11.0
    },
    {
      "name": "DSA",
      "usual_day": 12,
      "amount": 11
    },
    {
      "name": "Statefarm - Kate's Half",
      "usual_day": 13,
      "amount": 145.0
    },
    {
      "name": "ChatGPT",
      "usual_day": 15,
      "amount": 20.0
    },
    {
      "name": "Reform & Revolution",
      "usual_day": 17,
      "amount": 10.0
    },
    {
      "name": "YouTube",
      "usual_day": 20,
      "amount": 9.06
    },
    {
      "name": "Claude AI",
      "usual_day": 25,
      "amount": 20.0
    },
    {
      "name": "iCloud",
      "usual_day": 29,
      "amount": 2.99
    },
    {
      "name": "Eversource Electric - Bobbie's Half",
      "usual_day": 24,
      "amount": 105.0
    },
    {
      "name": "National Grid Gas - Bobbie's Half",
      "usual_day": 25,
      "amount": 68.0
    },{
      "name": "OneDrive",
      "usual_day": 13,
      "amount": 1.99
    }
  ],
  "credit_cards": [
    {
      "name": "Chase VISA",
      "apr": 0.2499,
      "balance": 9141.96,
      "due_day": 24,
      "min_pct": 0.0335,
      "min_floor": 40.0,
      "statement_day": null
    },
    {
      "name": "Bank of America",
      "apr": 0.2124,
      "balance": 3070.63,
      "due_day": 5,
      "min_pct": 0.0274,
      "min_floor": 35.0,
      "statement_day": null
    },
    {
      "name": "Samsung",
      "apr": 0.0,
      "balance": 1200.0,
      "due_day": 3,
      "min_pct": 0.0,
      "min_floor": 40.12,
      "statement_day": null
    },
    {
      "name": "Aidvantage",
      "apr": 0.0,
      "balance": 3821.26,
      "due_day": 11,
      "min_pct": 0.0,
      "min_floor": 0.0,
      "statement_day": null
    },
    {
      "name": "DentalFirst",
      "apr": 0.3299,
      "balance": 2236.0,
      "due_day": 10,
      "min_pct": 0.0362,
      "min_floor": 30.0,
      "statement_day": null
    },
    {
      "name": "CreditOne",
      "apr": 0.2949,
      "balance": 976.13,
      "due_day": 13,
      "min_pct": 0.05,
      "min_floor": 30.0,
      "statement_day": null
    }
  ],
  "ious": [
    {
      "name": "Mama",
      "balance": 1050.0,
      "apr": 0.0,
      "due_day": null,
      "min_pct": 0.0,
      "min_floor": 0.0
    },
    {
      "name": "Papa",
      "balance": 300.0,
      "apr": 0.0,
      "due_day": null,
      "min_pct": 0.0,
      "min_floor": 0.0
    },
    {
      "name": "Bobbie (NJ hotel)",
      "balance": 300.0,
      "apr": 0.0,
      "due_day": null,
      "min_pct": 0.0,
      "min_floor": 0.0
    }
  ],
  "oneoffs": [
    {
      "name": "Auto Repair Service",
      "due_date": "2025-09-30",
      "amount": 521.0,
      "priority": 5,
      "must_pay": true
    },
    {
      "name": "MA Annual Inspection Sticker",
      "due_date": "2025-11-05",
      "amount": 35.0,
      "priority": 1,
      "must_pay": true
    },
    {
      "name": "Car Registration",
      "due_date": "2025-10-29",
      "amount": 135.0,
      "priority": 2,
      "must_pay": true
    },
    {
      "name": "MA License",
      "due_date": "2025-10-29",
      "amount": 115.0,
      "priority": 2,
      "must_pay": true
    },{
      "name": "Car Key Copy for Bobbie",
      "due_date": "2025-11-29",
      "amount": 130.0,
      "priority": 3,
      "must_pay": false
    }
  ],
  "invest": {
    "enable": true,
    "hysa_apy": 0.045,
    "expected_market_return": 0.07,
    "use_dca": true,
    "roth_target": 0.0,
    "trad_target": 0.0,
    "hysa_target": 0.0,
    "robinhood_target": 0.0,
    "ira_year_limit": 7000.0,
    "roth_ok_today": true
  },
  "interest_mode": "statement_adb",
  "extra_strategy": "avalanche"
}
</file>

<file path="examples/configs/simple_baseline.json">
{
  "current_cash": 500.0,
  "safety_cushion": 250.0,
  "weekday_earnings": 120.0,
  "gas_pct": 0.15,
  "gas_fill_size": 25.0,
  "bills": [
    {"name": "Phone", "usual_day": 5, "amount": 60.0},
    {"name": "Internet", "usual_day": 12, "amount": 55.0},
    {"name": "Rent", "usual_day": 1, "amount": 900.0}
  ],
  "credit_cards": [],
  "ious": [],
  "oneoffs": [],
  "invest": {"enable": false, "hysa_apy": 0.045, "expected_market_return": 0.07, "use_dca": false, "roth_target": 0.0, "trad_target": 0.0, "hysa_target": 0.0, "robinhood_target": 0.0, "ira_year_limit": 7000.0, "roth_ok_today": true},
  "interest_mode": "statement_adb",
  "extra_strategy": "avalanche",
  "blackouts": []
}
</file>

<file path="examples/configs/stress_tight.fixed.json">
{
  "current_cash": 120.0,
  "safety_cushion": 150.0,
  "weekday_earnings": 95.0,
  "gas_pct": 0.20,
  "gas_fill_size": 25.0,
  "bills": [
    {"name": "Rent", "usual_day": 1, "amount": 900.0},
    {"name": "Insurance", "usual_day": 18, "amount": 140.0}
  ],
  "credit_cards": [
    {"name": "High APR", "apr": 0.2999, "balance": 2200.0, "limit": 2500.0, "min_pct": 0.03, "min_floor": 25.0, "statement_day": 22, "due_day": 16}
  ],
  "ious": [],
  "oneoffs": [
    {"name": "Car repair", "amount": 400.0, "due_date": "2026-01-10", "start_date": "2025-12-20"}
  ],
  "invest": {"enable": false, "hysa_apy": 0.045, "expected_market_return": 0.07, "use_dca": false, "roth_target": 0.0, "trad_target": 0.0, "hysa_target": 0.0, "robinhood_target": 0.0, "ira_year_limit": 7000.0, "roth_ok_today": true},
  "interest_mode": "statement_adb",
  "extra_strategy": "avalanche",
  "blackouts": []
}
</file>

<file path="examples/configs/stress_tight.json">
{
  "current_cash": 120.0,
  "safety_cushion": 150.0,
  "weekday_earnings": 95.0,
  "gas_pct": 0.20,
  "gas_fill_size": 25.0,
  "bills": [
    {"name": "Rent", "usual_day": 1, "amount": 900.0},
    {"name": "Insurance", "usual_day": 18, "amount": 140.0}
  ],
  "credit_cards": [
    {"name": "High APR", "apr": 0.2999, "balance": 2200.0, "limit": 2500.0, "min_pct": 0.03, "min_floor": 25.0, "statement_day": 22, "due_day": 16}
  ],
  "ious": [],
  "oneoffs": [
    {"name": "Car repair", "amount": 400.0, "due_date": "2026-01-10", "start_date": "2025-12-20"}
  ],
  "invest": {"enable": false, "hysa_apy": 0.045, "expected_market_return": 0.07, "use_dca": false, "roth_target": 0.0, "trad_target": 0.0, "hysa_target": 0.0, "robinhood_target": 0.0, "ira_year_limit": 7000.0, "roth_ok_today": true},
  "interest_mode": "statement_adb",
  "extra_strategy": "avalanche",
  "blackouts": []
}
</file>

<file path="src/cashsim/analytics/__init__.py">

</file>

<file path="src/cashsim/io/__init__.py">
from __future__ import annotations
</file>

<file path="src/cashsim/io/config_io.py">
from __future__ import annotations

import json
from pathlib import Path

from pydantic import ValidationError

from cashsim.models import Dials


def load_config(path: str | Path) -> Dials:
    p = Path(path)
    data = json.loads(p.read_text(encoding="utf-8"))
    try:
        return Dials.model_validate(data)
    except ValidationError as e:
        raise ValueError(f"Invalid config: {e}") from e


def save_config(dials: Dials, path: str | Path) -> None:
    p = Path(path)
    p.write_text(dials.model_dump_json(indent=2), encoding="utf-8")
</file>

<file path="src/cashsim/io/exporters.py">
from __future__ import annotations

import json
from dataclasses import asdict
from datetime import date, datetime
from pathlib import Path

import pandas as pd

from cashsim.sim.types import SimMetrics

BASE_SERIES_COLUMNS: list[str] = [
    "date",
    "weekday",
    "earn",
    "bill_due",
    "balance",
    "gas_bucket",
    "fillups",
    "cc_events",
    "cc_interest_posted",
    "cc_min_paid",
    "cc_extra_on_due",
    "oneoff_contribs",
    "oneoff_paid",
    "total_cc_balance",
    "total_iou_balance",
    "accrued_interest_unposted",
]

EVENT_COLUMNS: list[str] = [
    "cc_events",
    "cc_interest_posted",
    "cc_min_paid",
    "cc_extra_on_due",
    "oneoff_contribs",
    "oneoff_paid",
]


def _json_default(obj: object) -> str:
    if isinstance(obj, (date, datetime)):
        return obj.isoformat()
    return str(obj)


def metrics_to_dict(metrics: SimMetrics) -> dict[str, object]:
    d = asdict(metrics)
    out: dict[str, object] = {}
    for k, v in d.items():
        if isinstance(v, date):
            out[k] = v.isoformat()
        else:
            out[k] = v
    return out


def _normalize_series_for_export(df: pd.DataFrame) -> pd.DataFrame:
    out = df.copy()

    # Serialize dates deterministically
    if "date" in out.columns:
        out["date"] = pd.to_datetime(out["date"]).dt.date.astype(str)

    # Convert list/dict cells to compact JSON strings for stable CSV output
    for col in out.columns:
        if out[col].dtype == "object" and col in EVENT_COLUMNS:
            out[col] = out[col].apply(
                lambda v: json.dumps(
                    v or [],
                    separators=(",", ":"),
                    default=_json_default,
                )
            )

    # Stable column order: base columns first, then per-card balance columns, then any remaining
    cc_cols = sorted([c for c in out.columns if c.startswith("cc_") and c.endswith("_bal")])
    base = [c for c in BASE_SERIES_COLUMNS if c in out.columns]
    remaining = sorted([c for c in out.columns if c not in set(base + cc_cols)])

    ordered = base + cc_cols + remaining
    return out[ordered]


def write_metrics_json(metrics: SimMetrics, path: Path) -> None:
    path.write_text(
        json.dumps(metrics_to_dict(metrics), indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )


def write_series_csv(df: pd.DataFrame, path: Path) -> None:
    out = _normalize_series_for_export(df)
    out.to_csv(path, index=False, lineterminator="\n", float_format="%.2f")


def write_series_json(df: pd.DataFrame, path: Path) -> None:
    out = _normalize_series_for_export(df)
    records = out.to_dict(orient="records")
    path.write_text(json.dumps(records, indent=2, default=_json_default) + "\n", encoding="utf-8")


def write_events_csv(df: pd.DataFrame, path: Path) -> None:
    # Flatten event columns into a single table for easier analysis.
    rows: list[dict[str, object]] = []

    if "date" not in df.columns:
        raise ValueError("series df missing 'date' column")

    for _, r in df.iterrows():
        day = r["date"]
        for col in EVENT_COLUMNS:
            if col not in df.columns:
                continue
            events = r[col] or []
            for ev in events:
                if isinstance(ev, dict):
                    row = {"date": day, "kind": col}
                    row.update(ev)
                    rows.append(row)
                else:
                    rows.append({"date": day, "kind": col, "raw": ev})

    out = pd.DataFrame.from_records(rows)
    if len(out) == 0:
        out = pd.DataFrame(columns=["date", "kind"])

    out.to_csv(path, index=False, lineterminator="\n")


def prepare_outdir(out_dir: Path, *, overwrite: bool) -> None:
    out_dir.mkdir(parents=True, exist_ok=True)

    # If overwrite is False, fail fast if we would clobber known artifacts.
    if not overwrite:
        for name in [
            "metrics.json",
            "series.csv",
            "series.json",
            "events.csv",
            "compare.json",
            "series_diff.csv",
            "run_meta.json",
        ]:
            p = out_dir / name
            if p.exists():
                raise FileExistsError(f"Refusing to overwrite existing file: {p} (use --overwrite)")


def write_run(
    *,
    out_dir: Path,
    df: pd.DataFrame,
    metrics: SimMetrics,
    config_path: Path,
    start: str,
    days: int,
    series_format: str,
    include_events: bool,
    overwrite: bool,
) -> None:
    prepare_outdir(out_dir, overwrite=overwrite)

    (out_dir / "run_meta.json").write_text(
        json.dumps(
            {
                "config_path": str(config_path),
                "start": start,
                "days": days,
                "series_format": series_format,
                "include_events": include_events,
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )

    write_metrics_json(metrics, out_dir / "metrics.json")

    fmt = series_format.lower()
    if fmt == "csv":
        write_series_csv(df, out_dir / "series.csv")
    elif fmt == "json":
        write_series_json(df, out_dir / "series.json")
    else:
        raise ValueError("series_format must be 'csv' or 'json'")

    if include_events:
        write_events_csv(df, out_dir / "events.csv")

def normalize_series_for_export(df: pd.DataFrame) -> pd.DataFrame:
    """Public wrapper for the project’s stable series normalization.

    API and CLI must share the same formatting rules so clients can rely on exported columns.
    """
    return _normalize_series_for_export(df)
</file>

<file path="src/cashsim/planning/__init__.py">

</file>

<file path="src/cashsim/sim/__init__.py">

</file>

<file path="src/cashsim/sim/roll.py">
from __future__ import annotations

from datetime import date

from cashsim.models import Bill
from cashsim.utils.date_utils import next_due_date_cached as next_due_date


def roll_bills(today: date, bills: list[Bill]) -> list[tuple[Bill, date]]:
    rolled = []
    for b in bills:
        d = next_due_date(today, b.usual_day)
        rolled.append((b, d))
    rolled.sort(key=lambda t: t[1])
    return rolled


def sum_non_debt_bills_due_today(day: date, bills: list[Bill]) -> float:
    total = 0.0
    for b in bills:
        if next_due_date(day, b.usual_day) == day:
            total += b.amount
    return round(total, 2)
</file>

<file path="src/cashsim/ui/__init__.py">

</file>

<file path="src/cashsim/utils/__init__.py">

</file>

<file path="src/cashsim/utils/date_utils.py">
from __future__ import annotations

import calendar
from datetime import date
from functools import lru_cache

from dateutil.relativedelta import relativedelta

from cashsim.constants import CARD_ACT_MIN_DAYS


def clamp_day(year: int, month: int, day: int) -> int:
    last = calendar.monthrange(year, month)[1]
    return min(day, last)


def next_due_date(today: date, usual_day: int) -> date:
    cand = date(today.year, today.month, clamp_day(today.year, today.month, usual_day))
    if cand >= today:
        return cand
    nxt = today + relativedelta(months=+1)
    return date(nxt.year, nxt.month, clamp_day(nxt.year, nxt.month, usual_day))


@lru_cache(maxsize=4096)
def next_due_date_cached(today: date, usual_day: int) -> date:
    """LRU-cached wrapper; safe because inputs are hashable and function is pure."""
    return next_due_date(today, usual_day)


def prior_statement_day(due_day: int) -> int:
    return max(1, due_day - CARD_ACT_MIN_DAYS)


def next_statement_close_date(today: date, due_day: int, statement_day: int | None) -> date:
    sd = statement_day or prior_statement_day(due_day)
    cand = date(today.year, today.month, clamp_day(today.year, today.month, sd))
    if cand >= today:
        return cand
    nxt = today + relativedelta(months=+1)
    return date(nxt.year, nxt.month, clamp_day(nxt.year, nxt.month, sd))


def last_statement_close_date(today: date, due_day: int, statement_day: int | None) -> date:
    ns = next_statement_close_date(today, due_day, statement_day)
    if ns > today:
        from_prev = ns + relativedelta(months=-1)
        sd = statement_day or prior_statement_day(due_day)
        return date(from_prev.year, from_prev.month, clamp_day(from_prev.year, from_prev.month, sd))
    return ns
</file>

<file path="src/cashsim/batch.py">
from __future__ import annotations

from dataclasses import dataclass
from datetime import date
from pathlib import Path

import pandas as pd

from cashsim.io.config_io import load_config
from cashsim.sim.core import simulate_month
from cashsim.sim.types import SimMetrics


@dataclass(frozen=True)
class RunResult:
    config_path: Path
    start: date
    days: int
    df: pd.DataFrame
    metrics: SimMetrics


def run_from_config(*, config: Path, start: date, days: int) -> RunResult:
    dials = load_config(config)
    df, metrics = simulate_month(dials, start=start, days=days)
    return RunResult(
        config_path=Path(config).resolve(),
        start=start,
        days=days,
        df=df,
        metrics=metrics,
    )
</file>

<file path="src/cashsim/compare.py">
from __future__ import annotations

from dataclasses import asdict
from datetime import date
from typing import Any

import pandas as pd

from cashsim.sim.types import SimMetrics


TIMELINE_FIELDS: list[str] = [
    "min_balance_date",
    "first_negative_date",
    "friend_ask_latest_date",
    "cushion_breach_date",
]

SERIES_DEFAULT_COLUMNS: list[str] = [
    # Keep existing Phase 1/2 columns first
    "balance",
    "total_cc_balance",
    "total_iou_balance",
    "accrued_interest_unposted",
    # Additional helpful levers
    "earn",
    "bill_due",
    "gas_bucket",
]


def _date_to_str(v: date | None) -> str | None:
    return v.isoformat() if isinstance(v, date) else None


def metrics_delta(a: SimMetrics, b: SimMetrics) -> dict[str, Any]:
    """Stable summary delta for top-level metrics.

    Numeric fields become numbers in `delta`.
    Non-numeric fields (dates) remain {a,b} records.

    This format is intentionally simple for:
    - JSON diff review
    - downstream automation that reads numeric deltas
    """

    da = asdict(a)
    db = asdict(b)

    out: dict[str, Any] = {
        "a": {k: (_date_to_str(v) if isinstance(v, date) else v) for k, v in da.items()},
        "b": {k: (_date_to_str(v) if isinstance(v, date) else v) for k, v in db.items()},
        "delta": {},
    }

    for k in da:
        va = da[k]
        vb = db[k]
        if isinstance(va, (int, float)) and isinstance(vb, (int, float)):
            out["delta"][k] = round(vb - va, 2)
        else:
            out["delta"][k] = {
                "a": _date_to_str(va) if isinstance(va, date) else va,
                "b": _date_to_str(vb) if isinstance(vb, date) else vb,
            }

    return out


def series_diff(df_a: pd.DataFrame, df_b: pd.DataFrame, *, columns: list[str]) -> pd.DataFrame:
    """Aligned day-by-day delta table for selected numeric columns.

    Output columns are suffixed with _a, _b, and _delta.
    The join is outer to make missing dates visible.
    """

    a = df_a.copy()
    b = df_b.copy()

    a["date"] = pd.to_datetime(a["date"]).dt.date
    b["date"] = pd.to_datetime(b["date"]).dt.date

    a = a[["date"] + [c for c in columns if c in a.columns]].set_index("date")
    b = b[["date"] + [c for c in columns if c in b.columns]].set_index("date")

    joined = a.join(b, how="outer", lsuffix="_a", rsuffix="_b").reset_index()

    for c in columns:
        ca = f"{c}_a"
        cb = f"{c}_b"
        if ca in joined.columns and cb in joined.columns:
            joined[f"{c}_delta"] = (joined[cb] - joined[ca]).round(2)

    joined["date"] = joined["date"].astype(str)
    return joined


def timeline_deltas(a: SimMetrics, b: SimMetrics, *, fields: list[str] = TIMELINE_FIELDS) -> dict[str, Any]:
    """Key date deltas with delta_days when both sides are present."""

    out: dict[str, Any] = {}
    for f in fields:
        da = getattr(a, f, None)
        db = getattr(b, f, None)

        delta_days: int | None = None
        if isinstance(da, date) and isinstance(db, date):
            delta_days = (db - da).days

        out[f] = {
            "a": _date_to_str(da) if isinstance(da, date) else None,
            "b": _date_to_str(db) if isinstance(db, date) else None,
            "delta_days": delta_days,
        }

    return out


def _sum_event_amounts(cell: Any) -> float:
    """Sum amounts from a per-day event cell.

    The engine uses tuple-like events (name, amount) today.
    This helper also tolerates dict events that include amt/amount.

    Returning 0.0 for empty/None helps stability in aggregates.
    """

    if not cell:
        return 0.0

    total = 0.0
    for ev in cell:
        if isinstance(ev, dict):
            if "amt" in ev:
                total += float(ev["amt"])
            elif "amount" in ev:
                total += float(ev["amount"])
        elif isinstance(ev, (list, tuple)) and len(ev) >= 2:
            try:
                total += float(ev[1])
            except (TypeError, ValueError):
                continue

    return round(total, 2)


def category_totals(df: pd.DataFrame) -> dict[str, float]:
    """Coarse aggregates for human-readable comparisons.

    These are intentionally “big buckets” so reviewers can quickly infer meaning.
    They are not meant to replace detailed series inspection.
    """

    out: dict[str, float] = {}

    out["income"] = round(float(df["earn"].sum()), 2) if "earn" in df.columns else 0.0
    out["non_debt_bills"] = (
        round(float(df["bill_due"].sum()), 2) if "bill_due" in df.columns else 0.0
    )

    if "cc_min_paid" in df.columns:
        out["cc_mins_paid"] = round(float(df["cc_min_paid"].apply(_sum_event_amounts).sum()), 2)
    else:
        out["cc_mins_paid"] = 0.0

    if "cc_extra_on_due" in df.columns:
        out["cc_extra_paid"] = round(
            float(df["cc_extra_on_due"].apply(_sum_event_amounts).sum()), 2
        )
    else:
        out["cc_extra_paid"] = 0.0

    if "oneoff_paid" in df.columns:
        out["oneoffs_paid"] = round(float(df["oneoff_paid"].apply(_sum_event_amounts).sum()), 2)
    else:
        out["oneoffs_paid"] = 0.0

    # For now: interest is tracked on-card, not as cashflow, but it’s still useful context.
    if "cc_interest_posted" in df.columns:
        out["interest_posted"] = round(
            float(df["cc_interest_posted"].apply(_sum_event_amounts).sum()), 2
        )
    else:
        out["interest_posted"] = 0.0

    out["debt_payments_total"] = round(out["cc_mins_paid"] + out["cc_extra_paid"], 2)
    out["spend_total"] = round(out["non_debt_bills"] + out["oneoffs_paid"], 2)

    return out


def category_deltas(df_a: pd.DataFrame, df_b: pd.DataFrame) -> dict[str, Any]:
    a = category_totals(df_a)
    b = category_totals(df_b)

    keys = sorted(set(a) | set(b))
    out: dict[str, Any] = {}
    for k in keys:
        va = float(a.get(k, 0.0))
        vb = float(b.get(k, 0.0))
        out[k] = {"a": round(va, 2), "b": round(vb, 2), "delta": round(vb - va, 2)}
    return out


def series_summary(
    df_a: pd.DataFrame,
    df_b: pd.DataFrame,
    *,
    columns: list[str],
    threshold: float = 0.01,
) -> dict[str, Any]:
    """Summarize divergence for selected series columns.

    This provides a “where should I look first?” hint for reviewers.
    - max_abs_delta + date: fastest spot check
    - RMSE: overall drift
    - threshold exceed: first date a meaningful diff appears
    """

    diff = series_diff(df_a, df_b, columns=columns)

    out: dict[str, Any] = {}
    for c in columns:
        dcol = f"{c}_delta"
        if dcol not in diff.columns:
            continue

        ser = pd.to_numeric(diff[dcol], errors="coerce")
        abs_ser = ser.abs()

        if abs_ser.notna().any():
            idx = int(abs_ser.idxmax())
            max_abs = float(abs_ser.max())
            max_date = str(diff.loc[idx, "date"])
            rmse = float((ser.dropna() ** 2).mean() ** 0.5) if ser.dropna().size else 0.0
            mean_abs = float(abs_ser.dropna().mean()) if abs_ser.dropna().size else 0.0
        else:
            max_abs = 0.0
            max_date = None
            rmse = 0.0
            mean_abs = 0.0

        exceed = abs_ser > threshold
        first_exceed = str(diff.loc[exceed, "date"].iloc[0]) if exceed.any() else None

        out[c] = {
            "threshold": threshold,
            "max_abs_delta": round(max_abs, 2),
            "max_abs_delta_date": max_date,
            "rmse": round(rmse, 2),
            "mean_abs_delta": round(mean_abs, 2),
            "days_exceeding_threshold": int(exceed.sum()),
            "first_exceeds_threshold_date": first_exceed,
        }

    return out


def build_compare_payload(
    *,
    metrics_a: SimMetrics,
    metrics_b: SimMetrics,
    df_a: pd.DataFrame,
    df_b: pd.DataFrame,
    start: str,
    days: int,
    series_columns: list[str] | None = None,
    threshold: float = 0.01,
) -> dict[str, Any]:
    cols = series_columns or SERIES_DEFAULT_COLUMNS

    return {
        "metrics": metrics_delta(metrics_a, metrics_b),
        "timeline": timeline_deltas(metrics_a, metrics_b),
        "category_totals": category_deltas(df_a, df_b),
        "series_summary": series_summary(df_a, df_b, columns=cols, threshold=threshold),
        "meta": {
            "start": start,
            "days": days,
            "series_columns": cols,
            "threshold": threshold,
        },
    }


def render_compare_markdown(payload: dict[str, Any], *, series_diff_path: str | None = None) -> str:
    """Deterministic, human-readable Markdown report."""

    lines: list[str] = []
    lines.append("# CashSim Compare Report")

    meta = payload.get("meta", {})
    lines.append("")
    lines.append(f"- start: {meta.get('start')}")
    lines.append(f"- days: {meta.get('days')}")
    lines.append(f"- threshold: {meta.get('threshold')}")
    if series_diff_path:
        lines.append(f"- series_diff: {series_diff_path}")

    # Metrics
    metrics = payload.get("metrics", {})
    a = metrics.get("a", {})
    b = metrics.get("b", {})
    d = metrics.get("delta", {})

    lines.append("")
    lines.append("## Metrics")
    lines.append("")
    lines.append("| metric | A | B | delta |")
    lines.append("|---|---:|---:|---:|")

    keys = sorted(a.keys())
    for k in keys:
        va = a.get(k)
        vb = b.get(k)
        dv = d.get(k)
        if isinstance(dv, dict):
            lines.append(f"| {k} | {va} | {vb} |  |")
        else:
            lines.append(f"| {k} | {va} | {vb} | {dv} |")

    # Timeline
    timeline = payload.get("timeline", {})
    lines.append("")
    lines.append("## Timeline deltas")
    lines.append("")
    lines.append("| field | A | B | delta_days |")
    lines.append("|---|---|---|---:|")
    for k in sorted(timeline.keys()):
        t = timeline[k]
        lines.append(f"| {k} | {t.get('a')} | {t.get('b')} | {t.get('delta_days')} |")

    # Category totals
    cats = payload.get("category_totals", {})
    lines.append("")
    lines.append("## Category totals")
    lines.append("")
    lines.append("| category | A | B | delta |")
    lines.append("|---|---:|---:|---:|")
    for k in sorted(cats.keys()):
        c = cats[k]
        lines.append(f"| {k} | {c.get('a')} | {c.get('b')} | {c.get('delta')} |")

    # Series summary
    ss = payload.get("series_summary", {})
    lines.append("")
    lines.append("## Series divergence summary")
    lines.append("")
    lines.append("| column | max_abs_delta | date | rmse | days_exceeding_threshold | first_exceeds_threshold_date |")
    lines.append("|---|---:|---|---:|---:|---|")
    for k in sorted(ss.keys()):
        s = ss[k]
        lines.append(
            "| {col} | {max_abs_delta} | {date} | {rmse} | {days_exceeding} | {first_exceeds} |".format(
                col=k,
                max_abs_delta=s.get("max_abs_delta"),
                date=s.get("max_abs_delta_date"),
                rmse=s.get("rmse"),
                days_exceeding=s.get("days_exceeding_threshold"),
                first_exceeds=s.get("first_exceeds_threshold_date"),
            )
        )

    lines.append("")
    return "\n".join(lines)
</file>

<file path="tests/integration/test_cli_compare.py">
from __future__ import annotations

import json
from pathlib import Path

from typer.testing import CliRunner

from cashsim.cli import app


def test_cli_compare_writes_compare_json(tmp_path: Path) -> None:
    runner = CliRunner()

    a = tmp_path / "a.json"
    b = tmp_path / "b.json"

    base = {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.15,
        "gas_fill_size": 25.0,
        "bills": [{"name": "Rent", "usual_day": 1, "amount": 500.0}],
        "credit_cards": [],
        "ious": [],
        "oneoffs": [],
        "invest": {
            "enable": False,
            "hysa_apy": 0.045,
            "expected_market_return": 0.07,
            "use_dca": False,
            "roth_target": 0.0,
            "trad_target": 0.0,
            "hysa_target": 0.0,
            "robinhood_target": 0.0,
            "ira_year_limit": 7000.0,
            "roth_ok_today": True,
        },
        "interest_mode": "statement_adb",
        "extra_strategy": "avalanche",
        "blackouts": [],
    }

    base2 = dict(base)
    base2["weekday_earnings"] = 140.0

    a.write_text(json.dumps(base, indent=2) + "\n", encoding="utf-8")
    b.write_text(json.dumps(base2, indent=2) + "\n", encoding="utf-8")

    out = tmp_path / "cmp"

    args = [
        "compare",
        "--a",
        str(a),
        "--b",
        str(b),
        "--start",
        "2025-01-01",
        "--days",
        "10",
        "--out",
        str(out),
    ]

    res = runner.invoke(app, args)

    assert res.exit_code == 0, res.stdout
    assert (out / "compare.json").exists()
</file>

<file path="tests/integration/test_cli_determinism.py">
from __future__ import annotations

import filecmp
import json
from pathlib import Path

from typer.testing import CliRunner

from cashsim.cli import app


def test_cli_export_is_deterministic(tmp_path: Path) -> None:
    runner = CliRunner()

    cfg = tmp_path / "cfg.json"
    cfg.write_text(
        json.dumps(
            {
                "current_cash": 300.0,
                "safety_cushion": 150.0,
                "weekday_earnings": 120.0,
                "gas_pct": 0.15,
                "gas_fill_size": 25.0,
                "bills": [{"name": "Rent", "usual_day": 1, "amount": 500.0}],
                "credit_cards": [],
                "ious": [],
                "oneoffs": [],
                "invest": {
                    "enable": False,
                    "hysa_apy": 0.045,
                    "expected_market_return": 0.07,
                    "use_dca": False,
                    "roth_target": 0.0,
                    "trad_target": 0.0,
                    "hysa_target": 0.0,
                    "robinhood_target": 0.0,
                    "ira_year_limit": 7000.0,
                    "roth_ok_today": True,
                },
                "interest_mode": "statement_adb",
                "extra_strategy": "avalanche",
                "blackouts": [],
            },
            indent=2,
        )
        + "\n",
        encoding="utf-8",
    )

    out1 = tmp_path / "out1"
    out2 = tmp_path / "out2"

    args = [
        "export",
        "--config",
        str(cfg),
        "--start",
        "2025-01-01",
        "--days",
        "10",
        "--series-format",
        "csv",
    ]

    r1 = runner.invoke(app, args + ["--out", str(out1)])
    assert r1.exit_code == 0, r1.stdout

    r2 = runner.invoke(app, args + ["--out", str(out2)])
    assert r2.exit_code == 0, r2.stdout

    assert filecmp.cmp(out1 / "metrics.json", out2 / "metrics.json", shallow=False)
    assert filecmp.cmp(out1 / "series.csv", out2 / "series.csv", shallow=False)
</file>

<file path="tests/integration/test_cli_export.py">
from __future__ import annotations

import json
from pathlib import Path

from typer.testing import CliRunner

from cashsim.cli import app


def _write_config(tmp: Path) -> Path:
    cfg = {
        "current_cash": 300.0,
        "safety_cushion": 150.0,
        "weekday_earnings": 120.0,
        "gas_pct": 0.15,
        "gas_fill_size": 25.0,
        "bills": [{"name": "Rent", "usual_day": 1, "amount": 500.0}],
        "credit_cards": [],
        "ious": [],
        "oneoffs": [],
        "invest": {
            "enable": False,
            "hysa_apy": 0.045,
            "expected_market_return": 0.07,
            "use_dca": False,
            "roth_target": 0.0,
            "trad_target": 0.0,
            "hysa_target": 0.0,
            "robinhood_target": 0.0,
            "ira_year_limit": 7000.0,
            "roth_ok_today": True,
        },
        "interest_mode": "statement_adb",
        "extra_strategy": "avalanche",
        "blackouts": [],
    }
    p = tmp / "cfg.json"
    p.write_text(json.dumps(cfg, indent=2) + "\n", encoding="utf-8")
    return p


def test_cli_export_writes_artifacts(tmp_path: Path) -> None:
    runner = CliRunner()
    cfg = _write_config(tmp_path)
    out = tmp_path / "out"

    result = runner.invoke(
        app,
        [
            "export",
            "--config",
            str(cfg),
            "--start",
            "2025-01-01",
            "--days",
            "10",
            "--out",
            str(out),
            "--series-format",
            "csv",
        ],
    )

    assert result.exit_code == 0, result.stdout
    assert (out / "metrics.json").exists()
    assert (out / "series.csv").exists()
    assert (out / "run_meta.json").exists()


def test_cli_export_refuses_existing_output_without_overwrite(tmp_path: Path) -> None:
    runner = CliRunner()
    cfg = _write_config(tmp_path)
    out = tmp_path / "out"

    args = [
        "export",
        "--config",
        str(cfg),
        "--start",
        "2025-01-01",
        "--days",
        "10",
        "--out",
        str(out),
        "--series-format",
        "csv",
    ]

    result1 = runner.invoke(app, args)
    assert result1.exit_code == 0, result1.stdout

    result2 = runner.invoke(app, args)
    assert result2.exit_code != 0
    assert "Refusing to overwrite existing file:" in result2.output
    assert "Traceback" not in result2.output
</file>

<file path=".monkey-mods.txt">
cashsim.utils.money
</file>

<file path="example.json">

</file>

<file path="patch.diff">
*** Begin Patch
*** Update File: src/cashsim/utils/money.py
@@
-from __future__ import annotations
-
-from decimal import Decimal, ROUND_HALF_UP
-
-CENT = Decimal("0.01")
-
-
-def to_cents(x: float | str) -> int:
-    """Convert a float/str dollar amount to integer cents (half-up)."""
-    return int((Decimal(str(x)).quantize(CENT, rounding=ROUND_HALF_UP) * 100))
-
-
-def from_cents(c: int) -> float:
-    """Convert integer cents back to a float dollars representation."""
-    return float(Decimal(c) / 100)
-
-
-def round_cents(x: float | str | Decimal) -> float:
-    """Round a dollar amount to 2 decimals (half-up) and return float."""
-    return float(Decimal(str(x)).quantize(CENT, rounding=ROUND_HALF_UP))
+from __future__ import annotations
+
+from decimal import Decimal, ROUND_HALF_UP, ROUND_HALF_EVEN
+
+# Always define a single "cent" quantum for money rounding.
+CENT = Decimal("0.01")
+
+def D(x: float | int | str | Decimal) -> Decimal:
+    """Safe Decimal constructor (avoid binary float artifacts)."""
+    return x if isinstance(x, Decimal) else Decimal(str(x))
+
+def quantize_cents(x: Decimal, *, rounding=ROUND_HALF_UP) -> Decimal:
+    """Quantize a Decimal to cents with an explicit rounding mode."""
+    return D(x).quantize(CENT, rounding=rounding)
+
+def to_cents(x: float | str | Decimal) -> int:
+    """
+    Convert a dollar amount to integer cents using HALF_UP.
+    Use only at I/O boundaries (storage, display, external APIs).
+    """
+    return int(quantize_cents(D(x), rounding=ROUND_HALF_UP) * 100)
+
+def from_cents(c: int) -> float:
+    """Convert integer cents back to float dollars representation (display only)."""
+    return float(Decimal(int(c)) / 100)
+
+def round_cents(x: float | str | Decimal) -> float:
+    """
+    Round a dollar amount to two decimals (HALF_UP) and return float.
+    Prefer doing all intermediate arithmetic with Decimal, then call this when
+    you must hand a float to existing models/fields.
+    """
+    return float(quantize_cents(D(x), rounding=ROUND_HALF_UP))
*** End Patch
</file>

<file path="repomix.config.json">
{
  "$schema": "https://repomix.com/schemas/latest/schema.json",
  "input": {
    "maxFileSize": 52428800
  },
  "output": {
    "filePath": "repomix-output.xml",
    "style": "xml",
    "parsableStyle": false,
    "fileSummary": true,
    "directoryStructure": true,
    "files": true,
    "removeComments": false,
    "removeEmptyLines": false,
    "compress": false,
    "topFilesLength": 5,
    "showLineNumbers": false,
    "truncateBase64": false,
    "copyToClipboard": false,
    "tokenCountTree": false,
    "git": {
      "sortByChanges": true,
      "sortByChangesMaxCommits": 100,
      "includeDiffs": false,
      "includeLogs": false,
      "includeLogsCount": 50
    }
  },
  "include": [],
  "ignore": {
    "useGitignore": true,
    "useDefaultPatterns": true,
    "customPatterns": []
  },
  "security": {
    "enableSecurityCheck": true
  },
  "tokenCount": {
    "encoding": "o200k_base"
  }
}
</file>

<file path="run.json">
{
  "metrics": {
    "min_balance": -298.0,
    "min_balance_date": "2025-01-01",
    "first_negative_date": "2025-01-01",
    "total_upcoming_bills": 1015.0,
    "friend_ask_needed": 298.0,
    "friend_ask_latest_date": "2025-01-01",
    "cushion_breach_date": "2025-01-01",
    "accrued_interest_estimate": 0
  },
  "series": [
    {
      "date": "2025-01-01",
      "weekday": "Wed",
      "earn": 120.0,
      "bill_due": 900.0,
      "balance": -298.0,
      "gas_bucket": 18.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-02",
      "weekday": "Thu",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": -221.0,
      "gas_bucket": 11.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-03",
      "weekday": "Fri",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": -144.0,
      "gas_bucket": 4.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-04",
      "weekday": "Sat",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": -42.0,
      "gas_bucket": 22.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-05",
      "weekday": "Sun",
      "earn": 120.0,
      "bill_due": 60.0,
      "balance": -25.0,
      "gas_bucket": 15.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-06",
      "weekday": "Mon",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 52.0,
      "gas_bucket": 8.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-07",
      "weekday": "Tue",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 129.0,
      "gas_bucket": 1.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-08",
      "weekday": "Wed",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 231.0,
      "gas_bucket": 19.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-09",
      "weekday": "Thu",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 308.0,
      "gas_bucket": 12.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-10",
      "weekday": "Fri",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 385.0,
      "gas_bucket": 5.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-11",
      "weekday": "Sat",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 487.0,
      "gas_bucket": 23.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-12",
      "weekday": "Sun",
      "earn": 120.0,
      "bill_due": 55.0,
      "balance": 509.0,
      "gas_bucket": 16.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-13",
      "weekday": "Mon",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 586.0,
      "gas_bucket": 9.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-14",
      "weekday": "Tue",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 663.0,
      "gas_bucket": 2.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-15",
      "weekday": "Wed",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 765.0,
      "gas_bucket": 20.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-16",
      "weekday": "Thu",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 842.0,
      "gas_bucket": 13.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-17",
      "weekday": "Fri",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 919.0,
      "gas_bucket": 6.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-18",
      "weekday": "Sat",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1021.0,
      "gas_bucket": 24.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-19",
      "weekday": "Sun",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1098.0,
      "gas_bucket": 17.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-20",
      "weekday": "Mon",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1175.0,
      "gas_bucket": 10.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-21",
      "weekday": "Tue",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1252.0,
      "gas_bucket": 3.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-22",
      "weekday": "Wed",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1354.0,
      "gas_bucket": 21.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-23",
      "weekday": "Thu",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1431.0,
      "gas_bucket": 14.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-24",
      "weekday": "Fri",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1508.0,
      "gas_bucket": 7.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-25",
      "weekday": "Sat",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1585.0,
      "gas_bucket": 0.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-26",
      "weekday": "Sun",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1687.0,
      "gas_bucket": 18.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-27",
      "weekday": "Mon",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1764.0,
      "gas_bucket": 11.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-28",
      "weekday": "Tue",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1841.0,
      "gas_bucket": 4.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-29",
      "weekday": "Wed",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 1943.0,
      "gas_bucket": 22.0,
      "fillups": 0,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-30",
      "weekday": "Thu",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 2020.0,
      "gas_bucket": 15.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    },
    {
      "date": "2025-01-31",
      "weekday": "Fri",
      "earn": 120.0,
      "bill_due": 0.0,
      "balance": 2097.0,
      "gas_bucket": 8.0,
      "fillups": 1,
      "cc_events": [],
      "cc_interest_posted": [],
      "cc_min_paid": [],
      "cc_extra_on_due": [],
      "oneoff_contribs": [],
      "oneoff_paid": [],
      "total_cc_balance": 0,
      "total_iou_balance": 0,
      "accrued_interest_unposted": 0
    }
  ]
}
</file>

<file path=".devcontainer/devcontainer.json">
{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "src/cashsim/ui/streamlit_app.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "[ -f packages.txt ] && sudo apt update && sudo apt upgrade -y && sudo xargs apt install -y <packages.txt; [ -f requirements.txt ] && pip3 install --user -r requirements.txt; pip3 install --user streamlit; echo '✅ Packages installed and Requirements met'",
  "postAttachCommand": {
    "server": "streamlit run src/cashsim/ui/streamlit_app.py --server.enableCORS false --server.enableXsrfProtection false"
  },
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
</file>

<file path="src/cashsim/analytics/break_even.py">
from __future__ import annotations

from collections.abc import Iterable
from dataclasses import dataclass
from datetime import date

import pandas as pd

from cashsim.models import Dials
from cashsim.sim.core import simulate_month


@dataclass
class BreakEvenRow:
    weekday_earnings: float
    min_balance: float
    label: str
    days_to_cashout: int | None


def _label_from(min_balance: float, cushion: float) -> str:
    if min_balance >= cushion:
        return "Safe"
    if min_balance >= 0:
        return "Okay"
    return "Risky"


def break_even_grid(dials: Dials, candidates: Iterable[float], *, days: int = 31) -> pd.DataFrame:
    rows: list[BreakEvenRow] = []
    for e in candidates:
        test = dials.model_copy(update={"weekday_earnings": float(e)})
        _df, m = simulate_month(test, days=days)
        days_to_cashout = (
            None if m.first_negative_date is None else (m.first_negative_date - date.today()).days
        )
        rows.append(
            BreakEvenRow(
                weekday_earnings=round(float(e), 2),
                min_balance=m.min_balance,
                label=_label_from(m.min_balance, test.safety_cushion),
                days_to_cashout=days_to_cashout,
            )
        )
    return pd.DataFrame([r.__dict__ for r in rows]).sort_values("weekday_earnings")
</file>

<file path="src/cashsim/sim/events.py">
from __future__ import annotations

from datetime import date


def add_finance_event(
    events: list[dict], day: date, account: str, amt: float, new_balance: float
) -> None:
    events.append(
        {
            "date": day,
            "account": account,
            "finance_charge": amt,
            "minimum_paid": 0.0,
            "extra_paid": 0.0,
            "new_balance": new_balance,
        }
    )


def add_min_event(
    events: list[dict], day: date, account: str, amt: float, new_balance: float
) -> None:
    events.append(
        {
            "date": day,
            "account": account,
            "finance_charge": 0.0,
            "minimum_paid": amt,
            "extra_paid": 0.0,
            "new_balance": new_balance,
        }
    )


def add_extra_event(
    events: list[dict],
    day: date,
    account: str,
    extra: float,
    new_balance: float,
    *,
    surplus_used: float,
    reserve_needed: float,
    reserve_7d_bills: float,
    reserve_7d_mins: float,
    reserve_7d_gas: float,
) -> None:
    events.append(
        {
            "date": day,
            "account": account,
            "finance_charge": 0.0,
            "minimum_paid": 0.0,
            "extra_paid": extra,
            "new_balance": new_balance,
            "surplus_used": surplus_used,
            "reserve_needed": reserve_needed,
            "reserve_7d_bills": reserve_7d_bills,
            "reserve_7d_mins": reserve_7d_mins,
            "reserve_7d_gas": reserve_7d_gas,
        }
    )
</file>

<file path="src/cashsim/sim/gas.py">
from __future__ import annotations


def apply_gas_skim_and_fillups(
    balance: float,
    gas_bucket: float,
    daily_earn: float,
    gas_pct: float,
    fill_size: float,
) -> tuple[float, float, int]:
    """
    Skim daily gas, accumulate bucket, execute fill-ups (withdraw from balance).
    O(1) arithmetic version (no loop). Returns (new_balance, new_gas_bucket, fillups_count).
    """
    gas_skim = daily_earn * gas_pct
    new_bucket = gas_bucket + gas_skim

    fills_before = int(gas_bucket // fill_size)
    fills_after = int(new_bucket // fill_size)
    fills = max(0, fills_after - fills_before)

    balance -= fills * fill_size
    gas_bucket = new_bucket - fills * fill_size
    balance += daily_earn - gas_skim
    return round(balance, 2), round(gas_bucket, 2), fills


def predict_fill_cost_7d(
    gas_bucket: float, daily_earn: float, gas_pct: float, fill_size: float
) -> float:
    """
    Conservative predictor for gas spend within 7 days:
    count how many fillups will be triggered by 7 days worth of skims.
    """
    if fill_size <= 0:
        return 0.0
    added = 7.0 * daily_earn * gas_pct
    start_fills = int(gas_bucket // fill_size)
    end_fills = int((gas_bucket + added) // fill_size)
    new_fills = max(0, end_fills - start_fills)
    return round(new_fills * fill_size, 2)
</file>

<file path="src/cashsim/sim/reserve.py">
from __future__ import annotations

from datetime import date, timedelta

from cashsim.models import IOU, Bill, CreditCard, OneOff
from cashsim.utils.date_utils import next_due_date_cached as next_due_date

from .gas import predict_fill_cost_7d


def reserve_next_7_days(
    start_day: date,
    *,
    bills: list[Bill],
    cards: list[CreditCard],
    ious: list[IOU],
    oneoffs: list[OneOff],
    include_today: bool,
    locked_mins: dict[str, float] | None = None,
    locked_due_dates: dict[str, date] | None = None,
    oneoff_saved: dict[str, float] | None = None,
    gas_bucket: float = 0.0,
    daily_earn: float = 0.0,
    gas_pct: float = 0.0,
    fill_size: float = 1.0,
) -> tuple[float, float, float, float]:
    """
    7-day reserve = non-debt bills + CC/IOU minimums + one-off shortfalls + predicted gas fills.
    Returns (total, nondebt, debt_mins, gas_pred).
    """
    horizon_end = start_day + timedelta(days=6)
    left_bound = start_day if include_today else (start_day + timedelta(days=1))

    amt_bills = 0.0
    amt_mins = 0.0

    for b in bills:
        due = next_due_date(start_day, b.usual_day)
        if left_bound <= due <= horizon_end:
            amt_bills += b.amount

    if oneoffs:
        for o in oneoffs:
            due = o.due_date
            if left_bound <= due <= horizon_end:
                saved = (oneoff_saved or {}).get(o.name, 0.0)
                shortfall = max(0.0, round(o.amount - saved, 2))
                amt_bills += shortfall

    for c in cards:
        due = (locked_due_dates or {}).get(c.name) or next_due_date(start_day, c.due_day)
        if left_bound <= due <= horizon_end:
            if locked_mins and c.name in locked_mins:
                due_min = locked_mins[c.name]
            else:
                due_min = max(c.min_floor, round(c.balance * c.min_pct, 2))
            amt_mins += due_min

    for x in ious:
        if x.due_day is None:
            continue
        due = next_due_date(start_day, x.due_day)
        if left_bound <= due <= horizon_end:
            due_min = max(x.min_floor, round(x.balance * x.min_pct, 2))
            amt_mins += due_min

    gas_pred = predict_fill_cost_7d(gas_bucket, daily_earn, gas_pct, fill_size)
    total = round(amt_bills + amt_mins + gas_pred, 2)
    return total, round(amt_bills, 2), round(amt_mins, 2), round(gas_pred, 2)
</file>

<file path="src/cashsim/sim/types_runtime.py">
from __future__ import annotations

from dataclasses import dataclass


@dataclass(slots=True)
class CardRT:
    """Runtime-lean card structure for hot loops (validated at the edges)."""

    name: str
    apr: float
    balance_cents: int
    due_day: int
    min_pct: float
    min_floor_cents: int
    statement_day: int | None
</file>

<file path="src/cashsim/__main__.py">
from .cli import main

if __name__ == "__main__":
    main()
</file>

<file path="src/cashsim/constants.py">
from __future__ import annotations

DAYS_PER_YEAR = 365
CARD_ACT_MIN_DAYS = 21
</file>

<file path="tests/test_property_adb.py">
from decimal import Decimal

from hypothesis import given
from hypothesis import strategies as st

from cashsim.utils.money import D, quantize_cents


def _adb_interest(daily_balances: list[Decimal], apr: Decimal) -> Decimal:
    """interest = average_daily_balance × daily_rate × days_in_cycle"""
    days = Decimal(len(daily_balances))
    daily_rate = D(apr) / D(365)
    adb = sum((D(b) for b in daily_balances), D("0")) / days
    return quantize_cents(adb * daily_rate * days)


@given(
    daily=st.lists(st.decimals(min_value=0, max_value=10000, places=2), min_size=1, max_size=31),
    apr=st.decimals(min_value=0, max_value=Decimal("0.5"), places=4),
)
def test_adb_identity_property(daily: list[Decimal], apr: Decimal) -> None:
    daily = [Decimal(str(x)) for x in daily]
    daily_rate = D(apr) / D(365)
    lhs = quantize_cents(sum((D(b) * daily_rate for b in daily), D("0")))
    rhs = _adb_interest(daily, apr)
    assert lhs == rhs
</file>

<file path=".repomixignore">
# Add patterns to ignore here, one per line
# Example:
# *.log
# tmp/
.streamlit/secrets.toml
monkeytype.sqlite3
</file>

<file path="requirements.txt">
-e .

# requirements.txt (third-party deps)
pydantic>=2.10,<3
pandas>=2.2.3,<3.0
python-dateutil>=2.9.0.post0
streamlit>=1.49,<2.0
pyarrow>=21,<22
gspread>=6,<7
google-auth>=2.0
gspread-dataframe>=2.0.0
</file>

<file path="src/cashsim/ui/components.py">
from __future__ import annotations

import pandas as pd
import streamlit as st


def number_dials() -> None:
    col1, col2 = st.columns(2)
    col1.number_input("current cash", min_value=0.0, step=50.0, key="current_cash")
    col2.number_input("safety cushion", min_value=0.0, step=25.0, key="safety_cushion")
    col3, col4 = st.columns(2)
    col3.number_input("daily earnings", min_value=0.0, step=10.0, key="weekday_earnings")
    col4.number_input(
        "gas % of earnings (0-1)", min_value=0.0, max_value=1.0, step=0.05, key="gas_pct"
    )
    st.number_input("gas fill-up size", min_value=1.0, step=1.0, key="gas_fill_size")


def strategy_toggles() -> None:
    st.selectbox(
        "Interest calculation",
        ["statement_adb", "due_simple"],
        key="interest_mode",
        help="ADB posts at statement close; 'due_simple' posts on due date.",
    )
    st.selectbox(
        "Extra payment strategy",
        ["avalanche", "snowball"],
        key="extra_strategy",
        help="Avalanche = highest APR first; Snowball = smallest balance first.",
    )


def bills_editor(df: pd.DataFrame, key: str | int | None = "bill_editor") -> pd.DataFrame:
    return st.data_editor(
        df,
        num_rows="dynamic",
        width="stretch",
        column_config={
            "name": st.column_config.TextColumn("name"),
            "usual_day": st.column_config.NumberColumn(
                "usual day (1-31)", min_value=1, max_value=31
            ),
            "amount": st.column_config.NumberColumn("amount", min_value=0.0, step=1.0),
        },
        key=key,
    )


def cc_editor(df: pd.DataFrame, key: str | int | None = "cc_editor") -> pd.DataFrame:
    return st.data_editor(
        df,
        num_rows="dynamic",
        width="stretch",
        column_config={
            "name": st.column_config.TextColumn("name"),
            "apr": st.column_config.NumberColumn(
                "APR (decimal)", min_value=0.0, max_value=1.0, step=0.0001
            ),
            "balance": st.column_config.NumberColumn("balance", min_value=0.0, step=1.0),
            "due_day": st.column_config.NumberColumn("due day (1-31)", min_value=1, max_value=31),
            "min_pct": st.column_config.NumberColumn(
                "min % (0-1)", min_value=0.0, max_value=1.0, step=0.001
            ),
            "min_floor": st.column_config.NumberColumn("min floor", min_value=0.0, step=1.0),
            "statement_day": st.column_config.NumberColumn(
                "statement day (opt)", min_value=1, max_value=31
            ),
        },
        key=key,
    )


def iou_editor(df: pd.DataFrame, key: str | int | None = "iou_editor") -> pd.DataFrame:
    return st.data_editor(
        df,
        num_rows="dynamic",
        width="stretch",
        column_config={
            "name": st.column_config.TextColumn("name"),
            "balance": st.column_config.NumberColumn("balance", min_value=0.0, step=1.0),
            "apr": st.column_config.NumberColumn(
                "APR (decimal)", min_value=0.0, max_value=1.0, step=0.0001
            ),
            "due_day": st.column_config.NumberColumn("due day (opt)", min_value=1, max_value=31),
            "min_pct": st.column_config.NumberColumn(
                "min % (0-1)", min_value=0.0, max_value=1.0, step=0.001
            ),
            "min_floor": st.column_config.NumberColumn("min floor", min_value=0.0, step=1.0),
        },
        key=key,
    )


def oneoff_editor(df: pd.DataFrame, key: str | int | None = "oneoff_editor") -> pd.DataFrame:
    return st.data_editor(
        df,
        num_rows="dynamic",
        width="stretch",
        column_config={
            "name": st.column_config.TextColumn("name"),
            "due_date": st.column_config.DateColumn("due date", format="iso8601"),
            "amount": st.column_config.NumberColumn("amount", min_value=0.0, step=1.0),
            "priority": st.column_config.NumberColumn(
                "priority (higher=faster)", min_value=0, max_value=99, step=1
            ),
            "must_pay": st.column_config.CheckboxColumn("must pay?"),
        },
        key=key,
    )
</file>

<file path="src/cashsim/ui/state.py">
from __future__ import annotations

from datetime import date
from typing import Any

import pandas as pd
import streamlit as st

from cashsim.models import IOU, Bill, CreditCard, Dials, InvestmentSettings, OneOff

DEFAULT_BILLS_DF = pd.DataFrame(
    [
        {"name": "Rent", "usual_day": 1, "amount": 1200.0},
        {"name": "Car insurance", "usual_day": 15, "amount": 120.0},
        {"name": "Phone", "usual_day": 20, "amount": 60.0},
    ]
)
DEFAULT_CC_DF = pd.DataFrame(
    [
        {
            "name": "Chase VISA",
            "apr": 0.2499,
            "balance": 1200.0,
            "due_day": 24,
            "min_pct": 0.02,
            "min_floor": 25.0,
            "statement_day": None,
        }
    ]
)
DEFAULT_IOU_DF = pd.DataFrame(
    [
        {
            "name": "Friend",
            "balance": 0.0,
            "apr": 0.0,
            "due_day": None,
            "min_pct": 0.0,
            "min_floor": 0.0,
        }
    ]
)
DEFAULT_ONEOFF_DF = pd.DataFrame(
    [
        {
            "name": "Inspection",
            "due_date": date.today(),
            "amount": 35.0,
            "priority": 1,
            "must_pay": True,
        }
    ]
)


def init_session_once() -> None:
    st.session_state.setdefault("current_cash", 1200.0)
    st.session_state.setdefault("safety_cushion", 300.0)
    st.session_state.setdefault("weekday_earnings", 150.0)
    st.session_state.setdefault("gas_pct", 0.2)
    st.session_state.setdefault("gas_fill_size", 45.0)
    st.session_state.setdefault("bill_table", DEFAULT_BILLS_DF.copy())
    st.session_state.setdefault("cc_table", DEFAULT_CC_DF.copy())
    st.session_state.setdefault("iou_table", DEFAULT_IOU_DF.copy())
    st.session_state.setdefault("oneoff_table", DEFAULT_ONEOFF_DF.copy())
    st.session_state.setdefault("invest", InvestmentSettings().model_dump())
    st.session_state.setdefault("interest_mode", "statement_adb")
    st.session_state.setdefault("extra_strategy", "avalanche")
    st.session_state.setdefault(
        "blackouts_df", pd.DataFrame({"date": pd.Series([], dtype="datetime64[ns]")})
    )


def _coerce_date(v: object) -> date:
    if isinstance(v, date):
        return v
    if v is None or (isinstance(v, float) and pd.isna(v)):
        raise ValueError("one-off due_date is required")
    if isinstance(v, (str, float, int)):
        return pd.to_datetime(str(v)).date()
    raise ValueError(f"Cannot convert {type(v)} to date")


def _none_if_nan(v: object) -> int | None:
    if v is None:
        return None
    try:
        if isinstance(v, (float, int)) and pd.isna(v):
            return None
    except Exception:
        pass
    try:
        if isinstance(v, (int, float, str)):
            return int(float(v))
        return None
    except Exception:
        return None


def dials_from_state() -> Dials:
    bills = [Bill(**row) for row in st.session_state["bill_table"].to_dict(orient="records")]

    clean_cc_rows: list[dict[str, Any]] = []
    for row in st.session_state["cc_table"].to_dict(orient="records"):
        row_dict = dict(row)
        row_dict["statement_day"] = _none_if_nan(row_dict.get("statement_day"))
        clean_cc_rows.append(row_dict)
    cards = [CreditCard(**row) for row in clean_cc_rows]

    clean_iou_rows = []
    for row in st.session_state["iou_table"].to_dict(orient="records"):
        row = dict(row)
        row["due_day"] = _none_if_nan(row.get("due_day"))
        clean_iou_rows.append(row)
    ious = [IOU(**row) for row in clean_iou_rows]

    oneoffs = [
        OneOff(**{**row, "due_date": _coerce_date(row["due_date"])})
        for row in st.session_state["oneoff_table"].to_dict(orient="records")
    ]

    blackouts: list[date] = []
    for v in st.session_state.get("blackouts_df", pd.DataFrame({"date": []}))["date"]:
        if pd.notna(v):
            blackouts.append(pd.to_datetime(v).date())

    return Dials(
        current_cash=st.session_state["current_cash"],
        safety_cushion=st.session_state["safety_cushion"],
        weekday_earnings=st.session_state["weekday_earnings"],
        gas_pct=st.session_state["gas_pct"],
        gas_fill_size=st.session_state["gas_fill_size"],
        bills=bills,
        credit_cards=cards,
        ious=ious,
        oneoffs=oneoffs,
        invest=InvestmentSettings(**st.session_state["invest"]),
        interest_mode=st.session_state["interest_mode"],
        extra_strategy=st.session_state["extra_strategy"],
    )
</file>

<file path="src/cashsim/_typing_shims.py">
"""Typing shims.

These helpers provide stable type signatures for optional dependencies.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Any, overload


def _identity_decorator[**P, R](func: Callable[P, R]) -> Callable[P, R]:
    return func


def _decorator_factory[**P, R](
    *_args: Any,
    **_kwargs: Any,
) -> Callable[[Callable[P, R]], Callable[P, R]]:
    def decorator(func: Callable[P, R]) -> Callable[P, R]:
        return func

    return decorator


@overload
def cache_data[**P, R](func: Callable[P, R]) -> Callable[P, R]: ...


@overload
def cache_data[**P, R](
    *,
    ttl: float | None = None,
    **kwargs: Any,
) -> Callable[[Callable[P, R]], Callable[P, R]]: ...


def cache_data[**P, R](func: Callable[P, R] | None = None, **kwargs: Any) -> Any:
    """
    Streamlit cache_data shim.

    If streamlit is available, returns streamlit.cache_data. Otherwise,
    returns a no-op decorator.
    """
    try:
        import streamlit as st

        if func is None:
            return st.cache_data(**kwargs)
        return st.cache_data(func=func, **kwargs)
    except Exception:
        if func is not None:
            return _identity_decorator(func)
        return _decorator_factory(**kwargs)


@overload
def cache_resource[**P, R](func: Callable[P, R]) -> Callable[P, R]: ...


@overload
def cache_resource[**P, R](
    *,
    ttl: float | None = None,
    **kwargs: Any,
) -> Callable[[Callable[P, R]], Callable[P, R]]: ...


def cache_resource[**P, R](func: Callable[P, R] | None = None, **kwargs: Any) -> Any:
    """
    Streamlit cache_resource shim.

    If streamlit is available, returns streamlit.cache_resource. Otherwise,
    returns a no-op decorator.
    """
    try:
        import streamlit as st

        if func is None:
            return st.cache_resource(**kwargs)
        return st.cache_resource(func=func, **kwargs)
    except Exception:
        if func is not None:
            return _identity_decorator(func)
        return _decorator_factory(**kwargs)
</file>

<file path="src/cashsim/models.py">
from __future__ import annotations

from datetime import date
from typing import Literal

from pydantic import BaseModel, Field, field_validator


class Bill(BaseModel):
    name: str = Field(min_length=1)
    usual_day: int = Field(ge=1, le=31)
    amount: float = Field(ge=0)

    @field_validator("amount")
    @classmethod
    def two_decimals(cls, v: float) -> float:
        return round(float(v), 2)


class CreditCard(BaseModel):
    name: str = Field(min_length=1)
    apr: float = Field(ge=0, description="APR as decimal, e.g. 0.2499 for 24.99%")
    balance: float = Field(ge=0)
    due_day: int = Field(ge=1, le=31, description="Payment due day")
    min_pct: float = Field(ge=0, le=1, description="e.g. 0.02 for 2%")
    min_floor: float = Field(ge=0, description="absolute floor, e.g. $25")
    statement_day: int | None = Field(
        default=None,
        ge=1,
        le=31,
        description="If None, we approximate as due_day-21 (CARD Act minimum).",
    )


class IOU(BaseModel):
    name: str = Field(min_length=1)
    balance: float = Field(ge=0)
    apr: float = Field(ge=0, description="APR decimal; 0.0 if no interest")
    due_day: int | None = Field(default=None, ge=1, le=31)
    min_pct: float = Field(default=0.0, ge=0, le=1)
    min_floor: float = Field(default=0.0, ge=0)


class OneOff(BaseModel):
    name: str = Field(min_length=1)
    due_date: date
    amount: float = Field(ge=0)
    priority: int = 0
    must_pay: bool = True

    @field_validator("amount")
    @classmethod
    def two_decimals(cls, v: float) -> float:
        return round(float(v), 2)


class InvestmentSettings(BaseModel):
    enable: bool = True
    hysa_apy: float = Field(ge=0, default=0.045)
    expected_market_return: float = Field(ge=0, default=0.07)
    use_dca: bool = True
    roth_target: float = Field(ge=0, default=0.0)
    trad_target: float = Field(ge=0, default=0.0)
    hysa_target: float = Field(ge=0, default=0.0)
    robinhood_target: float = Field(ge=0, default=0.0)
    ira_year_limit: float = Field(ge=0, default=7000.0)
    roth_ok_today: bool = True


class Dials(BaseModel):
    current_cash: float = Field(ge=0)
    safety_cushion: float = Field(ge=0)
    weekday_earnings: float = Field(ge=0, description="Treated as DAILY earnings (7d/wk)")
    gas_pct: float = Field(ge=0, le=1)
    gas_fill_size: float = Field(gt=0)
    bills: list[Bill]
    credit_cards: list[CreditCard] = []
    ious: list[IOU] = []
    oneoffs: list[OneOff] = []
    invest: InvestmentSettings = InvestmentSettings()
    interest_mode: Literal["statement_adb", "due_simple"] = "statement_adb"
    extra_strategy: Literal["avalanche", "snowball"] = "avalanche"

    blackouts: list[date] = []
</file>

<file path=".pre-commit-config.yaml">
default_language_version:
  python: python3.12
repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  rev: v0.14.0
  hooks:
  - id: ruff-check
    args:
    - --fix
    - --unsafe-fixes
    - --ignore
    - E501
  - id: ruff-format
- repo: https://github.com/pre-commit/mirrors-mypy
  rev: v1.18.2
  hooks:
  - id: mypy
    additional_dependencies:
    - pandas-stubs
    - types-python-dateutil
    - typing-extensions>=4.0
- repo: https://github.com/ComPWA/pyright-pre-commit
  rev: v1.1.406
  hooks:
  - id: pyright
- repo: https://github.com/PyCQA/bandit
  rev: 1.8.6
  hooks:
  - id: bandit
    args:
    - -q
    - -r
    - src
    - --exclude
    - .venv,venv,tests
    pass_filenames: false
</file>

<file path="README.md">
# CashSim

> Streamlit-powered cash-flow & debt planning simulator with daily scheduling, ADB interest, and “what-if” analytics.

[![CI](https://github.com/josuejero/cashsim/actions/workflows/ci.yml/badge.svg)](https://github.com/josuejero/cashsim/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.12+-informational.svg)](#)
[![Live App](https://img.shields.io/badge/app-live-brightgreen.svg)](https://cashsim.streamlit.app)



## ✨ Features
- **Single-page Streamlit UI**: inputs, simulation, analytics, planner, wishlist (Google Sheets).
- **ADB interest math** (Decimal-correct) & due-date postings.
- **Daily planner**: variable schedule with blackout days and 7-day reserve logic.
- **Break-even grid & monthly snapshot** analytics.
- **Google Sheets** wishlist integration via service account.

## 🚀 Quickstart (local)
````bash
# 1) Python 3.12+ recommended
python -m venv .venv && source .venv/bin/activate

# 2) Install the package (editable ok)
pip install -e ".[dev]"     # or: pip install -r requirements.txt

# 3) (Optional) Google Sheets secrets for wishlist tab
#    Put TOML at: .streamlit/secrets.toml (see docs/SECRETS.md)

# 4) Launch the app
cashsim               # invokes Streamlit runner under the hood
# or: streamlit run src/cashsim/ui/streamlit_app.py
`````

## 🔧 Configuration

* Example config: [`examples/configs/sample.json`](examples/configs/sample.json)
* Local secrets: see [`docs/SECRETS.md`](docs/SECRETS.md)
  **Never commit secrets.** Use Streamlit Cloud’s Secrets for production.

## 🧪 Tests

```bash
pytest -q
```

## 📦 Deploy

### Streamlit Community Cloud (recommended)

* Connect repo → set **App file**: `src/cashsim/ui/streamlit_app.py`.
* Add **Secrets** (paste TOML from `docs/SECRETS.md` template).
* Optional: set custom subdomain (e.g., `cashsim.streamlit.app`).

### Hugging Face Spaces (Docker)

* Create Space (Docker) → add the `Dockerfile` from this repo.
* Add secrets in Space settings; write them to `.streamlit/secrets.toml` at container start.

### Google Cloud Run

* Build & push container image → **Deploy** to Cloud Run.
* Mount a Secret Manager secret to `/.streamlit/secrets.toml`.

## 🗺️ Roadmap

* CSV import/export
* Scenario comparison view
* CLI subcommands for headless batch simulation

## 🤝 Contributing

Bug reports & PRs welcome. See **CONTRIBUTING.md**.

## 📝 License

MIT — see **LICENSE**.
</file>

<file path="src/cashsim/analytics/snapshot.py">
from __future__ import annotations

from datetime import date
from typing import Any, TypedDict

import pandas as pd

from cashsim.models import Dials


class Snapshot(TypedDict):
    events: list[dict[str, Any]]
    first_date: date | None
    last_date: date | None
    income: float
    gas: float
    leftover: float


def monthly_snapshot(dials: Dials, months: int = 1) -> Snapshot:
    months = max(1, min(6, int(months)))
    today = date.today()
    result: Snapshot = {
        "events": [],
        "first_date": None,
        "last_date": None,
        "income": 0.0,
        "gas": 0.0,
        "leftover": 0.0,
    }

    for k in range(months):
        first_ts = (pd.Timestamp(today).to_period("M") + k).to_timestamp()
        last_ts = (pd.Timestamp(today).to_period("M") + k + 1).to_timestamp()

        workday_count = 20
        income = dials.weekday_earnings * workday_count
        gas = dials.weekday_earnings * dials.gas_pct * workday_count
        total_bills = sum(b.amount for b in dials.bills)
        leftover = income - gas - total_bills

        event = {
            "month": first_ts.strftime("%Y-%m"),
            "workday_count": workday_count,
            "est_income": round(income, 2),
            "est_gas": round(gas, 2),
            "total_bills": total_bills,
            "leftover": round(leftover, 2),
            "annualized_leftover": round(leftover * 12, 2),
        }
        result["events"].append(event)
        if k == 0:
            result["first_date"] = first_ts.date()
            result["last_date"] = last_ts.date()
            result["income"] = income
            result["gas"] = gas
            result["leftover"] = leftover
    return result
</file>

<file path="src/cashsim/sim/core.py">
from __future__ import annotations

import re
from datetime import date, timedelta

import pandas as pd

from cashsim.models import IOU, Bill, CreditCard, Dials, OneOff
from cashsim.utils.date_utils import last_statement_close_date, next_statement_close_date
from cashsim.utils.date_utils import next_due_date_cached as next_due_date

from .events import add_extra_event, add_finance_event, add_min_event
from .gas import apply_gas_skim_and_fillups
from .interest import (
    accrue_adb_daily,
    post_simple_interest_if_due,
    post_statement_charges_and_advance,
)
from .reserve import reserve_next_7_days
from .roll import sum_non_debt_bills_due_today
from .types import SimMetrics


def _slug(s: str) -> str:
    return re.sub(r"[^a-z0-9_]+", "_", s.lower()).strip("_")


def simulate_month(
    dials: Dials, *, start: date | None = None, days: int = 31
) -> tuple[pd.DataFrame, SimMetrics]:
    start = start or date.today()

    bills: list[Bill] = list(dials.bills)
    cards: list[CreditCard] = [CreditCard(**c.model_dump()) for c in dials.credit_cards]
    ious: list[IOU] = [IOU(**x.model_dump()) for x in dials.ious]
    oneoffs: list[OneOff] = [OneOff(**o.model_dump()) for o in getattr(dials, "oneoffs", [])]

    card_state: dict[str, dict] = {}
    for c in cards:
        cycle_start = last_statement_close_date(start, c.due_day, c.statement_day)
        next_stmt = next_statement_close_date(start, c.due_day, c.statement_day)
        cycle_due = next_due_date(next_stmt + timedelta(days=1), c.due_day)
        card_state[c.name] = {
            "cycle_start": cycle_start,
            "next_stmt": next_stmt,
            "cycle_due": cycle_due,
            "adb_sum": 0.0,
            "adb_days": 0,
            "statement_balance": None,
            "min_due_locked": None,
            "grace_active": (c.balance <= 0.0),
            "payments_since_stmt": 0.0,
            "unposted_interest_est": 0.0,
        }

    oneoff_saved: dict[str, float] = {o.name: 0.0 for o in oneoffs}
    oneoff_paid: dict[str, bool] = {o.name: False for o in oneoffs}

    records: list[dict] = []
    gas_bucket = 0.0
    balance = float(dials.current_cash)
    min_balance = balance
    min_balance_date: date | None = start
    first_negative: date | None = None
    cushion_breach_date: date | None = None

    paid_non_debt_bills = 0.0
    paid_cc_mins = 0.0
    paid_iou_mins = 0.0
    paid_oneoffs_total = 0.0

    last_interest_post_simple: dict[str, date] = {c.name: start for c in cards}

    for i in range(days):
        day = start + timedelta(days=i)
        earn = float(dials.weekday_earnings)

        balance, gas_bucket, fillups = apply_gas_skim_and_fillups(
            balance, gas_bucket, earn, dials.gas_pct, dials.gas_fill_size
        )

        bill_due_today = sum_non_debt_bills_due_today(day, bills)
        if bill_due_today > 0:
            balance -= bill_due_today
            paid_non_debt_bills += bill_due_today

        oneoff_contrib_events: list[tuple[str, float]] = []
        total_now, _, _, gas_pred_now = reserve_next_7_days(
            day,
            bills=bills,
            cards=cards,
            ious=ious,
            oneoffs=oneoffs,
            include_today=True,
            locked_mins={},
            locked_due_dates={},
            oneoff_saved=oneoff_saved,
            gas_bucket=gas_bucket,
            daily_earn=earn,
            gas_pct=dials.gas_pct,
            fill_size=dials.gas_fill_size,
        )
        reserve_now = dials.safety_cushion + total_now
        surplus_now = round(balance - reserve_now, 2)
        if surplus_now > 0 and oneoffs:
            targets = sorted(
                [o for o in oneoffs if not oneoff_paid[o.name] and o.due_date >= day],
                key=lambda o: (-int(o.priority), o.due_date),
            )
            remaining = surplus_now
            for o in targets:
                shortfall = max(0.0, round(o.amount - oneoff_saved[o.name], 2))
                if shortfall <= 0 or remaining <= 0:
                    continue
                contrib = min(shortfall, remaining)
                oneoff_saved[o.name] = round(oneoff_saved[o.name] + contrib, 2)
                balance = round(balance - contrib, 2)
                remaining = round(remaining - contrib, 2)
                oneoff_contrib_events.append((o.name, contrib))

        oneoff_paid_events: list[tuple[str, float]] = []
        for o in oneoffs:
            if (not oneoff_paid[o.name]) and (day == o.due_date):
                use_saved = min(oneoff_saved[o.name], o.amount)
                from_balance = round(o.amount - use_saved, 2)
                oneoff_saved[o.name] = round(oneoff_saved[o.name] - use_saved, 2)
                if from_balance > 0:
                    balance = round(balance - from_balance, 2)
                oneoff_paid[o.name] = True
                oneoff_paid_events.append((o.name, o.amount))
                paid_oneoffs_total = round(paid_oneoffs_total + o.amount, 2)

        interest_events: list[tuple[str, float]] = []
        minpay_events: list[tuple[str, float]] = []
        extra_on_due_events: list[tuple[str, float]] = []
        unified_events: list[dict] = []

        if dials.interest_mode == "statement_adb":
            accrue_adb_daily(card_state, cards)

        if dials.interest_mode == "statement_adb":
            for acc, amt in post_statement_charges_and_advance(day, card_state, cards):
                interest_events.append((acc, amt))

                for c in cards:
                    if c.name == acc:
                        add_finance_event(unified_events, day, acc, amt, c.balance)
                        break

        due_today = [c for c in cards if next_due_date(day, c.due_day) == day and c.balance > 0]
        if due_today:
            locked_mins = {
                n: st["min_due_locked"]
                for n, st in card_state.items()
                if st["min_due_locked"] is not None
            }

            for c in due_today:
                locked = locked_mins.get(c.name)
                due_min = (
                    locked
                    if locked is not None
                    else max(c.min_floor, round(c.balance * c.min_pct, 2))
                )
                pay_min = min(due_min, c.balance)
                if pay_min > 0:
                    c.balance = round(c.balance - pay_min, 2)
                    balance = round(balance - pay_min, 2)
                    paid_cc_mins += pay_min
                    minpay_events.append((c.name, pay_min))
                    add_min_event(unified_events, day, c.name, pay_min, c.balance)
                    if dials.interest_mode == "statement_adb":
                        card_state[c.name]["payments_since_stmt"] += pay_min

            locked_due = {
                n: st["cycle_due"]
                for n, st in card_state.items()
                if st["min_due_locked"] is not None
            }
            total7, bills7, mins7, gas7 = reserve_next_7_days(
                day,
                bills=bills,
                cards=cards,
                ious=ious,
                oneoffs=oneoffs,
                include_today=False,
                locked_mins=locked_mins,
                locked_due_dates=locked_due,
                oneoff_saved=oneoff_saved,
                gas_bucket=gas_bucket,
                daily_earn=earn,
                gas_pct=dials.gas_pct,
                fill_size=dials.gas_fill_size,
            )
            reserve_need = dials.safety_cushion + total7
            surplus = round(balance - reserve_need, 2)

            if dials.extra_strategy == "avalanche":
                due_today.sort(key=lambda c: c.apr, reverse=True)
            else:
                due_today.sort(key=lambda c: c.balance)

            remaining = surplus
            for c in due_today:
                if remaining <= 0 or c.balance <= 0:
                    break
                extra = min(remaining, c.balance)
                if extra > 0:
                    c.balance = round(c.balance - extra, 2)
                    balance = round(balance - extra, 2)
                    remaining = round(remaining - extra, 2)
                    extra_on_due_events.append((c.name, extra))
                    add_extra_event(
                        unified_events,
                        day,
                        c.name,
                        extra,
                        c.balance,
                        surplus_used=extra,
                        reserve_needed=reserve_need,
                        reserve_7d_bills=bills7,
                        reserve_7d_mins=mins7,
                        reserve_7d_gas=gas7,
                    )
                    if dials.interest_mode == "statement_adb":
                        card_state[c.name]["payments_since_stmt"] += extra

            if dials.interest_mode == "statement_adb":
                for c in due_today:
                    st = card_state[c.name]
                    if st["statement_balance"] is not None:
                        st["grace_active"] = (st["payments_since_stmt"] + 1e-9) >= (
                            st["statement_balance"] - 1e-9
                        )

        if dials.interest_mode == "due_simple":
            for acc, amt in post_simple_interest_if_due(day, last_interest_post_simple, cards):
                interest_events.append((acc, amt))
                for c in cards:
                    if c.name == acc:
                        add_finance_event(unified_events, day, acc, amt, c.balance)
                        break

        if balance < min_balance:
            min_balance = balance
            min_balance_date = day
        if first_negative is None and balance < 0:
            first_negative = day
        if cushion_breach_date is None and balance < dials.safety_cushion:
            cushion_breach_date = day

        accrued_est = 0.0
        if dials.interest_mode == "statement_adb":
            accrued_est = round(sum(st["unposted_interest_est"] for st in card_state.values()), 2)

        row: dict = {
            "date": day,
            "weekday": day.strftime("%a"),
            "earn": round(earn, 2),
            "bill_due": round(bill_due_today, 2),
            "balance": round(balance, 2),
            "gas_bucket": round(gas_bucket, 2),
            "fillups": int(fillups),
            "cc_events": unified_events,
            "cc_interest_posted": interest_events,
            "cc_min_paid": minpay_events,
            "cc_extra_on_due": extra_on_due_events,
            "oneoff_contribs": oneoff_contrib_events,
            "oneoff_paid": oneoff_paid_events,
            "total_cc_balance": round(sum(c.balance for c in cards), 2),
            "total_iou_balance": round(sum(x.balance for x in ious), 2),
            "accrued_interest_unposted": accrued_est,
        }
        for c in cards:
            row[f"cc_{_slug(c.name)}_bal"] = round(c.balance, 2)
        records.append(row)

    df = pd.DataFrame.from_records(records)

    if min_balance < 0:
        friend_ask_needed = round(-min_balance, 2)
        friend_ask_latest_date = first_negative
    else:
        friend_ask_needed = 0.0
        friend_ask_latest_date = None

    total_upcoming_bills = round(
        paid_non_debt_bills + paid_cc_mins + paid_iou_mins + paid_oneoffs_total, 2
    )

    metrics = SimMetrics(
        min_balance=round(min_balance, 2),
        min_balance_date=min_balance_date,
        first_negative_date=first_negative,
        total_upcoming_bills=total_upcoming_bills,
        friend_ask_needed=friend_ask_needed,
        friend_ask_latest_date=friend_ask_latest_date,
        cushion_breach_date=cushion_breach_date,
        accrued_interest_estimate=round(
            sum(st["unposted_interest_est"] for st in card_state.values()), 2
        )
        if dials.interest_mode == "statement_adb"
        else 0.0,
    )
    return df, metrics
</file>

<file path="src/cashsim/sim/interest.py">
from __future__ import annotations

from datetime import date, timedelta
from decimal import ROUND_HALF_UP

from cashsim.models import CreditCard
from cashsim.utils.date_utils import next_due_date, next_statement_close_date
from cashsim.utils.money import D, quantize_cents, round_cents


def accrue_adb_daily(card_state: dict[str, dict], cards: list[CreditCard]) -> None:
    """
    Accumulate ADB using Decimal internally for interest math.
    Store back as floats to maintain compatibility with existing code.
    """
    for c in cards:
        st = card_state[c.name]
        st["adb_sum"] = float(D(st.get("adb_sum", 0)) + D(c.balance))
        st["adb_days"] = int(st.get("adb_days", 0)) + 1

        st["unposted_interest_est"] = float(
            D(st.get("unposted_interest_est", 0)) + (D(c.apr) / D(365)) * D(c.balance)
        )


def post_statement_charges_and_advance(
    day: date, card_state: dict[str, dict], cards: list[CreditCard]
) -> list[tuple[str, float]]:
    """
    Post ADB finance charges on statement close, lock min due, advance cycle.
    Interest is computed with Decimal and quantized to cents using HALF_UP,
    which is typical for consumer statements. Switch to ROUND_HALF_EVEN only
    if you explicitly choose banker's rounding and document why.
    """
    events: list[tuple[str, float]] = []
    for c in cards:
        st = card_state[c.name]
        if day == st["next_stmt"]:
            if st.get("adb_days", 0) > 0 and c.apr > 0 and c.balance > 0:
                avg_daily = D(st["adb_sum"]) / D(st["adb_days"])
                daily_rate = D(c.apr) / D(365)
                i_dec = quantize_cents(
                    avg_daily * daily_rate * D(st["adb_days"]), rounding=ROUND_HALF_UP
                )
                i_amt = float(i_dec)
                if i_amt > 0:
                    c.balance = round_cents(D(c.balance) + i_dec)
                    events.append((c.name, i_amt))

            st["statement_balance"] = c.balance
            min_from_pct = float(
                quantize_cents(D(st["statement_balance"]) * D(c.min_pct), rounding=ROUND_HALF_UP)
            )
            st["min_due_locked"] = max(c.min_floor, min_from_pct)
            st["next_stmt"] = next_statement_close_date(
                day + timedelta(days=1), c.due_day, c.statement_day
            )
            st["cycle_due"] = next_due_date(day + timedelta(days=1), c.due_day)
            st["adb_sum"] = 0.0
            st["adb_days"] = 0
            st["payments_since_stmt"] = 0.0
            st["unposted_interest_est"] = 0.0
    return events


def post_simple_interest_if_due(
    day: date, last_post: dict[str, date], cards: list[CreditCard]
) -> list[tuple[str, float]]:
    """Legacy: post interest on due date using days elapsed (Decimal math)."""
    out: list[tuple[str, float]] = []
    for c in cards:
        if next_due_date(day, c.due_day) == day and c.balance > 0 and c.apr > 0:
            days_elapsed = max(0, (day - last_post[c.name]).days)
            if days_elapsed > 0:
                i_dec = quantize_cents(
                    D(c.balance) * (D(c.apr) / D(365)) * D(days_elapsed), rounding=ROUND_HALF_UP
                )
                c.balance = round_cents(D(c.balance) + i_dec)
                out.append((c.name, float(i_dec)))
                last_post[c.name] = day
    return out
</file>

<file path="src/cashsim/ui/formatters.py">
from __future__ import annotations

from collections.abc import Iterable
from typing import cast

import pandas as pd


def stringify_events_columns(df: pd.DataFrame) -> pd.DataFrame:
    out = df.copy()

    def _fmt_pair_list(v: object) -> str:
        if isinstance(v, (list, tuple)) and len(v) > 0:
            try:
                pairs = cast(Iterable[tuple[str, float]], v)
                return "; ".join(f"{name}: ${float(amt):,.2f}" for (name, amt) in pairs)
            except Exception:
                return str(v)
        return "" if v is None or (isinstance(v, float) and pd.isna(v)) else str(v)

    for col in [
        "cc_extra_on_due",
        "cc_interest_posted",
        "cc_min_paid",
        "oneoff_contribs",
        "oneoff_paid",
    ]:
        if col in out.columns:
            out[col] = out[col].apply(_fmt_pair_list)
    return out
</file>

<file path="src/cashsim/__init__.py">
from importlib import import_module

from .analytics.break_even import break_even_grid
from .analytics.snapshot import monthly_snapshot
from .models import IOU, Bill, CreditCard, Dials, InvestmentSettings, OneOff
from .sim.core import simulate_month
from .sim.types import SimMetrics

__all__ = [
    "Bill",
    "CreditCard",
    "IOU",
    "OneOff",
    "InvestmentSettings",
    "Dials",
    "simulate_month",
    "SimMetrics",
    "break_even_grid",
    "monthly_snapshot",
]


def __getattr__(
    name: str,
) -> Bill | CreditCard | IOU | OneOff | InvestmentSettings | Dials | SimMetrics | type:
    if name in {"Bill", "CreditCard", "IOU", "OneOff", "InvestmentSettings", "Dials"}:
        m = import_module(".models", __name__)
        return getattr(m, name)

    if name == "simulate_month":
        return import_module(".sim.core", __name__).simulate_month
    if name == "SimMetrics":
        return import_module(".sim.types", __name__).SimMetrics

    if name == "break_even_grid":
        return import_module(".analytics.break_even", __name__).break_even_grid
    if name == "monthly_snapshot":
        return import_module(".analytics.snapshot", __name__).monthly_snapshot
    raise AttributeError(name)
</file>

<file path="tests/test_interest_decimal.py">
from decimal import ROUND_HALF_EVEN, ROUND_HALF_UP, Decimal

from cashsim.utils.money import D, quantize_cents


def test_rounding_modes_examples() -> None:
    assert quantize_cents(Decimal("1.005"), rounding=ROUND_HALF_UP) == Decimal("1.01")
    assert quantize_cents(Decimal("1.015"), rounding=ROUND_HALF_EVEN) == Decimal("1.02")


def test_adb_property_equivalence() -> None:
    daily = [Decimal("100.00")] * 15 + [Decimal("200.00")] * 15
    days = Decimal(len(daily))
    apr = Decimal("0.2199")
    daily_rate = apr / Decimal(365)
    adb = sum(daily, D("0")) / days

    i1 = quantize_cents(adb * daily_rate * days, rounding=ROUND_HALF_UP)
    i2 = quantize_cents(sum((b * daily_rate for b in daily), D("0")), rounding=ROUND_HALF_UP)
    assert i1 == i2
</file>

<file path=".gitignore">
config.json
bobbie_config.json

.streamlit/

# Streamlit secrets
.streamlit/
</file>

<file path="mypy.ini">
[mypy]
python_version = 3.12
warn_unused_ignores = True

[mypy-streamlit.*]
ignore_missing_imports = True

[mypy-gspread.*]
ignore_missing_imports = True

[mypy-google.*]
ignore_missing_imports = True

[mypy-gspread_dataframe.*]
ignore_missing_imports = True

[mypy-pydantic.*]
ignore_missing_imports = True

[mypy-typing_extensions.*]
ignore_missing_imports = True

[mypy-hypothesis.*]
ignore_missing_imports = True
</file>

<file path="src/cashsim/planning/planner.py">
from __future__ import annotations

from collections.abc import Iterable
from dataclasses import dataclass
from datetime import date, timedelta
from typing import Literal

from cashsim.models import IOU, Bill, CreditCard, Dials, OneOff
from cashsim.sim.core import simulate_month
from cashsim.sim.gas import apply_gas_skim_and_fillups
from cashsim.sim.reserve import reserve_next_7_days
from cashsim.sim.roll import sum_non_debt_bills_due_today
from cashsim.utils.date_utils import next_due_date_cached as next_due_date


@dataclass
class PlanResult:
    ok: bool
    daily_target: float
    min_balance: float
    first_negative_date: date | None


def plan_min_daily_earnings(
    dials: Dials,
    *,
    start: date | None = None,
    days: int = 31,
    lo: float = 0.0,
    hi: float = 1000.0,
    tol: float = 0.01,
) -> PlanResult:
    """
    Binary-search the minimum constant daily earnings that avoids going below $0
    (you can change the criterion to dials.safety_cushion if preferred).
    """
    start = start or date.today()

    while True:
        test = dials.model_copy(update={"weekday_earnings": hi})
        _df, m = simulate_month(test, start=start, days=days)
        if m.first_negative_date is None:
            break
        hi *= 2
        if hi > 1e6:
            return PlanResult(False, hi, m.min_balance, m.first_negative_date)

    best = hi
    while hi - lo > tol:
        mid = (lo + hi) / 2.0
        test = dials.model_copy(update={"weekday_earnings": mid})
        _df, m = simulate_month(test, start=start, days=days)
        if m.first_negative_date is None:
            best = mid
            hi = mid
        else:
            lo = mid

    _df, m = simulate_month(
        dials.model_copy(update={"weekday_earnings": best}), start=start, days=days
    )
    return PlanResult(True, round(best, 2), m.min_balance, m.first_negative_date)


@dataclass
class DailyPlanRow:
    date: date
    earn_required: float
    earn_capped: float
    cap_exceeded: bool
    end_balance: float
    reserve_7d_total: float
    bills_today: float
    mins_today: float
    oneoffs_today: float
    gas_fill_cost_today: float
    gas_bucket_end: float


@dataclass
class DailyPlanResult:
    ok: bool
    min_balance: float
    min_balance_date: date | None
    rows: list[DailyPlanRow]


def _cc_mins_due_today(day: date, cards: list[CreditCard]) -> float:
    total = 0.0
    for c in cards:
        due = next_due_date(day, c.due_day)
        if due == day and c.balance > 0:
            due_min = max(c.min_floor, round(c.balance * c.min_pct, 2))
            pay = min(due_min, c.balance)
            total += pay
    return round(total, 2)


def _iou_mins_due_today(day: date, ious: list[IOU]) -> float:
    total = 0.0
    for x in ious:
        if x.due_day is None:
            continue
        due = next_due_date(day, x.due_day)
        if due == day and x.balance > 0:
            due_min = max(x.min_floor, round(x.balance * x.min_pct, 2))
            pay = min(due_min, x.balance)
            total += pay
    return round(total, 2)


def _pay_today_side_effects(
    day: date, cards: list[CreditCard], ious: list[IOU], oneoffs: list[OneOff]
) -> tuple[float, float, float]:
    """
    Apply today's minimum payments to card/IOU balances (mutating them) and return:
    (bills_today_non_debt_placeholder, mins_today_paid, oneoffs_paid_today).
    Non-debt bills are handled separately by sum_non_debt_bills_due_today.
    """
    bills_today = 0.0
    mins_today = 0.0
    oneoffs_today = 0.0

    for c in cards:
        due = next_due_date(day, c.due_day)
        if due == day and c.balance > 0:
            due_min = max(c.min_floor, round(c.balance * c.min_pct, 2))
            pay = min(due_min, c.balance)
            c.balance = round(c.balance - pay, 2)
            mins_today += pay

    for x in ious:
        if x.due_day is None:
            continue
        due = next_due_date(day, x.due_day)
        if due == day and x.balance > 0:
            due_min = max(x.min_floor, round(x.balance * x.min_pct, 2))
            pay = min(due_min, x.balance)
            x.balance = round(x.balance - pay, 2)
            mins_today += pay

    for o in oneoffs:
        if o.due_date == day and o.amount > 0:
            oneoffs_today += o.amount

    return round(bills_today, 2), round(mins_today, 2), round(oneoffs_today, 2)


def plan_variable_daily_earnings(
    dials: Dials,
    *,
    start: date | None = None,
    days: int = 60,
    safety_target: Literal["zero", "cushion"] = "zero",
    daily_cap: float | None = None,
    future_daily_hint: float | None = None,
    blackout_dates: Iterable[date] | None = None,
    tol: float = 0.01,
) -> DailyPlanResult:
    start = start or date.today()
    days = max(1, int(days))

    fh: float | None = future_daily_hint
    future_hint: float = float(fh) if fh is not None else float(dials.weekday_earnings)
    balance = float(dials.current_cash)
    gas_bucket = 0.0
    cards: list[CreditCard] = [CreditCard(**c.model_dump()) for c in dials.credit_cards]
    ious: list[IOU] = [IOU(**x.model_dump()) for x in dials.ious]
    oneoffs: list[OneOff] = [OneOff(**o.model_dump()) for o in getattr(dials, "oneoffs", [])]
    bills: list[Bill] = list(dials.bills)

    rows: list[DailyPlanRow] = []
    min_balance = balance
    min_balance_date: date | None = start
    ok = True

    blackout_set: set[date] = set(blackout_dates or [])

    for i in range(days):
        day = start + timedelta(days=i)
        is_blackout = day in blackout_set

        nondebt_today = sum_non_debt_bills_due_today(day, bills)

        cards_snapshot = [CreditCard(**c.model_dump()) for c in cards]
        ious_snapshot = [IOU(**x.model_dump()) for x in ious]

        cc_min_peek = _cc_mins_due_today(day, cards_snapshot)
        iou_min_peek = _iou_mins_due_today(day, ious_snapshot)
        oneoffs_today_peek = sum(o.amount for o in oneoffs if o.due_date == day)

        def end_bal_if(
            earn: float,
            *,
            _balance: float = balance,
            _gas_bucket: float = gas_bucket,
            _nondebt: float = nondebt_today,
            _cc_min: float = cc_min_peek,
            _iou_min: float = iou_min_peek,
            _oneoffs_due: float = oneoffs_today_peek,
        ) -> tuple[float, float, float]:
            """End-of-day balance if we earned 'earn' today (no mutation of real state)."""
            b2, g2, fills = apply_gas_skim_and_fillups(
                _balance, _gas_bucket, earn, dials.gas_pct, dials.gas_fill_size
            )
            gas_fill_cost = fills * dials.gas_fill_size
            b2 -= _nondebt
            b2 -= _cc_min
            b2 -= _iou_min
            b2 -= _oneoffs_due
            return round(b2, 2), round(g2, 2), round(gas_fill_cost, 2)

        def reserve_need_after(
            earn: float,
            *,
            _day: date = day,
            _cards: list[CreditCard] = cards,
            _ious: list[IOU] = ious,
            _oneoffs: list[OneOff] = oneoffs,
            _bills: list[Bill] = bills,
            _future_hint: float = future_hint,
            _gas_pct: float = float(dials.gas_pct),
            _fill_size: float = float(dials.gas_fill_size),
        ) -> tuple[float, float, float, float, float]:
            """Tomorrow's 7-day reserve need after earning 'earn' and paying today's items."""
            b2, g2, _ = end_bal_if(earn)

            cards_next = [CreditCard(**c.model_dump()) for c in _cards]
            ious_next = [IOU(**x.model_dump()) for x in _ious]
            oneoffs_next = [OneOff(**o.model_dump()) for o in _oneoffs]
            _ = _pay_today_side_effects(_day, cards_next, ious_next, oneoffs_next)

            total7, bills7, mins7, gas7 = reserve_next_7_days(
                _day,
                bills=_bills,
                cards=cards_next,
                ious=ious_next,
                oneoffs=oneoffs_next,
                include_today=False,
                locked_mins=None,
                locked_due_dates=None,
                oneoff_saved=None,
                gas_bucket=g2,
                daily_earn=_future_hint,
                gas_pct=_gas_pct,
                fill_size=_fill_size,
            )
            base = 0.0 if safety_target == "zero" else float(dials.safety_cushion)
            need = base + total7
            return round(need, 2), round(bills7, 2), round(mins7, 2), round(gas7, 2), b2

        if is_blackout:
            earn_required = 0.0
        else:
            lo, hi = 0.0, max(50.0, dials.weekday_earnings * 2.0)
            while True:
                need, _, _, _, b2 = reserve_need_after(hi)
                if b2 >= need:
                    break
                hi *= 2
                if hi > 1e6:
                    break

            best = hi
            if hi <= 1e6:
                while hi - lo > tol:
                    mid = (lo + hi) / 2.0
                    need, _, _, _, b2 = reserve_need_after(mid)
                    if b2 >= need:
                        best = mid
                        hi = mid
                    else:
                        lo = mid
            earn_required = round(best, 2)

        cap_exceeded = False
        earn_used = earn_required
        if daily_cap is not None:
            earn_used = min(earn_required, float(daily_cap))
            cap_exceeded = earn_required - earn_used > 1e-9
            if cap_exceeded:
                ok = False

        b2, g2, gas_fill_cost = apply_gas_skim_and_fillups(
            balance,
            gas_bucket,
            0.0 if is_blackout else earn_used,
            dials.gas_pct,
            dials.gas_fill_size,
        )

        b2 -= nondebt_today

        _, mins_today, oneoffs_today = _pay_today_side_effects(day, cards, ious, oneoffs)
        b2 -= mins_today
        b2 -= oneoffs_today

        if b2 < min_balance:
            min_balance = round(b2, 2)
            min_balance_date = day

        total7, _, _, _ = reserve_next_7_days(
            day,
            bills=bills,
            cards=cards,
            ious=ious,
            oneoffs=oneoffs,
            include_today=False,
            locked_mins=None,
            locked_due_dates=None,
            oneoff_saved=None,
            gas_bucket=g2,
            daily_earn=future_hint,
            gas_pct=dials.gas_pct,
            fill_size=dials.gas_fill_size,
        )
        reserve_total = round(total7, 2)

        rows.append(
            DailyPlanRow(
                date=day,
                earn_required=round(earn_required, 2),
                earn_capped=round(earn_used, 2),
                cap_exceeded=cap_exceeded,
                end_balance=round(b2, 2),
                reserve_7d_total=reserve_total,
                bills_today=round(nondebt_today, 2),
                mins_today=round(mins_today, 2),
                oneoffs_today=round(oneoffs_today, 2),
                gas_fill_cost_today=round(gas_fill_cost, 2),
                gas_bucket_end=round(g2, 2),
            )
        )

        balance = round(b2, 2)
        gas_bucket = round(g2, 2)

    return DailyPlanResult(
        ok=ok,
        min_balance=round(min_balance, 2),
        min_balance_date=min_balance_date,
        rows=rows,
    )


def constant_target_from_schedule(
    rows: list[DailyPlanRow], blackout_dates: Iterable[date]
) -> float:
    """Return a safe constant target to hit on working days.

    It equals the max `earn_required` on non-blackout days.
    """
    bset = set(blackout_dates or [])
    needers = [r.earn_required for r in rows if r.date not in bset]
    return round(max(needers), 2) if needers else 0.0
</file>

<file path="src/cashsim/sim/types.py">
from __future__ import annotations

from dataclasses import dataclass
from datetime import date
from typing import Literal

from typing_extensions import NotRequired, TypedDict


class BaseEvent(TypedDict):
    day: date
    account: str
    new_balance: float


class FinanceEvent(BaseEvent):
    kind: Literal["finance"]
    amt: float


class MinEvent(BaseEvent):
    kind: Literal["min"]
    amt: float


class ExtraEvent(BaseEvent):
    kind: Literal["extra"]
    extra: float
    surplus_used: NotRequired[float]
    reserve_needed: NotRequired[float]
    reserve_7d_bills: NotRequired[float]
    reserve_7d_mins: NotRequired[float]
    reserve_7d_gas: NotRequired[float]


Event = FinanceEvent | MinEvent | ExtraEvent


class CardState(TypedDict, total=False):
    balance: float
    adb_days: float
    adb_sum: float
    min_due_locked: float


@dataclass
class SimMetrics:
    min_balance: float
    min_balance_date: date | None
    first_negative_date: date | None
    total_upcoming_bills: float
    friend_ask_needed: float
    friend_ask_latest_date: date | None
    cushion_breach_date: date | None
    accrued_interest_estimate: float
</file>

<file path="src/cashsim/utils/money.py">
from __future__ import annotations

from decimal import ROUND_HALF_UP, Decimal

CENT = Decimal("0.01")


def D(x: float | int | str | Decimal) -> Decimal:
    """Safe Decimal constructor (avoid binary float artifacts)."""
    return x if isinstance(x, Decimal) else Decimal(str(x))


def quantize_cents(x: Decimal, *, rounding: str = ROUND_HALF_UP) -> Decimal:
    """Quantize a Decimal to cents with an explicit rounding mode."""
    return D(x).quantize(CENT, rounding=rounding)


def to_cents(x: float | str | Decimal) -> int:
    """
    Convert a dollar amount to integer cents using HALF_UP.
    Use only at I/O boundaries (storage, display, external APIs).
    """
    return int(quantize_cents(D(x), rounding=ROUND_HALF_UP) * 100)


def from_cents(c: int) -> float:
    """Convert integer cents back to float dollars representation (display only)."""
    return float(Decimal(int(c)) / 100)


def round_cents(x: float | str | Decimal) -> float:
    """
    Round a dollar amount to two decimals (HALF_UP) and return float.
    Prefer doing all intermediate arithmetic with Decimal, then call this when
    you must hand a float to existing models/fields.
    """
    return float(quantize_cents(D(x), rounding=ROUND_HALF_UP))


def round_money(x: Decimal) -> Decimal:
    return quantize_cents(D(x), rounding=ROUND_HALF_UP)


def post_daily_interest(balance: Decimal, apr: Decimal) -> Decimal:
    daily_rate = D(apr) / D("365")
    return round_money(D(balance) * daily_rate)
</file>

<file path=".github/workflows/ci.yml">
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }} (with pip cache)
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            requirements*.txt

      - name: Upgrade pip
        run: python -m pip install --upgrade pip

      - name: Install project (editable)
        run: pip install -e ".[dev]"

      - name: Ruff (lint)
        run: ruff check .

      - name: Pyright (type check)
        run: pyright

      - name: Mypy (type check)
        run: mypy .

      - name: Pytest
        run: pytest -q
</file>

<file path="src/cashsim/cli.py">
from __future__ import annotations

import importlib
import json
import sys
from datetime import date
from pathlib import Path
import subprocess
import typer

from cashsim.batch import run_from_config
from cashsim.compare import metrics_delta, series_diff
from cashsim.io.exporters import metrics_to_dict, write_run
from cashsim.compare import build_compare_payload, render_compare_markdown, series_diff

APP_HELP = (
    "CashSim CLI. Use `cashsim ui` for the Streamlit app and "
    "`cashsim simulate/export/compare` for batch mode."
)

app = typer.Typer(add_completion=False, no_args_is_help=False, help=APP_HELP)

HEADLESS_OPTION = typer.Option(
    False,
    "--headless/--no-headless",
    help="Run Streamlit without opening a browser.",
)
CONFIG_OPTION = typer.Option(
    ...,
    "--config",
    exists=True,
    dir_okay=False,
    help="Path to a Dials JSON config.",
)
START_OPTION = typer.Option(
    ...,
    "--start",
    help="Simulation start date (YYYY-MM-DD).",
)
DAYS_OPTION = typer.Option(
    31,
    "--days",
    min=1,
    max=366,
    help="Number of days to simulate.",
)
OUT_OPTION = typer.Option(
    Path("out"),
    "--out",
    help="Output directory.",
)
SERIES_FORMAT_OPTION = typer.Option(
    "csv",
    "--series-format",
    help="csv|json",
)
EVENTS_OPTION = typer.Option(
    False,
    "--events/--no-events",
    help="Also export events.csv (flattened).",
)
OVERWRITE_OPTION = typer.Option(
    False,
    "--overwrite",
    help="Overwrite existing artifacts in --out.",
)
A_OPTION = typer.Option(
    ...,
    "--a",
    exists=True,
    dir_okay=False,
    help="Config A (baseline).",
)
B_OPTION = typer.Option(
    ...,
    "--b",
    exists=True,
    dir_okay=False,
    help="Config B (candidate).",
)
SERIES_OPTION = typer.Option(
    False,
    "--series/--no-series",
    help="Also write an aligned series_diff.csv.",
)
REPORT_OPTION = typer.Option(
    None,
    "--report",
    help="Write a human-readable compare report to Markdown. If relative, it is written under --out.",
)
THRESHOLD_OPTION = typer.Option(
    0.01,
    "--threshold",
    min=0,
    help="Threshold used by series_summary to flag first divergence.",
)


def _parse_date(value: str) -> date:
    try:
        return date.fromisoformat(value)
    except ValueError as e:
        raise typer.BadParameter("Date must be YYYY-MM-DD") from e


def _streamlit_app_file() -> str:
    mod = importlib.import_module("cashsim.ui.streamlit_app")
    file = getattr(mod, "__file__", None)
    if file is None:
        raise RuntimeError("cashsim.ui.streamlit_app has no __file__; install may be broken")
    return str(Path(file).resolve())


def _run_streamlit(app_file: str, *, headless: bool, extra_args: list[str]) -> None:
    from streamlit.web import cli as stcli

    argv = ["streamlit", "run", app_file]
    if headless:
        argv.append("--server.headless=true")

    argv.extend(extra_args)

    old_argv = sys.argv
    try:
        sys.argv = argv
        stcli.main()
    finally:
        sys.argv = old_argv


@app.command(context_settings={"allow_extra_args": True, "ignore_unknown_options": True})
def ui(
    ctx: typer.Context,
    headless: bool = HEADLESS_OPTION,
) -> None:
    """Launch the Streamlit UI."""
    app_file = _streamlit_app_file()
    _run_streamlit(app_file, headless=headless, extra_args=list(ctx.args))


@app.command()
def simulate(
    config: Path = CONFIG_OPTION,
    start: str = START_OPTION,
    days: int = DAYS_OPTION,
) -> None:
    """Run a simulation and print the result as JSON to stdout."""
    s = _parse_date(start)
    run = run_from_config(config=config, start=s, days=days)

    payload = {
        "metrics": metrics_to_dict(run.metrics),
        "series": run.df.to_dict(orient="records"),
    }
    # NOTE: This is primarily for quick inspection; `export` is the stable artifact writer.
    typer.echo(json.dumps(payload, indent=2, default=str))


@app.command()
def export(
    config: Path = CONFIG_OPTION,
    start: str = START_OPTION,
    days: int = DAYS_OPTION,
    out: Path = OUT_OPTION,
    series_format: str = SERIES_FORMAT_OPTION,
    events: bool = EVENTS_OPTION,
    overwrite: bool = OVERWRITE_OPTION,
) -> None:
    """Run a simulation and write stable artifacts to disk."""
    s = _parse_date(start)
    run = run_from_config(config=config, start=s, days=days)

    try:
        write_run(
            out_dir=out.resolve(),
            df=run.df,
            metrics=run.metrics,
            config_path=run.config_path,
            start=start,
            days=days,
            series_format=series_format,
            include_events=events,
            overwrite=overwrite,
        )
    except FileExistsError as exc:
        raise typer.BadParameter(str(exc)) from exc

    typer.echo(f"Wrote artifacts to: {out.resolve()}")


@app.command()
def compare(
    a: Path = A_OPTION,
    b: Path = B_OPTION,
    start: str = START_OPTION,
    days: int = DAYS_OPTION,
    out: Path = OUT_OPTION,
    series: bool = SERIES_OPTION,
    report: Path | None = REPORT_OPTION,
    threshold: float = THRESHOLD_OPTION,
    overwrite: bool = OVERWRITE_OPTION,
) -> None:
    """Compare two configs (deep compare + optional series diff + optional Markdown report)."""

    s = _parse_date(start)
    run_a = run_from_config(config=a, start=s, days=days)
    run_b = run_from_config(config=b, start=s, days=days)

    out_dir = out.resolve()
    out_dir.mkdir(parents=True, exist_ok=True)

    compare_path = out_dir / "compare.json"
    diff_path = out_dir / "series_diff.csv"

    # Report path: if a relative path is provided, write it under out_dir
    report_path: Path | None = None
    if report is not None:
        report_path = report if report.is_absolute() else (out_dir / report)

    # Overwrite protections
    to_check: list[Path] = [compare_path]
    if series:
        to_check.append(diff_path)
    if report_path is not None:
        to_check.append(report_path)

    if not overwrite:
        for p in to_check:
            if p.exists():
                raise typer.BadParameter(f"Refusing to overwrite {p} (use --overwrite)")

    payload = build_compare_payload(
        metrics_a=run_a.metrics,
        metrics_b=run_b.metrics,
        df_a=run_a.df,
        df_b=run_b.df,
        start=start,
        days=days,
        threshold=threshold,
    )

    compare_path.write_text(
        json.dumps(payload, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )

    if series:
        # Use the same default columns used by build_compare_payload to keep UX aligned
        cols = payload.get("meta", {}).get("series_columns", [])
        diff = series_diff(run_a.df, run_b.df, columns=list(cols))
        diff.to_csv(diff_path, index=False, lineterminator="\n")

    if report_path is not None:
        md = render_compare_markdown(payload, series_diff_path=("series_diff.csv" if series else None))
        report_path.write_text(md, encoding="utf-8", newline="\n")

    typer.echo(f"Wrote compare artifacts to: {out_dir}")




@app.command(context_settings={"allow_extra_args": True, "ignore_unknown_options": True})
def api(
    ctx: typer.Context,
    host: str = "127.0.0.1",
    port: int = 8000,
    reload: bool = False,
) -> None:
    """Run the CashSim FastAPI server (Uvicorn)."""

    args = [
        "uvicorn",
        "cashsim.api.app:app",
        "--host",
        host,
        "--port",
        str(port),
    ]

    if reload:
        args.append("--reload")

    # pass-through for things like: --log-level debug, --workers 2
    args.extend(list(ctx.args))

    raise SystemExit(subprocess.call(args))







def main() -> None:
    # Compatibility: `cashsim` launches the UI.
    # Canonical UI: `cashsim ui`.
    if len(sys.argv) == 1:
        sys.argv = [sys.argv[0], "ui"]

    app()


if __name__ == "__main__":
    main()
</file>

<file path="pyproject.toml">
[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "cashsim"
version = "0.1.0"
description = "Cash flow simulator with Streamlit UI"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "You" }]
dependencies = [
  # Core
  "pydantic>=2.10,<3",
  "pandas>=2.2.3,<3.0",          # 2.2.3+ has 3.13 wheels
  "python-dateutil>=2.9.0.post0", # relativedelta
  # UI
  "streamlit>=1.49,<2.0",
  # Streamlit dataframes use Arrow underneath; keep it modern for 3.13 wheels
  "pyarrow>=22,<23",
  # NEW: Google Sheets integration
  "gspread>=6,<7",
  "google-auth>=2.0",
  "gspread-dataframe>=2.0.0",
  "typer>=0.12,<1.0",
  "fastapi>=0.110,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "pydantic-settings>=2.0,<3.0",

]

[project.optional-dependencies]
dev = [
  "ruff>=0.5.0",
  "pytest>=8.2",
  "httpx>=0.27,<1.0",
  "mypy>=1.10",
  "types-python-dateutil>=2.9.0.20250822"
,
  "pyright"
,
  "pandas-stubs"
,
  "MonkeyType",
  "pre-commit",
  "bandit",
  "hypothesis>=6.112"
]

[project.scripts]
cashsim = "cashsim.cli:main"

[tool.ruff]
line-length = 100
target-version = "py312"
force-exclude = true

extend-exclude = ["typings/**", "**/*.pyi"]
[tool.ruff.lint]
select = [
  "E", "F",   # pyflakes/pycodestyle
  "I",        # import sorting
  "UP",       # pyupgrade
  "B",        # bugbear
  "SIM",      # flake8-simplify
  "ANN"       # type annotations
]

[tool.ruff.format]
quote-style = "double"

[tool.pyright]
# Use the src layout
venvPath = "."
venv = ".venv"
reportMissingTypeStubs = "warning"
typeCheckingMode = "strict"

[tool.hatch.build.targets.wheel]
packages = ["src/cashsim"]
sources = ["src"]

[tool.ruff.lint.flake8-annotations]
allow-star-arg-any = true

[tool.ruff.lint.per-file-ignores]
"typings/**" = ["ALL"]
"**/*.pyi" = ["ALL"]
"src/cashsim/sim/types.py" = ["UP"]
</file>

<file path="pyrightconfig.json">
{
  "typeCheckingMode": "standard",
  "executionEnvironments": [
    {
      "root": "src"
    },
    {
      "root": "./src"
    },
    {
      "root": "./tests",
      "extraPaths": [
        "./src"
      ]
    }
  ],
  "useLibraryCodeForTypes": true,
  "reportUnknownVariableType": "warning",
  "reportUnknownMemberType": "warning",
  "reportUnknownArgumentType": "warning",
  "reportUnknownParameterType": "warning",
  "reportUnknownLambdaType": "warning",
  "reportMissingTypeStubs": "warning",
  "reportMissingTypeArgument": "warning",
  "reportMissingParameterType": "warning",
  "reportArgumentType": "warning",
  "reportAttributeAccessIssue": "warning",
  "reportMissingImports": "warning",
  "reportUntypedFunctionDecorator": "warning",
  "reportUnusedVariable": "warning",
  "reportCallIssue": "warning",
  "include": [
    "src",
    "tests"
  ],
  "extraPaths": [
    "src"
  ],
  "pythonVersion": "3.14",
  "venvPath": ".",
  "venv": ".venv"
}
</file>

<file path="src/cashsim/io/gsheets.py">
from __future__ import annotations

from collections.abc import Mapping
from typing import Any, cast

import google.auth
import gspread
import pandas as pd
import streamlit as st
from google.auth.credentials import Credentials
from google.oauth2 import service_account
from gspread_dataframe import (
    get_as_dataframe,
    set_with_dataframe,
)

from cashsim._typing_shims import cache_data, cache_resource

SHEETS_READONLY = ["https://www.googleapis.com/auth/spreadsheets.readonly"]
SHEETS_RW = ["https://www.googleapis.com/auth/spreadsheets"]


def _choose_creds(*, readonly: bool, sa_info: Mapping[str, Any] | None) -> Credentials:
    """Prefer st.secrets if provided; otherwise fall back to ADC (keyless on GCP)."""
    scopes = SHEETS_READONLY if readonly else SHEETS_RW
    if sa_info:
        return service_account.Credentials.from_service_account_info(dict(sa_info), scopes=scopes)
    if "gcp_service_account" in st.secrets:
        return service_account.Credentials.from_service_account_info(
            dict(st.secrets["gcp_service_account"]), scopes=scopes
        )

    creds, _ = google.auth.default(scopes=scopes)
    return cast(Credentials, creds)


@cache_resource(show_spinner=False)
def gs_client(sa_info: Mapping[str, Any] | None = None, *, readonly: bool = True) -> gspread.Client:
    """Singleton gspread client; cached across reruns & sessions."""
    creds = _choose_creds(readonly=readonly, sa_info=sa_info)
    return gspread.authorize(creds)


@cache_data(show_spinner=False)
def batch_read_values(
    sheet_url: str, ranges: list[str], sa_info: Mapping[str, Any] | None = None
) -> dict[str, list[list[Any]]]:
    """
    Read multiple A1 ranges across sheets in one call via values:batchGet.
    Returns {range: values}. Missing ranges return [].
    """
    sh: gspread.Spreadsheet = gs_client(sa_info, readonly=True).open_by_url(sheet_url)
    resp: dict[str, Any] = sh.values_batch_get(ranges)
    out: dict[str, list[list[Any]]] = {}
    for vr in resp.get("valueRanges", []):
        out[vr.get("range", "")] = vr.get("values", [])

    for r in ranges:
        out.setdefault(r, [])
    return out


def batch_write_values(
    sheet_url: str,
    value_ranges: list[dict[str, Any]],
    *,
    value_input_option: str = "USER_ENTERED",
    sa_info: Mapping[str, Any] | None = None,
) -> dict:
    """
    Write multiple ranges in one call via values:batchUpdate.
    value_ranges = [{"range": "Sheet1!A1:B3", "values": [[...], ...]}, ...]
    """
    sh: gspread.Spreadsheet = gs_client(sa_info, readonly=False).open_by_url(sheet_url)
    body = {"valueInputOption": value_input_option, "data": value_ranges}
    return sh.values_batch_update(body)


@cache_data(show_spinner=False)
def read_wishlist_by_url(
    sheet_url: str, worksheet_name: str, sa_info: Mapping[str, Any] | None = None
) -> pd.DataFrame:
    """
    Read a wishlist sheet into a DataFrame using batched client.
    Expected columns:
      - Item, Category, Price, Name, Days I have to Buy Again, Priority
    """
    client = gs_client(sa_info, readonly=True)
    ws = client.open_by_url(sheet_url).worksheet(worksheet_name)
    df_any = get_as_dataframe(ws, evaluate_formulas=True, header=0).dropna(how="all")
    df: pd.DataFrame = cast(pd.DataFrame, df_any)

    wanted = ["Item", "Category", "Price", "Name", "Days I have to Buy Again", "Priority"]
    missing = [c for c in wanted if c not in df.columns]
    if missing:
        raise ValueError(f"Missing columns: {missing}")

    df["Price"] = pd.to_numeric(df["Price"], errors="coerce").fillna(0.0)
    df["Priority"] = (
        pd.to_numeric(df["Priority"], downcast="integer", errors="coerce").fillna(0).astype(int)
    )
    df["Days I have to Buy Again"] = (
        pd.to_numeric(df["Days I have to Buy Again"], errors="coerce").fillna(0).astype(int)
    )
    return df[wanted]


def write_dataframe(
    sheet_url: str,
    worksheet_name: str,
    df: pd.DataFrame,
    *,
    clear_first: bool = True,
    sa_info: Mapping[str, Any] | None = None,
) -> None:
    """Clear+set a DataFrame using one or two value-range calls (fast)."""
    client: gspread.Client = gs_client(sa_info, readonly=False)
    ws: gspread.Worksheet = client.open_by_url(sheet_url).worksheet(worksheet_name)
    if clear_first:
        ws.clear()
    set_with_dataframe(ws, df, include_index=False, include_column_header=True, resize=True)


@cache_data(show_spinner=False)
def load_transactions(sheet_url: str) -> pd.DataFrame:
    gc = gs_client(sa_info=None, readonly=True)
    ws = gc.open_by_url(sheet_url).worksheet("Sheet1")
    values = ws.get_all_records()
    return pd.DataFrame(values)
</file>

<file path="src/cashsim/ui/streamlit_app.py">
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))


import json
import os
from datetime import date
from typing import Any

import pandas as pd
import streamlit as st

from cashsim.analytics.break_even import break_even_grid
from cashsim.analytics.snapshot import monthly_snapshot
from cashsim.io.config_io import load_config, save_config
from cashsim.io.gsheets import read_wishlist_by_url
from cashsim.models import Dials
from cashsim.planning.planner import (
    constant_target_from_schedule,
    plan_min_daily_earnings,
    plan_variable_daily_earnings,
)
from cashsim.sim.core import simulate_month
from cashsim.sim.types import SimMetrics
from cashsim.ui.components import (
    bills_editor,
    cc_editor,
    iou_editor,
    number_dials,
    oneoff_editor,
    strategy_toggles,
)
from cashsim.ui.formatters import stringify_events_columns
from cashsim.ui.state import dials_from_state, init_session_once
from cashsim.utils.date_utils import next_due_date_cached as next_due_date

pd.options.mode.copy_on_write = True

st.set_page_config(page_title="CashSim — Single Page", layout="wide")
init_session_once()


def _fingerprint(dials: Dials) -> str:
    """
    Stable, JSON-serializable fingerprint for Streamlit caching.
    Use pydantic v2's mode='json' so dates become ISO strings.
    """
    payload = dials.model_dump(mode="json")
    return json.dumps(payload, sort_keys=True)


@st.cache_data(show_spinner=False)
def run_sim(dials_json: str, start: date, days: int) -> tuple[pd.DataFrame, SimMetrics]:
    d = Dials.model_validate_json(dials_json)
    return simulate_month(d, start=start, days=days)


@st.cache_data(show_spinner=False)
def run_break_even(dials_json: str, days: int = 31) -> pd.DataFrame:
    d = Dials.model_validate_json(dials_json)
    candidates = [float(i) for i in range(0, 201)]
    return break_even_grid(d, candidates, days=days)


@st.cache_data(show_spinner=False)
def _load_config_cached(path: str, mtime: float) -> Dials:
    return load_config(path)


def render_inputs_tab() -> None:
    st.header("Inputs")
    st.subheader("Config I/O")

    cfg_path = st.text_input("path", value="config.json")
    c1, c2, _c3 = st.columns([1, 1, 1])

    if c1.button("Load file", use_container_width=True):
        try:
            mtime = os.path.getmtime(cfg_path) if os.path.exists(cfg_path) else 0.0
            d = _load_config_cached(cfg_path, mtime)

            st.session_state.update(
                {
                    "current_cash": float(d.current_cash),
                    "safety_cushion": float(d.safety_cushion),
                    "weekday_earnings": float(d.weekday_earnings),
                    "gas_pct": float(d.gas_pct),
                    "gas_fill_size": float(d.gas_fill_size),
                    "bill_table": pd.DataFrame([b.model_dump() for b in d.bills]),
                    "cc_table": pd.DataFrame([c.model_dump() for c in d.credit_cards]),
                    "iou_table": pd.DataFrame([x.model_dump() for x in d.ious]),
                    "oneoff_table": pd.DataFrame([o.model_dump() for o in d.oneoffs]),
                    "interest_mode": d.interest_mode,
                    "extra_strategy": d.extra_strategy,
                    "invest": d.invest.model_dump(),
                    "blackouts_df": pd.DataFrame(
                        {"date": pd.to_datetime(getattr(d, "blackouts", []), errors="coerce")}
                    ),
                }
            )
            st.rerun()
        except Exception as e:
            st.error(str(e))

    uploaded = st.file_uploader("or upload json", type="json")
    if uploaded is not None:
        try:
            data = json.load(uploaded)
            d = Dials.model_validate(data)
            st.session_state.update(
                {
                    "current_cash": float(d.current_cash),
                    "safety_cushion": float(d.safety_cushion),
                    "weekday_earnings": float(d.weekday_earnings),
                    "gas_pct": float(d.gas_pct),
                    "gas_fill_size": float(d.gas_fill_size),
                    "bill_table": pd.DataFrame([b.model_dump() for b in d.bills]),
                    "cc_table": pd.DataFrame([c.model_dump() for c in d.credit_cards]),
                    "iou_table": pd.DataFrame([x.model_dump() for x in d.ious]),
                    "oneoff_table": pd.DataFrame([o.model_dump() for o in d.oneoffs]),
                    "interest_mode": d.interest_mode,
                    "extra_strategy": d.extra_strategy,
                    "invest": d.invest.model_dump(),
                    "blackouts_df": pd.DataFrame(
                        {"date": pd.to_datetime(getattr(d, "blackouts", []), errors="coerce")}
                    ),
                }
            )
            st.rerun()
        except Exception as e:
            st.error(str(e))

    def _dials_with_blackouts() -> Dials:
        base_dials = dials_from_state()

        bdf = st.session_state.get("blackouts_df", pd.DataFrame({"date": []})).copy()
        if "date" not in bdf.columns:
            bdf["date"] = pd.Series([], dtype="datetime64[ns]")
        dates: list[date] = []
        for v in pd.to_datetime(bdf["date"], errors="coerce"):
            if pd.notna(v):
                dates.append(pd.to_datetime(v).date())
        return base_dials.model_copy(update={"blackouts": dates})

    st.download_button(
        "Download current config",
        data=_dials_with_blackouts().model_dump_json(indent=2),
        file_name="config.json",
        mime="application/json",
    )

    if c2.button("Save file", use_container_width=True):
        try:
            save_config(_dials_with_blackouts(), cfg_path)
            st.success(f"Saved {cfg_path}")
        except Exception as e:
            st.error(str(e))

    st.divider()
    number_dials()
    strategy_toggles()


def render_bills_tab() -> None:
    st.header("Bills")
    st.session_state["bill_table"] = bills_editor(st.session_state["bill_table"])

    today = date.today()
    dials_tmp = dials_from_state()
    bills_rows = []
    for b in dials_tmp.bills:
        nd = next_due_date(today, b.usual_day)
        bills_rows.append(
            {
                "type": "bill",
                "name": b.name,
                "next_due": nd,
                "days_remaining": (nd - today).days,
                "amount": round(b.amount, 2),
            }
        )

    cc_rows = []
    for c in dials_tmp.credit_cards:
        due = next_due_date(today, c.due_day)
        est_min = round(max(c.min_floor, c.balance * c.min_pct), 2)
        cc_rows.append(
            {
                "type": "cc_min",
                "name": f"{c.name} (min)",
                "next_due": due,
                "days_remaining": (due - today).days,
                "amount": est_min,
            }
        )

    rolled_df = pd.DataFrame(bills_rows + cc_rows).sort_values(
        ["next_due", "type", "name"], kind="mergesort"
    )
    st.subheader("Rolled-forward preview (bills + estimated CC mins)")
    st.dataframe(rolled_df, use_container_width=True, hide_index=True)
    st.caption(
        "CC minimums here are estimates from current balances; "
        "actual mins are locked from statement balances."
    )


def render_debt_tab() -> None:
    st.header("Credit cards & IOUs")
    st.session_state["cc_table"] = cc_editor(st.session_state["cc_table"])
    st.session_state["iou_table"] = iou_editor(st.session_state["iou_table"])

    st.subheader("Upcoming due-date events (interest postings, minimums, extras)")
    dials = dials_from_state()
    fingerprint = _fingerprint(dials)
    sim_df, _sim_metrics = run_sim(fingerprint, date.today(), 60)

    ev = sim_df.explode("cc_events", ignore_index=True)
    if bool("cc_events" in ev.columns and ev["cc_events"].notna().any()):
        base_df = ev.loc[ev["cc_events"].notna(), ["date", "cc_events"]].reset_index(drop=True)
        events_df = pd.json_normalize(base_df["cc_events"].tolist()).reset_index(drop=True)
        if "date" in events_df.columns:
            events_df = events_df.rename(columns={"date": "event_date"})
        events_df.insert(0, "date", base_df["date"].reset_index(drop=True))
        events_df = events_df.loc[:, ~events_df.columns.duplicated()]
        pref = [
            "date",
            "account",
            "finance_charge",
            "minimum_paid",
            "extra_paid",
            "new_balance",
            "surplus_used",
            "reserve_needed",
            "reserve_7d_bills",
            "reserve_7d_mins",
            "reserve_7d_gas",
            "event_date",
        ]
        cols = [c for c in pref if c in events_df.columns]
        st.dataframe(events_df[cols], use_container_width=True, hide_index=True)
    else:
        st.write("No due-date events within 60 days.")


def render_oneoffs_tab() -> None:
    st.header("One-off (sinking) expenses")
    st.session_state["oneoff_table"] = oneoff_editor(st.session_state["oneoff_table"])

    st.subheader("Contributions & payments (next 60 days)")
    dials = dials_from_state()
    fingerprint = _fingerprint(dials)
    sim_df, _sim_metrics = run_sim(fingerprint, date.today(), 60)

    events: list[dict[str, Any]] = []
    for _, r in sim_df.iterrows():
        for item in r.get("oneoff_contribs") or []:
            try:
                name, amt = item
            except Exception:
                name, amt = str(item), float("nan")
            events.append({"date": r["date"], "event": "contrib", "name": name, "amount": amt})
        for item in r.get("oneoff_paid") or []:
            try:
                name, amt = item
            except Exception:
                name, amt = str(item), float("nan")
            events.append({"date": r["date"], "event": "paid", "name": name, "amount": amt})

    if events:
        oneoff_events_df = (
            pd.DataFrame(events).sort_values(["date", "event", "name"]).reset_index(drop=True)
        )
        st.dataframe(oneoff_events_df, use_container_width=True, hide_index=True)
    else:
        st.write("No one-off contributions or payments in the next 60 days.")


def render_sim_tab() -> None:
    st.header("Simulation")
    days = st.slider("days to simulate", min_value=28, max_value=186, value=31, step=1)
    dials = dials_from_state()
    fingerprint = _fingerprint(dials)
    sim_df, sim_metrics = run_sim(fingerprint, date.today(), days)

    left_col, right_col = st.columns([1.2, 1.0])
    with left_col:
        st.subheader("Daily balance (projection)")
        st.line_chart(sim_df.set_index("date")[["balance"]], use_container_width=True)
        st.dataframe(stringify_events_columns(sim_df), use_container_width=True, hide_index=True)

    with right_col:
        st.subheader("Key results")
        st.metric(
            "Min balance",
            f"${sim_metrics.min_balance:,.2f}",
            help=str(sim_metrics.min_balance_date),
        )
        st.metric(
            "Overdraft date",
            "-"
            if sim_metrics.first_negative_date is None
            else str(sim_metrics.first_negative_date),
        )
        st.metric(
            "Cushion breach",
            "-"
            if sim_metrics.cushion_breach_date is None
            else str(sim_metrics.cushion_breach_date),
        )
        st.metric(
            "Upcoming bills total (within horizon)", f"${sim_metrics.total_upcoming_bills:,.2f}"
        )
        st.metric(
            "Est. unposted interest (cards)", f"${sim_metrics.accrued_interest_estimate:,.2f}"
        )


def render_analytics_tab() -> None:
    st.header("Analytics")
    dials = dials_from_state()

    st.subheader("Break-even daily earnings search (0–200)")
    fingerprint = _fingerprint(dials)
    be_df = run_break_even(fingerprint, days=31)
    st.dataframe(be_df, use_container_width=True, hide_index=True)

    st.subheader("Monthly snapshot (6 months)")
    snap = monthly_snapshot(dials, months=6)
    st.json(snap)


def render_planner_tab() -> None:
    st.header("Planner")

    dials = dials_from_state()
    mode = st.radio(
        "Mode",
        ["Constant daily target", "Day-by-day plan"],
        horizontal=True,
        key="planner_mode",
        help=(
            "Pick a single daily amount to avoid overdraft, "
            "or generate a daily schedule with caps/constraints."
        ),
    )

    if mode == "Constant daily target":
        st.subheader("Minimum constant daily earnings")
        horizon_days = st.slider(
            "days to satisfy (horizon)",
            min_value=28,
            max_value=186,
            value=60,
            step=1,
            key="planner_days_constant",
        )

        res_const = plan_min_daily_earnings(dials, days=horizon_days)
        if res_const.ok:
            st.success(
                f"Minimum constant daily earnings to avoid overdraft in {horizon_days} days: "
                f"${res_const.daily_target:,.2f}"
            )
            st.caption(
                f"Simulated min balance: ${res_const.min_balance:,.2f} "
                f"(first negative: {res_const.first_negative_date})"
            )
        else:
            st.error("Could not find a feasible daily target up to a very large upper bound.")

    else:
        st.subheader("Day-by-day earnings schedule")

        c1, c2, c3, c4 = st.columns([1, 1, 1, 1])
        horizon_days = c1.slider(
            "Horizon (days)", min_value=28, max_value=186, value=60, step=1, key="planner_days_var"
        )
        safety_target = c2.selectbox(
            "Safety target",
            ["zero", "cushion"],
            index=0,
            key="planner_safety_target",
            help="Keep end-of-day ≥ 0 (zero) or ≥ your safety cushion (cushion).",
        )
        daily_cap_val = c3.number_input(
            "Daily cap (optional)",
            min_value=0.0,
            step=10.0,
            value=0.0,
            key="planner_cap",
            help="0 = no cap",
        )
        future_hint = c4.number_input(
            "Future daily earnings hint",
            min_value=0.0,
            step=10.0,
            value=float(dials.weekday_earnings),
            key="planner_future_hint",
            help="Used only to predict gas fill-ups in next-7-days reserve.",
        )
        cap = None if daily_cap_val <= 0 else float(daily_cap_val)

        st.markdown("**No-drive dates** (add rows as needed)")
        st.session_state.setdefault(
            "blackouts_df", pd.DataFrame({"date": pd.Series([], dtype="datetime64[ns]")})
        )
        _blackouts = st.session_state["blackouts_df"].copy()
        if "date" not in _blackouts.columns:
            _blackouts["date"] = pd.Series([], dtype="datetime64[ns]")
        else:
            _blackouts["date"] = pd.to_datetime(_blackouts["date"], errors="coerce")

        st.session_state["blackouts_df"] = st.data_editor(
            _blackouts,
            num_rows="dynamic",
            column_config={"date": st.column_config.DateColumn("date", format="iso8601")},
            use_container_width=True,
            key="blackouts_editor",
        )

        planner_blackouts: list[date] = []
        for v in pd.to_datetime(st.session_state["blackouts_df"]["date"], errors="coerce"):
            if pd.notna(v):
                planner_blackouts.append(pd.to_datetime(v).date())

        res_var = plan_variable_daily_earnings(
            dials,
            days=horizon_days,
            safety_target="zero" if safety_target == "zero" else "cushion",
            daily_cap=cap,
            future_daily_hint=future_hint,
            blackout_dates=planner_blackouts,
        )
        df = pd.DataFrame([r.__dict__ for r in res_var.rows])
        st.metric(
            "Min projected balance",
            f"${res_var.min_balance:,.2f}",
            help=str(res_var.min_balance_date),
        )
        if not res_var.ok:
            st.warning("Plan hits the daily cap on at least one day ...")

        if not df.empty:
            df["date"] = pd.to_datetime(df["date"])

            st.line_chart(
                df.set_index("date")[["earn_required", "earn_capped"]],
                use_container_width=True,
            )

            st.dataframe(
                df,
                use_container_width=True,
                hide_index=True,
                column_config={
                    "date": st.column_config.DateColumn("date", format="YYYY-MM-DD"),
                    "earn_required": st.column_config.NumberColumn("earn_required", format="$%.2f"),
                    "earn_capped": st.column_config.NumberColumn("earn_capped", format="$%.2f"),
                    "cap_exceeded": st.column_config.CheckboxColumn("cap_exceeded"),
                    "end_balance": st.column_config.NumberColumn("end_balance", format="$%.2f"),
                    "reserve_7d_total": st.column_config.NumberColumn(
                        "reserve_7d_total", format="$%.2f"
                    ),
                    "bills_today": st.column_config.NumberColumn("bills_today", format="$%.2f"),
                    "mins_today": st.column_config.NumberColumn("mins_today", format="$%.2f"),
                    "oneoffs_today": st.column_config.NumberColumn("oneoffs_today", format="$%.2f"),
                    "gas_fill_cost_today": st.column_config.NumberColumn(
                        "gas_fill_cost_today", format="$%.2f"
                    ),
                    "gas_bucket_end": st.column_config.NumberColumn(
                        "gas_bucket_end", format="$%.2f"
                    ),
                },
            )

            const_target = constant_target_from_schedule(res_var.rows, planner_blackouts)
            st.metric(
                "Constant target on working days",
                f"${const_target:,.2f}",
                help=(
                    "Hit at least this number on each day you can drive to stay above your "
                    "chosen safety target."
                ),
            )
        else:
            st.info("No rows to display.")


def render_wishlist_tab() -> None:
    st.header("Wishlist (Google Sheets)")

    st.markdown(
        "Store your **service account JSON** under `st.secrets['gcp_service_account']`.\n"
        "Share the Google Sheet with that service account’s **client_email**."
    )
    sheet_url = st.text_input("Sheet URL")
    ws_name = st.text_input("Worksheet name", value="Sheet1")
    horizon = st.slider("Planning horizon (days)", 30, 180, 120, 1)

    if st.button("Fetch & Predict"):
        try:
            sa = st.secrets["gcp_service_account"]
            wish = read_wishlist_by_url(sheet_url, ws_name, sa)
            st.subheader("Wishlist data")
            st.dataframe(wish, use_container_width=True, hide_index=True)

            dials = dials_from_state()

            bdf = st.session_state.get("blackouts_df", pd.DataFrame({"date": []})).copy()
            if "date" not in bdf.columns:
                bdf["date"] = pd.Series([], dtype="datetime64[ns]")
            else:
                bdf["date"] = pd.to_datetime(bdf["date"], errors="coerce")

            wishlist_blackouts: list[date] = []
            for v in bdf["date"]:
                if pd.notna(v):
                    wishlist_blackouts.append(pd.to_datetime(v).date())

            plan_for_wishlist = plan_variable_daily_earnings(
                dials,
                days=horizon,
                safety_target="zero",
                daily_cap=None,
                future_daily_hint=float(dials.weekday_earnings),
                blackout_dates=wishlist_blackouts,
            )
            plan_df = pd.DataFrame([r.__dict__ for r in plan_for_wishlist.rows])
            plan_df["date"] = pd.to_datetime(plan_df["date"])
            safe_floor = 0.0
            plan_df["safe_surplus"] = (
                plan_df["end_balance"] - (safe_floor + plan_df["reserve_7d_total"])
            ).round(2)

            items = wish.sort_values(["Priority", "Item"], ascending=[False, True]).to_dict(
                "records"
            )
            remaining = {it["Item"]: float(it["Price"]) for it in items}
            first_date = {it["Item"]: None for it in items}

            for _, r in plan_df.iterrows():
                avail = max(0.0, float(r["safe_surplus"]))
                if avail <= 0:
                    continue
                for it in items:
                    name = it["Item"]
                    if remaining[name] <= 0:
                        continue
                    take = min(avail, remaining[name])
                    remaining[name] = round(remaining[name] - take, 2)
                    avail = round(avail - take, 2)
                    if remaining[name] <= 0 and first_date[name] is None:
                        first_date[name] = r["date"].date()
                    if avail <= 0:
                        break

            out_rows = []
            today = date.today()
            for it in items:
                nm = it["Item"]
                when = first_date[nm]
                out_rows.append(
                    {
                        "Item": nm,
                        "Price": it["Price"],
                        "Priority": it["Priority"],
                        "Earliest fully-funded": when,
                        "Days until": None if when is None else (when - today).days,
                    }
                )
            out = pd.DataFrame(out_rows).sort_values(
                ["Earliest fully-funded", "Priority"], na_position="last", ascending=[True, False]
            )
            st.subheader("Earliest good buy dates (by priority)")
            st.dataframe(out, use_container_width=True, hide_index=True)
        except KeyError:
            st.error("Missing st.secrets['gcp_service_account']. See instructions below.")
        except Exception as e:
            st.error(str(e))


st.title("CashSim (single-page)")
st.caption(
    "Edit inputs, bills, debt, and one-offs — then simulate and analyze break-even, "
    "and plan a daily target — all here."
)

tab_inputs, tab_bills, tab_debt, tab_oneoffs, tab_sim, tab_analytics, tab_planner, tab_wishlist = (
    st.tabs(
        ["Inputs", "Bills", "Debt", "One-offs", "Simulation", "Analytics", "Planner", "Wishlist"]
    )
)

with tab_inputs:
    render_inputs_tab()
with tab_bills:
    render_bills_tab()
with tab_debt:
    render_debt_tab()
with tab_oneoffs:
    render_oneoffs_tab()
with tab_sim:
    render_sim_tab()
with tab_analytics:
    render_analytics_tab()
with tab_planner:
    render_planner_tab()
with tab_wishlist:
    render_wishlist_tab()
</file>

</files>
